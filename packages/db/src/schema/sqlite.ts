// GENERATED by packages/db/scripts/gen-drizzle.mjs from db/schema.json — do not edit.
// 353 tables · 4085 columns
/* eslint-disable */
import { sqliteTable, text, integer, numeric, real, index, uniqueIndex } from 'drizzle-orm/sqlite-core';

// ============ Core · tenancy, identity, access ============
/** One row per school (tenant). Settings JSON holds feature flags and policies. */
export const schools = sqliteTable('schools', {
  id: text('id').primaryKey(),
  code: text('code').notNull(),
  name: text('name').notNull(),
  nameBn: text('name_bn'),
  institutionType: text('institution_type', { enum: ['school', 'college', 'school_college', 'madrasa', 'kindergarten', 'coaching', 'university'] }).notNull(),
  board: text('board'),
  eiin: text('eiin'),
  mpoCode: text('mpo_code'),
  address: text('address', { mode: 'json' }),
  phone: text('phone'),
  email: text('email'),
  website: text('website'),
  logoFileId: text('logo_file_id'),
  timezone: text('timezone').notNull(),
  currency: text('currency').notNull(),
  locale: text('locale').notNull(),
  planId: text('plan_id'),
  status: text('status', { enum: ['trial', 'active', 'suspended', 'closed'] }).notNull(),
  trialEndsAt: text('trial_ends_at'),
  settings: text('settings', { mode: 'json' }),
  theme: text('theme', { mode: 'json' }),
  customDomain: text('custom_domain'),
  onboardedAt: text('onboarded_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
  deletedAt: text('deleted_at'),
});

/** Branches of one school; users can be scoped to a campus. */
export const campuses = sqliteTable('campuses', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  code: text('code').notNull(),
  address: text('address', { mode: 'json' }),
  phone: text('phone'),
  isMain: integer('is_main', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_campuses_school_id_code'.slice(0, 63)).on(t.schoolId, t.code),
]);

/** Login identity for admins, staff, students and guardians. Phone is the primary login in Bangladesh. */
export const users = sqliteTable('users', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userType: text('user_type', { enum: ['admin', 'staff', 'student', 'guardian', 'alumni', 'vendor', 'api'] }).notNull(),
  username: text('username'),
  email: text('email'),
  phone: text('phone'),
  passwordHash: text('password_hash'),
  displayName: text('display_name').notNull(),
  avatarFileId: text('avatar_file_id'),
  locale: text('locale'),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  emailVerifiedAt: text('email_verified_at'),
  phoneVerifiedAt: text('phone_verified_at'),
  twoFactorSecret: text('two_factor_secret'),
  twoFactorEnabled: integer('two_factor_enabled', { mode: 'boolean' }).notNull(),
  sessionEpoch: integer('session_epoch').notNull(),
  lastLoginAt: text('last_login_at'),
  failedLogins: integer('failed_logins').notNull(),
  lockedUntil: text('locked_until'),
  preferences: text('preferences', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
  deletedAt: text('deleted_at'),
}, (t) => [
  uniqueIndex('uq_users_school_id_email'.slice(0, 63)).on(t.schoolId, t.email),
  uniqueIndex('uq_users_school_id_phone'.slice(0, 63)).on(t.schoolId, t.phone),
  uniqueIndex('uq_users_school_id_username'.slice(0, 63)).on(t.schoolId, t.username),
]);

/** Named permission sets (Principal, Accountant, Teacher…). System roles are seeded and protected. */
export const roles = sqliteTable('roles', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  slug: text('slug').notNull(),
  description: text('description'),
  isSystem: integer('is_system', { mode: 'boolean' }).notNull(),
  level: integer('level').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_roles_school_id_slug'.slice(0, 63)).on(t.schoolId, t.slug),
]);

/** Global catalogue: module.resource.action (fees.invoice.create). */
export const permissions = sqliteTable('permissions', {
  id: text('id').primaryKey(),
  keyName: text('key_name').notNull(),
  module: text('module').notNull(),
  description: text('description'),
});

/** Role ↔ permission. */
export const rolePermissions = sqliteTable('role_permissions', {
  id: text('id').primaryKey(),
  roleId: text('role_id').notNull(),
  permissionId: text('permission_id').notNull(),
}, (t) => [
  uniqueIndex('uq_role_permissions_role_id_permission_id'.slice(0, 63)).on(t.roleId, t.permissionId),
]);

/** User ↔ role, optionally limited to a campus. */
export const userRoles = sqliteTable('user_roles', {
  id: text('id').primaryKey(),
  userId: text('user_id').notNull(),
  roleId: text('role_id').notNull(),
  campusId: text('campus_id'),
}, (t) => [
  uniqueIndex('uq_user_roles_user_id_role_id_campus_id'.slice(0, 63)).on(t.userId, t.roleId, t.campusId),
]);

/** Server sessions per device. Valid only while session.epoch == users.session_epoch. */
export const authSessions = sqliteTable('auth_sessions', {
  id: text('id').primaryKey(),
  userId: text('user_id').notNull(),
  tokenHash: text('token_hash').notNull(),
  epoch: integer('epoch').notNull(),
  deviceName: text('device_name'),
  deviceId: text('device_id'),
  platform: text('platform', { enum: ['web', 'android', 'ios', 'pwa', 'api'] }),
  ip: text('ip'),
  userAgent: text('user_agent'),
  expiresAt: text('expires_at').notNull(),
  revokedAt: text('revoked_at'),
  lastSeenAt: text('last_seen_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Web Push (VAPID) subscriptions per user/device; FCM/APNs tokens for native apps later. */
export const pushSubscriptions = sqliteTable('push_subscriptions', {
  id: text('id').primaryKey(),
  userId: text('user_id').notNull(),
  kind: text('kind', { enum: ['webpush', 'fcm', 'apns'] }).notNull(),
  endpoint: text('endpoint').notNull(),
  p256dh: text('p256dh'),
  authKey: text('auth_key'),
  token: text('token'),
  userAgent: text('user_agent'),
  lastUsedAt: text('last_used_at'),
  failedCount: integer('failed_count').notNull(),
  revokedAt: text('revoked_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_push_subscriptions_user_id_endpoint'.slice(0, 63)).on(t.userId, t.endpoint),
]);

/** One-time codes for login, verification, password reset, invites. */
export const otpCodes = sqliteTable('otp_codes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userId: text('user_id'),
  target: text('target').notNull(),
  channel: text('channel', { enum: ['sms', 'email', 'whatsapp'] }).notNull(),
  purpose: text('purpose', { enum: ['login', 'verify', 'reset', 'invite', 'consent'] }).notNull(),
  codeHash: text('code_hash').notNull(),
  attempts: integer('attempts').notNull(),
  expiresAt: text('expires_at').notNull(),
  consumedAt: text('consumed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Stored file metadata. Bytes live on local disk (shared hosting) or S3 when configured. */
export const files = sqliteTable('files', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  uploadedBy: text('uploaded_by'),
  disk: text('disk').notNull(),
  path: text('path').notNull(),
  fileName: text('file_name').notNull(),
  mimeType: text('mime_type').notNull(),
  sizeBytes: integer('size_bytes').notNull(),
  checksum: text('checksum'),
  visibility: text('visibility', { enum: ['private', 'school', 'public'] }).notNull(),
  entityType: text('entity_type'),
  entityId: text('entity_id'),
  purpose: text('purpose'),
  width: integer('width'),
  height: integer('height'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Typed key/value policies per school (attendance.auto_absent_at, fees.reminder_stages…). */
export const settings = sqliteTable('settings', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  keyName: text('key_name').notNull(),
  value: text('value', { mode: 'json' }),
  updatedBy: text('updated_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_settings_school_id_key_name'.slice(0, 63)).on(t.schoolId, t.keyName),
]);

/** School-defined extra fields on any entity (student, staff, application…). */
export const customFields = sqliteTable('custom_fields', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  entityType: text('entity_type').notNull(),
  fieldKey: text('field_key').notNull(),
  label: text('label').notNull(),
  labelBn: text('label_bn'),
  fieldType: text('field_type', { enum: ['text', 'number', 'date', 'select', 'multiselect', 'bool', 'file', 'phone'] }).notNull(),
  options: text('options', { mode: 'json' }),
  isRequired: integer('is_required', { mode: 'boolean' }).notNull(),
  sortOrder: integer('sort_order').notNull(),
  showInList: integer('show_in_list', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_custom_fields_school_id_entity_type_field_key'.slice(0, 63)).on(t.schoolId, t.entityType, t.fieldKey),
]);

/** Values for custom fields. */
export const customFieldValues = sqliteTable('custom_field_values', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  fieldId: text('field_id').notNull(),
  entityId: text('entity_id').notNull(),
  value: text('value', { mode: 'json' }),
}, (t) => [
  uniqueIndex('uq_custom_field_values_field_id_entity_id'.slice(0, 63)).on(t.fieldId, t.entityId),
]);

/** Append-only change log written by middleware for every mutation. */
export const auditLogs = sqliteTable('audit_logs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  actorUserId: text('actor_user_id'),
  actorType: text('actor_type', { enum: ['user', 'system', 'automation', 'api'] }).notNull(),
  action: text('action').notNull(),
  entityType: text('entity_type').notNull(),
  entityId: text('entity_id'),
  beforeData: text('before_data', { mode: 'json' }),
  afterData: text('after_data', { mode: 'json' }),
  ip: text('ip'),
  userAgent: text('user_agent'),
  requestId: text('request_id'),
  createdAt: text('created_at').notNull(),
}, (t) => [
  index('ix_audit_logs_school_id_entity_type_entity_id'.slice(0, 63)).on(t.schoolId, t.entityType, t.entityId),
  index('ix_audit_logs_school_id_created_at'.slice(0, 63)).on(t.schoolId, t.createdAt),
]);

/** Per-school counters for admission no, invoice no, receipt no… */
export const numberSequences = sqliteTable('number_sequences', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  keyName: text('key_name').notNull(),
  prefix: text('prefix').notNull(),
  nextValue: integer('next_value').notNull(),
  padding: integer('padding').notNull(),
  resetYearly: integer('reset_yearly', { mode: 'boolean' }).notNull(),
  yearTag: text('year_tag'),
}, (t) => [
  uniqueIndex('uq_number_sequences_school_id_key_name'.slice(0, 63)).on(t.schoolId, t.keyName),
]);

/** Override UI strings per school and language (bn/en/ar). */
export const translations = sqliteTable('translations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  locale: text('locale').notNull(),
  keyName: text('key_name').notNull(),
  value: text('value').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_translations_school_id_locale_key_name'.slice(0, 63)).on(t.schoolId, t.locale, t.keyName),
]);

/** Platform-level flags with per-plan / per-school targeting. */
export const featureFlags = sqliteTable('feature_flags', {
  id: text('id').primaryKey(),
  keyName: text('key_name').notNull(),
  description: text('description'),
  defaultOn: integer('default_on', { mode: 'boolean' }).notNull(),
  targeting: text('targeting', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Zero-touch installer progress (cPanel): DB detected, migrations, seeds, cron mode, health. */
export const installerState = sqliteTable('installer_state', {
  id: text('id').primaryKey(),
  step: text('step').notNull(),
  status: text('status', { enum: ['pending', 'done', 'failed', 'skipped'] }).notNull(),
  detail: text('detail', { mode: 'json' }),
  finishedAt: text('finished_at'),
});

/** Self-check results (cron heartbeat, queue lag, disk, mail, SMS balance). */
export const systemHealth = sqliteTable('system_health', {
  id: text('id').primaryKey(),
  checkKey: text('check_key').notNull(),
  status: text('status', { enum: ['ok', 'warn', 'fail'] }).notNull(),
  detail: text('detail', { mode: 'json' }),
  checkedAt: text('checked_at').notNull(),
});

// ============ Automation platform · events, rules, jobs, approvals, workflows ============
/** Domain events written in the same transaction as the change. Relay publishes to the job queue. */
export const outboxEvents = sqliteTable('outbox_events', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  eventUid: text('event_uid').notNull(),
  eventType: text('event_type').notNull(),
  aggregateType: text('aggregate_type').notNull(),
  aggregateId: text('aggregate_id').notNull(),
  payload: text('payload', { mode: 'json' }),
  actorUserId: text('actor_user_id'),
  occurredAt: text('occurred_at').notNull(),
  publishedAt: text('published_at'),
  version: integer('version').notNull(),
}, (t) => [
  index('ix_outbox_events_aggregate_type_aggregate_id'.slice(0, 63)).on(t.aggregateType, t.aggregateId),
]);

/** Idempotency: consumer × event processed once; failures count their attempts on one row. */
export const eventConsumptions = sqliteTable('event_consumptions', {
  id: text('id').primaryKey(),
  consumer: text('consumer').notNull(),
  eventUid: text('event_uid').notNull(),
  attempts: integer('attempts').notNull(),
  processedAt: text('processed_at').notNull(),
}, (t) => [
  uniqueIndex('uq_event_consumptions_consumer_event_uid'.slice(0, 63)).on(t.consumer, t.eventUid),
]);

/** WHEN trigger IF conditions (JSONLogic) THEN actions. Seeded system defaults are editable, not deletable. */
export const automationRules = sqliteTable('automation_rules', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  code: text('code').notNull(),
  name: text('name').notNull(),
  module: text('module').notNull(),
  description: text('description'),
  triggerKind: text('trigger_kind', { enum: ['event', 'schedule', 'threshold', 'manual'] }).notNull(),
  eventType: text('event_type'),
  cronExpr: text('cron_expr'),
  conditions: text('conditions', { mode: 'json' }),
  actions: text('actions', { mode: 'json' }),
  isSystem: integer('is_system', { mode: 'boolean' }).notNull(),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  previewUntil: text('preview_until'),
  priority: integer('priority').notNull(),
  cooldownMinutes: integer('cooldown_minutes'),
  runCount: integer('run_count').notNull(),
  lastRunAt: text('last_run_at'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_automation_rules_school_id_code'.slice(0, 63)).on(t.schoolId, t.code),
]);

/** One row per rule execution with result/error. */
export const automationRuns = sqliteTable('automation_runs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  ruleId: text('rule_id').notNull(),
  triggerEventUid: text('trigger_event_uid'),
  aggregateType: text('aggregate_type'),
  aggregateId: text('aggregate_id'),
  startedAt: text('started_at').notNull(),
  finishedAt: text('finished_at'),
  status: text('status', { enum: ['running', 'success', 'failed', 'skipped', 'preview'] }).notNull(),
  actionsResult: text('actions_result', { mode: 'json' }),
  error: text('error'),
}, (t) => [
  index('ix_automation_runs_rule_id_started_at'.slice(0, 63)).on(t.ruleId, t.startedAt),
]);

/** Cron catalogue. On cPanel runs via one cron line or the request-driven heartbeat (web cron) with a lock. */
export const scheduledJobs = sqliteTable('scheduled_jobs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  jobKey: text('job_key').notNull(),
  cronExpr: text('cron_expr').notNull(),
  timezone: text('timezone').notNull(),
  payload: text('payload', { mode: 'json' }),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  nextRunAt: text('next_run_at'),
  lastRunAt: text('last_run_at'),
  lastStatus: text('last_status', { enum: ['success', 'failed', 'running'] }),
  lastDurationMs: integer('last_duration_ms'),
  lockedUntil: text('locked_until'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_scheduled_jobs_school_id_job_key'.slice(0, 63)).on(t.schoolId, t.jobKey),
]);

/** Durable job queue (database driver on shared hosting). Long jobs run in chunks under 30 s each and resume from cursor. */
export const backgroundJobs = sqliteTable('background_jobs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  queue: text('queue').notNull(),
  jobName: text('job_name').notNull(),
  payload: text('payload', { mode: 'json' }),
  status: text('status', { enum: ['pending', 'running', 'success', 'failed', 'cancelled', 'paused'] }).notNull(),
  attempts: integer('attempts').notNull(),
  maxAttempts: integer('max_attempts').notNull(),
  scheduledFor: text('scheduled_for').notNull(),
  startedAt: text('started_at'),
  finishedAt: text('finished_at'),
  progressPct: numeric('progress_pct', { mode: 'number' }).notNull(),
  cursor: text('cursor', { mode: 'json' }),
  totalItems: integer('total_items'),
  doneItems: integer('done_items').notNull(),
  lockedUntil: text('locked_until'),
  result: text('result', { mode: 'json' }),
  error: text('error'),
  triggeredBy: text('triggered_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_background_jobs_school_id_status_scheduled_for'.slice(0, 63)).on(t.schoolId, t.status, t.scheduledFor),
]);

/** Multi-step approvals: which entity, which conditions pick this workflow, ordered approver steps. */
export const approvalWorkflows = sqliteTable('approval_workflows', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  entityType: text('entity_type').notNull(),
  name: text('name').notNull(),
  conditions: text('conditions', { mode: 'json' }),
  steps: text('steps', { mode: 'json' }),
  autoApproveAfterHours: integer('auto_approve_after_hours'),
  escalateAfterHours: integer('escalate_after_hours'),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_approval_workflows_school_id_entity_type_name'.slice(0, 63)).on(t.schoolId, t.entityType, t.name),
]);

/** A pending/decided approval for one entity. */
export const approvalRequests = sqliteTable('approval_requests', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  workflowId: text('workflow_id').notNull(),
  entityType: text('entity_type').notNull(),
  entityId: text('entity_id').notNull(),
  requestedBy: text('requested_by'),
  currentStep: integer('current_step').notNull(),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'cancelled', 'escalated'] }).notNull(),
  summary: text('summary', { mode: 'json' }),
  dueAt: text('due_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_approval_requests_entity_type_entity_id'.slice(0, 63)).on(t.entityType, t.entityId),
  index('ix_approval_requests_school_id_status'.slice(0, 63)).on(t.schoolId, t.status),
]);

/** Decision per step. */
export const approvalActions = sqliteTable('approval_actions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  requestId: text('request_id').notNull(),
  step: integer('step').notNull(),
  actorId: text('actor_id'),
  decision: text('decision', { enum: ['approved', 'rejected', 'delegated', 'auto_approved', 'escalated'] }).notNull(),
  comment: text('comment'),
  actedAt: text('acted_at').notNull(),
});

/** To-dos created by rules or people (call guardian, verify document, review lag). */
export const tasks = sqliteTable('tasks', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  description: text('description'),
  taskType: text('task_type'),
  assignedTo: text('assigned_to'),
  assignedRole: text('assigned_role'),
  entityType: text('entity_type'),
  entityId: text('entity_id'),
  dueAt: text('due_at'),
  priority: text('priority', { enum: ['low', 'normal', 'high', 'urgent'] }).notNull(),
  status: text('status', { enum: ['open', 'in_progress', 'done', 'cancelled'] }).notNull(),
  createdBy: text('created_by').notNull(),
  completedAt: text('completed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_tasks_assigned_to_status'.slice(0, 63)).on(t.assignedTo, t.status),
]);

/** No-code form builder (surveys, admission forms, feedback, custom registers). */
export const formDefinitions = sqliteTable('form_definitions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  slug: text('slug').notNull(),
  purpose: text('purpose', { enum: ['survey', 'admission', 'feedback', 'register', 'poll', 'consent', 'other'] }).notNull(),
  schemaJson: text('schema_json', { mode: 'json' }),
  audience: text('audience', { mode: 'json' }),
  isPublic: integer('is_public', { mode: 'boolean' }).notNull(),
  opensAt: text('opens_at'),
  closesAt: text('closes_at'),
  status: text('status', { enum: ['draft', 'open', 'closed'] }).notNull(),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_form_definitions_school_id_slug'.slice(0, 63)).on(t.schoolId, t.slug),
]);

/** Answers to a form. */
export const formSubmissions = sqliteTable('form_submissions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  formId: text('form_id').notNull(),
  submittedBy: text('submitted_by'),
  entityType: text('entity_type'),
  entityId: text('entity_id'),
  answers: text('answers', { mode: 'json' }),
  ip: text('ip'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** No-code workflow builder: states and transitions for custom processes. */
export const workflowDefinitions = sqliteTable('workflow_definitions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  entityType: text('entity_type').notNull(),
  states: text('states', { mode: 'json' }),
  transitions: text('transitions', { mode: 'json' }),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Outbound webhooks to school ERPs / Zapier. */
export const webhooks = sqliteTable('webhooks', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  url: text('url').notNull(),
  secret: text('secret').notNull(),
  eventTypes: text('event_types', { mode: 'json' }),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  failureCount: integer('failure_count').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Delivery attempts. */
export const webhookDeliveries = sqliteTable('webhook_deliveries', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  webhookId: text('webhook_id').notNull(),
  eventUid: text('event_uid').notNull(),
  attempt: integer('attempt').notNull(),
  responseCode: integer('response_code'),
  responseBody: text('response_body'),
  deliveredAt: text('delivered_at'),
  nextRetryAt: text('next_retry_at'),
  createdAt: text('created_at').notNull(),
});

/** Connected providers (Zoom, Google, ZKTeco cloud, GPS vendor, gateways) with encrypted config. */
export const integrations = sqliteTable('integrations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  provider: text('provider').notNull(),
  category: text('category', { enum: ['meeting', 'sso', 'device', 'gps', 'payment', 'sms', 'email', 'storage', 'ai', 'accounting', 'other'] }).notNull(),
  config: text('config', { mode: 'json' }),
  status: text('status', { enum: ['connected', 'failed', 'disconnected'] }).notNull(),
  lastSyncAt: text('last_sync_at'),
  lastError: text('last_error'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_integrations_school_id_provider'.slice(0, 63)).on(t.schoolId, t.provider),
]);

/** Keys for third-party apps and integrations. */
export const apiKeys = sqliteTable('api_keys', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  keyPrefix: text('key_prefix').notNull(),
  keyHash: text('key_hash').notNull(),
  scopes: text('scopes', { mode: 'json' }),
  rateLimitPerMin: integer('rate_limit_per_min').notNull(),
  expiresAt: text('expires_at'),
  lastUsedAt: text('last_used_at'),
  createdBy: text('created_by'),
  revokedAt: text('revoked_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Report builder: saved queries/pivots with schedule and recipients. */
export const reportDefinitions = sqliteTable('report_definitions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  module: text('module').notNull(),
  definition: text('definition', { mode: 'json' }),
  outputFormat: text('output_format', { enum: ['pdf', 'xlsx', 'csv', 'html'] }).notNull(),
  cronExpr: text('cron_expr'),
  recipients: text('recipients', { mode: 'json' }),
  isSystem: integer('is_system', { mode: 'boolean' }).notNull(),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Generated report files. */
export const reportSnapshots = sqliteTable('report_snapshots', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  definitionId: text('definition_id').notNull(),
  params: text('params', { mode: 'json' }),
  fileId: text('file_id'),
  rowCount: integer('row_count'),
  generatedAt: text('generated_at').notNull(),
});

/** Daily KPI snapshot per school for dashboards and anomaly alerts. */
export const kpiDaily = sqliteTable('kpi_daily', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  day: text('day').notNull(),
  studentsActive: integer('students_active'),
  attendancePct: numeric('attendance_pct', { mode: 'number' }),
  staffAttendancePct: numeric('staff_attendance_pct', { mode: 'number' }),
  feesCollected: numeric('fees_collected', { mode: 'number' }),
  feesOutstanding: numeric('fees_outstanding', { mode: 'number' }),
  newEnquiries: integer('new_enquiries'),
  newAdmissions: integer('new_admissions'),
  smsSent: integer('sms_sent'),
  extra: text('extra', { mode: 'json' }),
}, (t) => [
  uniqueIndex('uq_kpi_daily_school_id_day'.slice(0, 63)).on(t.schoolId, t.day),
]);

/** Bulk Excel/CSV imports with mapping, validation and error file. */
export const importJobs = sqliteTable('import_jobs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  entityType: text('entity_type').notNull(),
  fileId: text('file_id').notNull(),
  mapping: text('mapping', { mode: 'json' }),
  totalRows: integer('total_rows'),
  successRows: integer('success_rows'),
  errorRows: integer('error_rows'),
  errorsFileId: text('errors_file_id'),
  status: text('status', { enum: ['pending', 'validating', 'running', 'success', 'failed'] }).notNull(),
  createdBy: text('created_by'),
  finishedAt: text('finished_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Automated backups (DB dump + files) to local, Google Drive, Dropbox or S3. */
export const backups = sqliteTable('backups', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  kind: text('kind', { enum: ['database', 'files', 'full'] }).notNull(),
  target: text('target', { enum: ['local', 'gdrive', 'dropbox', 's3'] }).notNull(),
  filePath: text('file_path'),
  sizeBytes: integer('size_bytes'),
  status: text('status', { enum: ['running', 'success', 'failed'] }).notNull(),
  startedAt: text('started_at').notNull(),
  finishedAt: text('finished_at'),
  error: text('error'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Hourly rollup of notification volume and cost (for the health page). */
export const notificationsQueueStats = sqliteTable('notifications_queue_stats', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  hour: text('hour').notNull(),
  channel: text('channel').notNull(),
  sent: integer('sent').notNull(),
  delivered: integer('delivered').notNull(),
  failed: integer('failed').notNull(),
  cost: numeric('cost', { mode: 'number' }).notNull(),
}, (t) => [
  uniqueIndex('uq_notifications_queue_stats_school_id_hour_channel'.slice(0, 63)).on(t.schoolId, t.hour, t.channel),
]);

// ============ SaaS billing & partners ============
/** Pricing plans with limits and enabled modules. */
export const saasPlans = sqliteTable('saas_plans', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  priceMonthly: numeric('price_monthly', { mode: 'number' }).notNull(),
  priceYearly: numeric('price_yearly', { mode: 'number' }).notNull(),
  currency: text('currency').notNull(),
  studentLimit: integer('student_limit'),
  smsIncluded: integer('sms_included').notNull(),
  storageGb: integer('storage_gb').notNull(),
  modules: text('modules', { mode: 'json' }),
  isPublic: integer('is_public', { mode: 'boolean' }).notNull(),
  sortOrder: integer('sort_order').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** A school’s subscription to a plan. */
export const saasSubscriptions = sqliteTable('saas_subscriptions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  planId: text('plan_id').notNull(),
  billingCycle: text('billing_cycle', { enum: ['monthly', 'yearly'] }).notNull(),
  startsAt: text('starts_at').notNull(),
  endsAt: text('ends_at'),
  status: text('status', { enum: ['trial', 'active', 'past_due', 'cancelled', 'expired'] }).notNull(),
  autoRenew: integer('auto_renew', { mode: 'boolean' }).notNull(),
  price: numeric('price', { mode: 'number' }).notNull(),
  discountPct: numeric('discount_pct', { mode: 'number' }).notNull(),
  resellerId: text('reseller_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Invoices from the platform to the school. */
export const saasInvoices = sqliteTable('saas_invoices', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  subscriptionId: text('subscription_id'),
  invoiceNo: text('invoice_no').notNull(),
  periodStart: text('period_start').notNull(),
  periodEnd: text('period_end').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  tax: numeric('tax', { mode: 'number' }).notNull(),
  total: numeric('total', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['draft', 'issued', 'paid', 'overdue', 'void'] }).notNull(),
  dueDate: text('due_date').notNull(),
  paidAt: text('paid_at'),
  paymentRef: text('payment_ref'),
  pdfFileId: text('pdf_file_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Metered usage per month (students, SMS, storage, API calls). */
export const saasUsage = sqliteTable('saas_usage', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  month: text('month').notNull(),
  metric: text('metric').notNull(),
  quantity: integer('quantity').notNull(),
}, (t) => [
  uniqueIndex('uq_saas_usage_school_id_month_metric'.slice(0, 63)).on(t.schoolId, t.month, t.metric),
]);

/** Resellers / referral partners earning commission. */
export const saasPartners = sqliteTable('saas_partners', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  contact: text('contact', { mode: 'json' }),
  commissionPct: numeric('commission_pct', { mode: 'number' }).notNull(),
  referralCode: text('referral_code').notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Commission payouts. */
export const saasPartnerPayouts = sqliteTable('saas_partner_payouts', {
  id: text('id').primaryKey(),
  partnerId: text('partner_id').notNull(),
  period: text('period').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['pending', 'paid'] }).notNull(),
  paidAt: text('paid_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Schools’ support requests to the platform team. */
export const saasSupportTickets = sqliteTable('saas_support_tickets', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  subject: text('subject').notNull(),
  body: text('body'),
  priority: text('priority', { enum: ['low', 'normal', 'high', 'urgent'] }).notNull(),
  status: text('status', { enum: ['open', 'answered', 'closed'] }).notNull(),
  openedBy: text('opened_by'),
  assignedTo: text('assigned_to'),
  closedAt: text('closed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Website & CMS ============
/** Public pages built from blocks. */
export const cmsPages = sqliteTable('cms_pages', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  slug: text('slug').notNull(),
  locale: text('locale').notNull(),
  blocks: text('blocks', { mode: 'json' }),
  seo: text('seo', { mode: 'json' }),
  isHome: integer('is_home', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['draft', 'published'] }).notNull(),
  publishedAt: text('published_at'),
  authorId: text('author_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
  deletedAt: text('deleted_at'),
}, (t) => [
  uniqueIndex('uq_cms_pages_school_id_slug_locale'.slice(0, 63)).on(t.schoolId, t.slug, t.locale),
]);

/** News, blog, achievements. */
export const cmsPosts = sqliteTable('cms_posts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  slug: text('slug').notNull(),
  category: text('category'),
  excerpt: text('excerpt'),
  body: text('body'),
  coverFileId: text('cover_file_id'),
  status: text('status', { enum: ['draft', 'published'] }).notNull(),
  publishedAt: text('published_at'),
  authorId: text('author_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
  deletedAt: text('deleted_at'),
}, (t) => [
  uniqueIndex('uq_cms_posts_school_id_slug'.slice(0, 63)).on(t.schoolId, t.slug),
]);

/** Navigation menus. */
export const cmsMenus = sqliteTable('cms_menus', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  items: text('items', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_cms_menus_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Photo/video albums. */
export const cmsGalleries = sqliteTable('cms_galleries', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  description: text('description'),
  coverFileId: text('cover_file_id'),
  isPublic: integer('is_public', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Album items. */
export const cmsGalleryItems = sqliteTable('cms_gallery_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  galleryId: text('gallery_id').notNull(),
  fileId: text('file_id'),
  videoUrl: text('video_url'),
  caption: text('caption'),
  sortOrder: integer('sort_order').notNull(),
});

/** Website themes. */
export const cmsThemes = sqliteTable('cms_themes', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  previewFileId: text('preview_file_id'),
  tokens: text('tokens', { mode: 'json' }),
  isDefault: integer('is_default', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Custom domains and SSL status for a school site. */
export const cmsDomains = sqliteTable('cms_domains', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  domain: text('domain').notNull(),
  isPrimary: integer('is_primary', { mode: 'boolean' }).notNull(),
  sslStatus: text('ssl_status', { enum: ['pending', 'active', 'failed'] }).notNull(),
  verifiedAt: text('verified_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Messages from the website contact form. */
export const cmsContactMessages = sqliteTable('cms_contact_messages', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  phone: text('phone'),
  email: text('email'),
  message: text('message').notNull(),
  status: text('status', { enum: ['new', 'read', 'replied'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Academic structure ============
/** Session. Only one is current. */
export const academicYears = sqliteTable('academic_years', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  startDate: text('start_date').notNull(),
  endDate: text('end_date').notNull(),
  isCurrent: integer('is_current', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['planned', 'active', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_academic_years_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Terms (school) or semesters (college) inside a year. */
export const terms = sqliteTable('terms', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  name: text('name').notNull(),
  sequence: integer('sequence').notNull(),
  startDate: text('start_date').notNull(),
  endDate: text('end_date').notNull(),
  kind: text('kind', { enum: ['term', 'semester', 'trimester'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_terms_academic_year_id_sequence'.slice(0, 63)).on(t.academicYearId, t.sequence),
]);

/** Morning / Day shifts with times. */
export const shifts = sqliteTable('shifts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  startTime: text('start_time').notNull(),
  endTime: text('end_time').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_shifts_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** College/university programmes (HSC Science, BBA…) or coaching courses. Schools may ignore. */
export const programs = sqliteTable('programs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  code: text('code').notNull(),
  level: text('level', { enum: ['secondary', 'higher_secondary', 'bachelor', 'master', 'diploma', 'coaching'] }).notNull(),
  durationTerms: integer('duration_terms'),
  totalCredits: numeric('total_credits', { mode: 'number' }),
  departmentId: text('department_id'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_programs_school_id_code'.slice(0, 63)).on(t.schoolId, t.code),
]);

/** Grade level: Play, Nursery, KG, 1–12 (or programme year). */
export const classes = sqliteTable('classes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  nameBn: text('name_bn'),
  numericLevel: integer('numeric_level').notNull(),
  stream: text('stream'),
  programId: text('program_id'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_classes_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Physical rooms; used by timetable, exams, events, bookings. */
export const rooms = sqliteTable('rooms', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campusId: text('campus_id').notNull(),
  name: text('name').notNull(),
  building: text('building'),
  floor: text('floor'),
  capacity: integer('capacity'),
  roomType: text('room_type', { enum: ['classroom', 'lab', 'hall', 'library', 'office', 'playground', 'other'] }).notNull(),
  amenities: text('amenities', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_rooms_campus_id_name'.slice(0, 63)).on(t.campusId, t.name),
]);

/** Section (school) or batch (coaching) of a class in a year. */
export const sections = sqliteTable('sections', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  classId: text('class_id').notNull(),
  campusId: text('campus_id'),
  shiftId: text('shift_id'),
  name: text('name').notNull(),
  capacity: integer('capacity').notNull(),
  roomId: text('room_id'),
  classTeacherId: text('class_teacher_id'),
  genderPolicy: text('gender_policy', { enum: ['mixed', 'boys', 'girls'] }).notNull(),
  medium: text('medium', { enum: ['bangla', 'english', 'arabic'] }).notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_sections_academic_year_id_class_id_name'.slice(0, 63)).on(t.academicYearId, t.classId, t.name),
]);

/** Subject catalogue with board codes. */
export const subjects = sqliteTable('subjects', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  nameBn: text('name_bn'),
  code: text('code').notNull(),
  subjectType: text('subject_type', { enum: ['theory', 'practical', 'both', 'activity'] }).notNull(),
  isOptional: integer('is_optional', { mode: 'boolean' }).notNull(),
  departmentId: text('department_id'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_subjects_school_id_code'.slice(0, 63)).on(t.schoolId, t.code),
]);

/** Which subjects a class studies in a year and how they are marked (full/pass marks, theory/practical/CA split, credit). */
export const classSubjects = sqliteTable('class_subjects', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  classId: text('class_id').notNull(),
  subjectId: text('subject_id').notNull(),
  isCompulsory: integer('is_compulsory', { mode: 'boolean' }).notNull(),
  fullMarks: numeric('full_marks', { mode: 'number' }).notNull(),
  passMarks: numeric('pass_marks', { mode: 'number' }).notNull(),
  theoryMarks: numeric('theory_marks', { mode: 'number' }),
  practicalMarks: numeric('practical_marks', { mode: 'number' }),
  caMarks: numeric('ca_marks', { mode: 'number' }),
  credit: numeric('credit', { mode: 'number' }).notNull(),
  weeklyPeriods: integer('weekly_periods').notNull(),
  sortOrder: integer('sort_order').notNull(),
  assessmentMode: text('assessment_mode', { enum: ['marks', 'competency', 'both'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_class_subjects_academic_year_id_class_id_subject_id'.slice(0, 63)).on(t.academicYearId, t.classId, t.subjectId),
]);

/** Optional/4th subject choice per student. */
export const studentSubjectChoices = sqliteTable('student_subject_choices', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  isFourth: integer('is_fourth', { mode: 'boolean' }).notNull(),
}, (t) => [
  uniqueIndex('uq_student_subject_choices_student_id_class_subject_id'.slice(0, 63)).on(t.studentId, t.classSubjectId),
]);

/** Semester/credit mode: what a student registered for in one term, with the credit it carries and the grade it earned. A school never touches this; a college cannot work without it. */
export const courseRegistrations = sqliteTable('course_registrations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  termId: text('term_id').notNull(),
  studentId: text('student_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  programId: text('program_id'),
  credit: numeric('credit', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['registered', 'dropped', 'completed', 'failed'] }).notNull(),
  percent: numeric('percent', { mode: 'number' }),
  grade: text('grade'),
  gradePoint: numeric('grade_point', { mode: 'number' }),
  registeredOn: text('registered_on').notNull(),
  droppedOn: text('dropped_on'),
  completedOn: text('completed_on'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_course_registrations_student_id_term_id_class_subject_id'.slice(0, 63)).on(t.studentId, t.termId, t.classSubjectId),
]);

/** Period grid per shift. */
export const periods = sqliteTable('periods', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  shiftId: text('shift_id'),
  name: text('name').notNull(),
  sequence: integer('sequence').notNull(),
  startTime: text('start_time').notNull(),
  endTime: text('end_time').notNull(),
  isBreak: integer('is_break', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_periods_school_id_shift_id_sequence'.slice(0, 63)).on(t.schoolId, t.shiftId, t.sequence),
]);

/** Houses for sports/points. */
export const houses = sqliteTable('houses', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  color: text('color'),
  points: integer('points').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_houses_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Holidays, vacations, exam windows, events. Holidays drive attendance and reminder automation. */
export const calendarEvents = sqliteTable('calendar_events', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id'),
  title: text('title').notNull(),
  eventType: text('event_type', { enum: ['holiday', 'vacation', 'exam', 'event', 'ptm', 'deadline', 'meeting'] }).notNull(),
  startDate: text('start_date').notNull(),
  endDate: text('end_date').notNull(),
  isHoliday: integer('is_holiday', { mode: 'boolean' }).notNull(),
  appliesTo: text('applies_to', { mode: 'json' }),
  description: text('description'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_calendar_events_school_id_start_date_end_date'.slice(0, 63)).on(t.schoolId, t.startDate, t.endDate),
]);

/** Weekend days (BD: Fri, Sat). */
export const weeklyOffs = sqliteTable('weekly_offs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  dayOfWeek: integer('day_of_week').notNull(),
}, (t) => [
  uniqueIndex('uq_weekly_offs_school_id_day_of_week'.slice(0, 63)).on(t.schoolId, t.dayOfWeek),
]);

/** Madrasa: Quran memorisation tracking per student (para/surah/ayah, revision). */
export const hifzProgress = sqliteTable('hifz_progress', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  recordedOn: text('recorded_on').notNull(),
  para: integer('para'),
  surah: integer('surah'),
  ayahFrom: integer('ayah_from'),
  ayahTo: integer('ayah_to'),
  kind: text('kind', { enum: ['new', 'revision', 'test'] }).notNull(),
  quality: text('quality', { enum: ['excellent', 'good', 'fair', 'weak'] }),
  teacherId: text('teacher_id'),
  remarks: text('remarks'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ People · students, guardians, staff ============
/** Academic and admin departments. */
export const departments = sqliteTable('departments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  headStaffId: text('head_staff_id'),
  kind: text('kind', { enum: ['academic', 'admin', 'support'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_departments_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Job titles with hierarchy level. */
export const designations = sqliteTable('designations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  level: integer('level').notNull(),
  category: text('category', { enum: ['teaching', 'non_teaching', 'admin', 'support'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_designations_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Employee master. */
export const staff = sqliteTable('staff', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userId: text('user_id'),
  employeeNo: text('employee_no').notNull(),
  firstName: text('first_name').notNull(),
  lastName: text('last_name'),
  nameBn: text('name_bn'),
  gender: text('gender', { enum: ['male', 'female', 'other'] }),
  dateOfBirth: text('date_of_birth'),
  phone: text('phone'),
  email: text('email'),
  nidNo: text('nid_no'),
  photoFileId: text('photo_file_id'),
  campusId: text('campus_id'),
  departmentId: text('department_id'),
  designationId: text('designation_id'),
  reportsToId: text('reports_to_id'),
  staffCategory: text('staff_category', { enum: ['teaching', 'non_teaching', 'admin', 'support'] }).notNull(),
  employmentType: text('employment_type', { enum: ['permanent', 'contract', 'part_time', 'intern', 'volunteer', 'mpo'] }).notNull(),
  mpoIndexNo: text('mpo_index_no'),
  joinDate: text('join_date').notNull(),
  probationEnd: text('probation_end'),
  leaveDate: text('leave_date'),
  status: text('status', { enum: ['active', 'probation', 'on_leave', 'resigned', 'terminated', 'retired'] }).notNull(),
  address: text('address', { mode: 'json' }),
  emergencyContact: text('emergency_contact', { mode: 'json' }),
  bankDetails: text('bank_details', { mode: 'json' }),
  biometricId: text('biometric_id'),
  rfidTag: text('rfid_tag'),
  meta: text('meta', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
  deletedAt: text('deleted_at'),
}, (t) => [
  uniqueIndex('uq_staff_school_id_employee_no'.slice(0, 63)).on(t.schoolId, t.employeeNo),
]);

/** Degrees. */
export const staffQualifications = sqliteTable('staff_qualifications', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  degree: text('degree').notNull(),
  institution: text('institution'),
  passingYear: integer('passing_year'),
  result: text('result'),
  fileId: text('file_id'),
});

/** Subjects a teacher can teach (timetable + substitution engine). */
export const staffSubjects = sqliteTable('staff_subjects', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  subjectId: text('subject_id').notNull(),
  preference: integer('preference').notNull(),
}, (t) => [
  uniqueIndex('uq_staff_subjects_staff_id_subject_id'.slice(0, 63)).on(t.staffId, t.subjectId),
]);

/** NID, certificates, contracts with expiry reminders. */
export const staffDocuments = sqliteTable('staff_documents', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  docType: text('doc_type').notNull(),
  fileId: text('file_id').notNull(),
  expiresAt: text('expires_at'),
  verifiedBy: text('verified_by'),
  verifiedAt: text('verified_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Student master with denormalised current pointers. */
export const students = sqliteTable('students', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userId: text('user_id'),
  admissionNo: text('admission_no').notNull(),
  firstName: text('first_name').notNull(),
  lastName: text('last_name'),
  nameBn: text('name_bn'),
  gender: text('gender', { enum: ['male', 'female', 'other'] }).notNull(),
  dateOfBirth: text('date_of_birth').notNull(),
  bloodGroup: text('blood_group'),
  religion: text('religion'),
  nationality: text('nationality').notNull(),
  birthCertificateNo: text('birth_certificate_no'),
  nidNo: text('nid_no'),
  photoFileId: text('photo_file_id'),
  admissionDate: text('admission_date').notNull(),
  admissionClassId: text('admission_class_id'),
  currentAcademicYearId: text('current_academic_year_id'),
  currentClassId: text('current_class_id'),
  currentSectionId: text('current_section_id'),
  currentRollNo: text('current_roll_no'),
  houseId: text('house_id'),
  status: text('status', { enum: ['applicant', 'active', 'suspended', 'graduated', 'transferred', 'dropped', 'alumni'] }).notNull(),
  statusChangedAt: text('status_changed_at'),
  statusReason: text('status_reason'),
  presentAddress: text('present_address', { mode: 'json' }),
  permanentAddress: text('permanent_address', { mode: 'json' }),
  previousSchool: text('previous_school', { mode: 'json' }),
  medicalSummary: text('medical_summary', { mode: 'json' }),
  specialNeeds: text('special_needs', { mode: 'json' }),
  biometricId: text('biometric_id'),
  rfidTag: text('rfid_tag'),
  walletId: text('wallet_id'),
  meta: text('meta', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
  deletedAt: text('deleted_at'),
}, (t) => [
  uniqueIndex('uq_students_school_id_admission_no'.slice(0, 63)).on(t.schoolId, t.admissionNo),
  index('ix_students_school_id_current_section_id_status'.slice(0, 63)).on(t.schoolId, t.currentSectionId, t.status),
]);

/** One row per student per academic year: class, section, roll. Full history. */
export const studentEnrollments = sqliteTable('student_enrollments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  classId: text('class_id').notNull(),
  sectionId: text('section_id'),
  programId: text('program_id'),
  rollNo: text('roll_no'),
  enrolledOn: text('enrolled_on').notNull(),
  leftOn: text('left_on'),
  status: text('status', { enum: ['active', 'promoted', 'retained', 'transferred', 'left', 'graduated'] }).notNull(),
  promotedFromId: text('promoted_from_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_student_enrollments_student_id_academic_year_id'.slice(0, 63)).on(t.studentId, t.academicYearId),
  index('ix_student_enrollments_section_id_roll_no'.slice(0, 63)).on(t.sectionId, t.rollNo),
]);

/** Guardian keyed by phone; same phone = same guardian = siblings. */
export const guardians = sqliteTable('guardians', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userId: text('user_id'),
  fullName: text('full_name').notNull(),
  phone: text('phone').notNull(),
  altPhone: text('alt_phone'),
  email: text('email'),
  occupation: text('occupation'),
  nidNo: text('nid_no'),
  monthlyIncome: numeric('monthly_income', { mode: 'number' }),
  address: text('address', { mode: 'json' }),
  photoFileId: text('photo_file_id'),
  isStaff: integer('is_staff', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_guardians_school_id_phone'.slice(0, 63)).on(t.schoolId, t.phone),
]);

/** Student ↔ guardian with relation and rights. */
export const studentGuardians = sqliteTable('student_guardians', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  guardianId: text('guardian_id').notNull(),
  relation: text('relation', { enum: ['father', 'mother', 'grandparent', 'sibling', 'uncle', 'aunt', 'legal_guardian', 'other'] }).notNull(),
  isPrimary: integer('is_primary', { mode: 'boolean' }).notNull(),
  isEmergency: integer('is_emergency', { mode: 'boolean' }).notNull(),
  canPickup: integer('can_pickup', { mode: 'boolean' }).notNull(),
  receivesNotifications: integer('receives_notifications', { mode: 'boolean' }).notNull(),
  paysFees: integer('pays_fees', { mode: 'boolean' }).notNull(),
}, (t) => [
  uniqueIndex('uq_student_guardians_student_id_guardian_id'.slice(0, 63)).on(t.studentId, t.guardianId),
]);

/** KG/primary: who may collect the child; photo + ID; one-time passes. */
export const pickupAuthorisations = sqliteTable('pickup_authorisations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  personName: text('person_name').notNull(),
  relation: text('relation'),
  phone: text('phone'),
  photoFileId: text('photo_file_id'),
  idProof: text('id_proof'),
  validFrom: text('valid_from'),
  validTo: text('valid_to'),
  isOneTime: integer('is_one_time', { mode: 'boolean' }).notNull(),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Uploaded student documents with verification. */
export const studentDocuments = sqliteTable('student_documents', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  docType: text('doc_type').notNull(),
  fileId: text('file_id').notNull(),
  verifiedBy: text('verified_by'),
  verifiedAt: text('verified_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Every status change (suspended, transferred…) with reason. */
export const studentStatusHistory = sqliteTable('student_status_history', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  fromStatus: text('from_status'),
  toStatus: text('to_status').notNull(),
  reason: text('reason'),
  changedBy: text('changed_by'),
  changedAt: text('changed_at').notNull(),
});

// ============ Curriculum & timetable ============
/** Who teaches which subject in which section this year. */
export const sectionSubjectTeachers = sqliteTable('section_subject_teachers', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  sectionId: text('section_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  teacherId: text('teacher_id').notNull(),
  isPrimary: integer('is_primary', { mode: 'boolean' }).notNull(),
}, (t) => [
  uniqueIndex('uq_section_subject_teachers_section_id_class_subject_id_teacher_id'.slice(0, 63)).on(t.sectionId, t.classSubjectId, t.teacherId),
]);

/** Published versions of the timetable; auto-generator writes drafts. */
export const timetableVersions = sqliteTable('timetable_versions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  name: text('name').notNull(),
  effectiveFrom: text('effective_from').notNull(),
  effectiveTo: text('effective_to'),
  status: text('status', { enum: ['draft', 'published', 'archived'] }).notNull(),
  generatedBy: text('generated_by', { enum: ['manual', 'auto'] }).notNull(),
  constraints: text('constraints', { mode: 'json' }),
  score: numeric('score', { mode: 'number' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Section × day × period → subject, teacher, room. App enforces no teacher/room double booking. */
export const timetableSlots = sqliteTable('timetable_slots', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  versionId: text('version_id').notNull(),
  sectionId: text('section_id').notNull(),
  dayOfWeek: integer('day_of_week').notNull(),
  periodId: text('period_id').notNull(),
  classSubjectId: text('class_subject_id'),
  teacherId: text('teacher_id'),
  roomId: text('room_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_timetable_slots_version_id_section_id_day_of_week_period_id'.slice(0, 63)).on(t.versionId, t.sectionId, t.dayOfWeek, t.periodId),
  uniqueIndex('uq_timetable_slots_version_id_teacher_id_day_of_week_period_id'.slice(0, 63)).on(t.versionId, t.teacherId, t.dayOfWeek, t.periodId),
  uniqueIndex('uq_timetable_slots_version_id_room_id_day_of_week_period_id'.slice(0, 63)).on(t.versionId, t.roomId, t.dayOfWeek, t.periodId),
]);

/** Day-specific override when a teacher is absent; auto-suggested from free, qualified teachers. */
export const timetableSubstitutions = sqliteTable('timetable_substitutions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  slotId: text('slot_id').notNull(),
  onDate: text('on_date').notNull(),
  originalTeacherId: text('original_teacher_id'),
  substituteTeacherId: text('substitute_teacher_id'),
  reason: text('reason'),
  leaveApplicationId: text('leave_application_id'),
  status: text('status', { enum: ['suggested', 'pending', 'approved', 'rejected', 'cancelled'] }).notNull(),
  isAutoSuggested: integer('is_auto_suggested', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_timetable_substitutions_slot_id_on_date'.slice(0, 63)).on(t.slotId, t.onDate),
]);

/** Syllabus per class-subject and term. */
export const syllabi = sqliteTable('syllabi', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  termId: text('term_id'),
  title: text('title').notNull(),
  fileId: text('file_id'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Chapters/units with planned end dates (behind-schedule alerts). */
export const syllabusUnits = sqliteTable('syllabus_units', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  syllabusId: text('syllabus_id').notNull(),
  title: text('title').notNull(),
  sequence: integer('sequence').notNull(),
  plannedPeriods: integer('planned_periods').notNull(),
  plannedEndDate: text('planned_end_date'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_syllabus_units_syllabus_id_sequence'.slice(0, 63)).on(t.syllabusId, t.sequence),
]);

/** Competencies / performance indicators (NCTB 2023 curriculum) mapped to units and Bloom level. */
export const learningOutcomes = sqliteTable('learning_outcomes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  unitId: text('unit_id'),
  code: text('code').notNull(),
  statement: text('statement').notNull(),
  statementBn: text('statement_bn'),
  bloomLevel: text('bloom_level', { enum: ['remember', 'understand', 'apply', 'analyse', 'evaluate', 'create'] }),
  weight: numeric('weight', { mode: 'number' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_learning_outcomes_class_subject_id_code'.slice(0, 63)).on(t.classSubjectId, t.code),
]);

/** Teacher lesson plans by date with outcomes, resources, homework and review. */
export const lessonPlans = sqliteTable('lesson_plans', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  teacherId: text('teacher_id').notNull(),
  sectionId: text('section_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  unitId: text('unit_id'),
  planDate: text('plan_date').notNull(),
  topic: text('topic').notNull(),
  objectives: text('objectives'),
  activities: text('activities'),
  outcomes: text('outcomes', { mode: 'json' }),
  resources: text('resources', { mode: 'json' }),
  homework: text('homework'),
  status: text('status', { enum: ['planned', 'taught', 'skipped'] }).notNull(),
  taughtAt: text('taught_at'),
  reviewedBy: text('reviewed_by'),
  reviewNote: text('review_note'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_lesson_plans_teacher_id_plan_date'.slice(0, 63)).on(t.teacherId, t.planDate),
]);

/** Rollup: units taught per section (refreshed by scheduler). */
export const syllabusProgress = sqliteTable('syllabus_progress', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  syllabusId: text('syllabus_id').notNull(),
  sectionId: text('section_id').notNull(),
  totalUnits: integer('total_units').notNull(),
  taughtUnits: integer('taught_units').notNull(),
  pct: numeric('pct', { mode: 'number' }).notNull(),
  refreshedAt: text('refreshed_at').notNull(),
}, (t) => [
  uniqueIndex('uq_syllabus_progress_syllabus_id_section_id'.slice(0, 63)).on(t.syllabusId, t.sectionId),
]);

// ============ Admissions & enquiry CRM ============
/** A season with seats per class, fees, test settings and selection mode. */
export const admissionCampaigns = sqliteTable('admission_campaigns', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  name: text('name').notNull(),
  opensAt: text('opens_at').notNull(),
  closesAt: text('closes_at').notNull(),
  formFee: numeric('form_fee', { mode: 'number' }).notNull(),
  admissionFeeHeadId: text('admission_fee_head_id'),
  selectionMode: text('selection_mode', { enum: ['test', 'lottery', 'first_come', 'interview', 'mixed'] }).notNull(),
  requiresTest: integer('requires_test', { mode: 'boolean' }).notNull(),
  autoMeritList: integer('auto_merit_list', { mode: 'boolean' }).notNull(),
  autoOffer: integer('auto_offer', { mode: 'boolean' }).notNull(),
  offerValidityDays: integer('offer_validity_days').notNull(),
  siblingPriority: integer('sibling_priority', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['draft', 'open', 'closed', 'archived'] }).notNull(),
  publicFormSlug: text('public_form_slug'),
  formSchema: text('form_schema', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Seats and age limits per class. */
export const admissionCampaignClasses = sqliteTable('admission_campaign_classes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campaignId: text('campaign_id').notNull(),
  classId: text('class_id').notNull(),
  seats: integer('seats').notNull(),
  minAgeYears: numeric('min_age_years', { mode: 'number' }),
  maxAgeYears: numeric('max_age_years', { mode: 'number' }),
  testId: text('test_id'),
}, (t) => [
  uniqueIndex('uq_admission_campaign_classes_campaign_id_class_id'.slice(0, 63)).on(t.campaignId, t.classId),
]);

/** Lead with source, counsellor (round-robin) and follow-up date. */
export const admissionEnquiries = sqliteTable('admission_enquiries', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campaignId: text('campaign_id'),
  studentName: text('student_name').notNull(),
  guardianName: text('guardian_name').notNull(),
  phone: text('phone').notNull(),
  email: text('email'),
  classId: text('class_id'),
  source: text('source', { enum: ['walk_in', 'website', 'facebook', 'referral', 'call', 'whatsapp', 'event', 'other'] }).notNull(),
  assignedTo: text('assigned_to'),
  status: text('status', { enum: ['new', 'contacted', 'visited', 'converted', 'lost'] }).notNull(),
  lostReason: text('lost_reason'),
  nextFollowUpAt: text('next_follow_up_at'),
  notes: text('notes'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Interaction log. */
export const enquiryFollowups = sqliteTable('enquiry_followups', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  enquiryId: text('enquiry_id').notNull(),
  note: text('note').notNull(),
  channel: text('channel', { enum: ['call', 'visit', 'sms', 'email', 'whatsapp'] }),
  byUserId: text('by_user_id'),
  nextAt: text('next_at'),
  createdAt: text('created_at').notNull(),
});

/** Application with applicant snapshot, pipeline status, score, rank, waitlist. */
export const admissionApplications = sqliteTable('admission_applications', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campaignId: text('campaign_id').notNull(),
  enquiryId: text('enquiry_id'),
  applicationNo: text('application_no').notNull(),
  classId: text('class_id').notNull(),
  shiftId: text('shift_id'),
  firstName: text('first_name').notNull(),
  lastName: text('last_name'),
  gender: text('gender', { enum: ['male', 'female', 'other'] }).notNull(),
  dateOfBirth: text('date_of_birth').notNull(),
  photoFileId: text('photo_file_id'),
  guardianName: text('guardian_name').notNull(),
  guardianPhone: text('guardian_phone').notNull(),
  guardianEmail: text('guardian_email'),
  guardianRelation: text('guardian_relation'),
  address: text('address', { mode: 'json' }),
  previousSchool: text('previous_school', { mode: 'json' }),
  extraFields: text('extra_fields', { mode: 'json' }),
  siblingStudentId: text('sibling_student_id'),
  status: text('status', { enum: ['draft', 'submitted', 'screening', 'test_scheduled', 'tested', 'shortlisted', 'waitlisted', 'offered', 'accepted', 'enrolled', 'rejected', 'withdrawn'] }).notNull(),
  formFeeInvoiceId: text('form_fee_invoice_id'),
  testScore: numeric('test_score', { mode: 'number' }),
  meritRank: integer('merit_rank'),
  lotteryNo: text('lottery_no'),
  waitlistPosition: integer('waitlist_position'),
  studentId: text('student_id'),
  submittedAt: text('submitted_at'),
  decidedAt: text('decided_at'),
  decidedBy: text('decided_by'),
  rejectionReason: text('rejection_reason'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_admission_applications_school_id_application_no'.slice(0, 63)).on(t.schoolId, t.applicationNo),
  index('ix_admission_applications_campaign_id_class_id_status'.slice(0, 63)).on(t.campaignId, t.classId, t.status),
]);

/** Uploaded applicant documents. */
export const applicationDocuments = sqliteTable('application_documents', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  applicationId: text('application_id').notNull(),
  docType: text('doc_type').notNull(),
  fileId: text('file_id').notNull(),
  verifiedAt: text('verified_at'),
  verifiedBy: text('verified_by'),
});

/** Entrance test/interview with components. */
export const admissionTests = sqliteTable('admission_tests', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campaignId: text('campaign_id').notNull(),
  classId: text('class_id').notNull(),
  name: text('name').notNull(),
  heldAt: text('held_at').notNull(),
  durationMin: integer('duration_min'),
  venue: text('venue'),
  totalMarks: numeric('total_marks', { mode: 'number' }).notNull(),
  passMarks: numeric('pass_marks', { mode: 'number' }),
  components: text('components', { mode: 'json' }),
  onlineExamId: text('online_exam_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Score per applicant. */
export const admissionTestResults = sqliteTable('admission_test_results', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  testId: text('test_id').notNull(),
  applicationId: text('application_id').notNull(),
  componentMarks: text('component_marks', { mode: 'json' }),
  totalMarks: numeric('total_marks', { mode: 'number' }),
  isAbsent: integer('is_absent', { mode: 'boolean' }).notNull(),
  remarks: text('remarks'),
  enteredBy: text('entered_by'),
  enteredAt: text('entered_at').notNull(),
}, (t) => [
  uniqueIndex('uq_admission_test_results_test_id_application_id'.slice(0, 63)).on(t.testId, t.applicationId),
]);

/** Interview slots per applicant: when, where, who sits on the panel, and how it went. */
export const admissionInterviews = sqliteTable('admission_interviews', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  testId: text('test_id').notNull(),
  applicationId: text('application_id'),
  startsAt: text('starts_at').notNull(),
  endsAt: text('ends_at').notNull(),
  venue: text('venue'),
  panel: text('panel', { mode: 'json' }),
  status: text('status', { enum: ['open', 'booked', 'attended', 'no_show', 'cancelled'] }).notNull(),
  notes: text('notes'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_admission_interviews_application_id'.slice(0, 63)).on(t.applicationId),
  index('ix_admission_interviews_test_id_starts_at'.slice(0, 63)).on(t.testId, t.startsAt),
]);

/** Offer with expiry; auto-revoked and waitlist promoted when unpaid. */
export const admissionOffers = sqliteTable('admission_offers', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  applicationId: text('application_id').notNull(),
  offeredAt: text('offered_at').notNull(),
  expiresAt: text('expires_at').notNull(),
  admissionFeeInvoiceId: text('admission_fee_invoice_id'),
  offerLetterFileId: text('offer_letter_file_id'),
  acceptedAt: text('accepted_at'),
  declinedAt: text('declined_at'),
  revokedAt: text('revoked_at'),
  revokeReason: text('revoke_reason'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Attendance & leave ============
/** Biometric / RFID / face / QR / bus readers. */
export const attendanceDevices = sqliteTable('attendance_devices', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campusId: text('campus_id'),
  name: text('name').notNull(),
  deviceType: text('device_type', { enum: ['biometric', 'rfid', 'face', 'qr', 'gps_bus', 'mobile_app'] }).notNull(),
  vendor: text('vendor'),
  serialNo: text('serial_no'),
  apiKeyHash: text('api_key_hash'),
  location: text('location'),
  direction: text('direction', { enum: ['in', 'out', 'both'] }).notNull(),
  lastSeenAt: text('last_seen_at'),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Punches exactly as received; resolved to a person later. */
export const devicePunchLogs = sqliteTable('device_punch_logs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  deviceId: text('device_id').notNull(),
  identifier: text('identifier').notNull(),
  punchedAt: text('punched_at').notNull(),
  direction: text('direction'),
  rawPayload: text('raw_payload', { mode: 'json' }),
  personType: text('person_type', { enum: ['student', 'staff'] }),
  personId: text('person_id'),
  processedAt: text('processed_at'),
  error: text('error'),
});

/** One row per student per day. */
export const studentAttendance = sqliteTable('student_attendance', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  sectionId: text('section_id'),
  onDate: text('on_date').notNull(),
  status: text('status', { enum: ['present', 'absent', 'late', 'half_day', 'excused', 'holiday'] }).notNull(),
  checkIn: text('check_in'),
  checkOut: text('check_out'),
  lateMinutes: integer('late_minutes'),
  source: text('source', { enum: ['manual', 'device', 'app', 'import', 'system', 'bus'] }).notNull(),
  markedBy: text('marked_by'),
  remarks: text('remarks'),
  guardianNotifiedAt: text('guardian_notified_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_student_attendance_student_id_on_date'.slice(0, 63)).on(t.studentId, t.onDate),
  index('ix_student_attendance_section_id_on_date'.slice(0, 63)).on(t.sectionId, t.onDate),
]);

/** Period-wise attendance (college / subject-wise). */
export const studentPeriodAttendance = sqliteTable('student_period_attendance', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  slotId: text('slot_id').notNull(),
  onDate: text('on_date').notNull(),
  status: text('status', { enum: ['present', 'absent', 'late', 'excused'] }).notNull(),
  markedBy: text('marked_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_student_period_attendance_student_id_slot_id_on_date'.slice(0, 63)).on(t.studentId, t.slotId, t.onDate),
]);

/** Staff daily attendance with work/overtime minutes feeding payroll. */
export const staffAttendance = sqliteTable('staff_attendance', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  onDate: text('on_date').notNull(),
  status: text('status', { enum: ['present', 'absent', 'late', 'half_day', 'excused', 'holiday', 'wfh'] }).notNull(),
  checkIn: text('check_in'),
  checkOut: text('check_out'),
  lateMinutes: integer('late_minutes'),
  earlyLeaveMinutes: integer('early_leave_minutes'),
  workMinutes: integer('work_minutes'),
  overtimeMinutes: integer('overtime_minutes'),
  source: text('source', { enum: ['manual', 'device', 'app', 'import', 'system'] }).notNull(),
  markedBy: text('marked_by'),
  remarks: text('remarks'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_staff_attendance_staff_id_on_date'.slice(0, 63)).on(t.staffId, t.onDate),
]);

/** Cut-off time, late/half-day thresholds, notification switches, escalation, minimum %. */
export const attendancePolicies = sqliteTable('attendance_policies', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  audience: text('audience', { enum: ['student', 'staff'] }).notNull(),
  classId: text('class_id'),
  shiftId: text('shift_id'),
  lateAfterMinutes: integer('late_after_minutes').notNull(),
  halfDayAfterMinutes: integer('half_day_after_minutes').notNull(),
  autoAbsentAt: text('auto_absent_at'),
  notifyOnArrival: integer('notify_on_arrival', { mode: 'boolean' }).notNull(),
  notifyOnAbsent: integer('notify_on_absent', { mode: 'boolean' }).notNull(),
  notifyOnLate: integer('notify_on_late', { mode: 'boolean' }).notNull(),
  consecutiveAbsentAlert: integer('consecutive_absent_alert').notNull(),
  minAttendancePct: numeric('min_attendance_pct', { mode: 'number' }).notNull(),
  blockExamBelowMin: integer('block_exam_below_min', { mode: 'boolean' }).notNull(),
  lateCountToLop: integer('late_count_to_lop'),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Casual, sick, earned, maternity, study… with accrual and carry-forward. */
export const leaveTypes = sqliteTable('leave_types', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  code: text('code').notNull(),
  audience: text('audience', { enum: ['student', 'staff'] }).notNull(),
  daysPerYear: numeric('days_per_year', { mode: 'number' }).notNull(),
  accrual: text('accrual', { enum: ['yearly', 'monthly', 'none'] }).notNull(),
  isPaid: integer('is_paid', { mode: 'boolean' }).notNull(),
  carryForwardMax: numeric('carry_forward_max', { mode: 'number' }).notNull(),
  requiresDocument: integer('requires_document', { mode: 'boolean' }).notNull(),
  minNoticeDays: integer('min_notice_days').notNull(),
  encashable: integer('encashable', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_leave_types_school_id_code'.slice(0, 63)).on(t.schoolId, t.code),
]);

/** Per staff per type per year. */
export const leaveBalances = sqliteTable('leave_balances', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  leaveTypeId: text('leave_type_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  allocated: numeric('allocated', { mode: 'number' }).notNull(),
  carriedForward: numeric('carried_forward', { mode: 'number' }).notNull(),
  used: numeric('used', { mode: 'number' }).notNull(),
  encashed: numeric('encashed', { mode: 'number' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_leave_balances_staff_id_leave_type_id_academic_year_id'.slice(0, 63)).on(t.staffId, t.leaveTypeId, t.academicYearId),
]);

/** Student or staff leave through the approval workflow. */
export const leaveApplications = sqliteTable('leave_applications', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  applicantType: text('applicant_type', { enum: ['student', 'staff'] }).notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  leaveTypeId: text('leave_type_id').notNull(),
  fromDate: text('from_date').notNull(),
  toDate: text('to_date').notNull(),
  halfDay: text('half_day', { enum: ['first', 'second'] }),
  days: numeric('days', { mode: 'number' }).notNull(),
  reason: text('reason').notNull(),
  documentFileId: text('document_file_id'),
  appliedBy: text('applied_by'),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'cancelled'] }).notNull(),
  approvalRequestId: text('approval_request_id'),
  decidedBy: text('decided_by'),
  decidedAt: text('decided_at'),
  decisionNote: text('decision_note'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_leave_applications_school_id_status'.slice(0, 63)).on(t.schoolId, t.status),
]);

/** Rollup per student per month (refreshed nightly); used by report cards, eligibility, alerts. */
export const attendanceMonthlySummary = sqliteTable('attendance_monthly_summary', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  month: text('month').notNull(),
  presentDays: integer('present_days').notNull(),
  absentDays: integer('absent_days').notNull(),
  lateDays: integer('late_days').notNull(),
  excusedDays: integer('excused_days').notNull(),
  workingDays: integer('working_days').notNull(),
  pct: numeric('pct', { mode: 'number' }).notNull(),
}, (t) => [
  uniqueIndex('uq_attendance_monthly_summary_student_id_month'.slice(0, 63)).on(t.studentId, t.month),
]);

// ============ Examinations & assessment ============
/** Named scales (BD Board GPA 5.0, Cambridge A*–G, IB 1–7). */
export const gradingScales = sqliteTable('grading_scales', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  isDefault: integer('is_default', { mode: 'boolean' }).notNull(),
  gpaMax: numeric('gpa_max', { mode: 'number' }).notNull(),
  failGpaZero: integer('fail_gpa_zero', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_grading_scales_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Grade bands with ranges and points. */
export const gradingBands = sqliteTable('grading_bands', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  scaleId: text('scale_id').notNull(),
  grade: text('grade').notNull(),
  minPercent: numeric('min_percent', { mode: 'number' }).notNull(),
  maxPercent: numeric('max_percent', { mode: 'number' }).notNull(),
  gradePoint: numeric('grade_point', { mode: 'number' }).notNull(),
  isFail: integer('is_fail', { mode: 'boolean' }).notNull(),
  remarks: text('remarks'),
}, (t) => [
  uniqueIndex('uq_grading_bands_scale_id_grade'.slice(0, 63)).on(t.scaleId, t.grade),
]);

/** Rating scales for competency-based assessment (e.g. NCTB triangle/circle/square, 1–4 rubric). */
export const competencyScales = sqliteTable('competency_scales', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  levels: text('levels', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_competency_scales_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Class test, half-yearly, annual, model test… with weight in the annual aggregate. */
export const examTypes = sqliteTable('exam_types', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  weightPct: numeric('weight_pct', { mode: 'number' }).notNull(),
  isInternal: integer('is_internal', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_exam_types_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** An exam event for a year/term with scale, eligibility rules and publish time. */
export const exams = sqliteTable('exams', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  termId: text('term_id'),
  examTypeId: text('exam_type_id').notNull(),
  gradingScaleId: text('grading_scale_id').notNull(),
  name: text('name').notNull(),
  startDate: text('start_date').notNull(),
  endDate: text('end_date').notNull(),
  marksEntryDeadline: text('marks_entry_deadline'),
  publishAt: text('publish_at'),
  status: text('status', { enum: ['draft', 'scheduled', 'ongoing', 'marks_entry', 'processing', 'published', 'locked'] }).notNull(),
  requireFeeClearance: integer('require_fee_clearance', { mode: 'boolean' }).notNull(),
  minAttendancePct: numeric('min_attendance_pct', { mode: 'number' }),
  rankScope: text('rank_scope', { enum: ['section', 'class', 'both'] }).notNull(),
  tieRule: text('tie_rule', { enum: ['share_rank', 'dense', 'by_total'] }).notNull(),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** One paper per class-subject: date, time, room, marks split. */
export const examSchedules = sqliteTable('exam_schedules', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  examId: text('exam_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  examDate: text('exam_date'),
  startTime: text('start_time'),
  endTime: text('end_time'),
  roomId: text('room_id'),
  fullMarks: numeric('full_marks', { mode: 'number' }).notNull(),
  passMarks: numeric('pass_marks', { mode: 'number' }).notNull(),
  theoryMarks: numeric('theory_marks', { mode: 'number' }),
  practicalMarks: numeric('practical_marks', { mode: 'number' }),
  caMarks: numeric('ca_marks', { mode: 'number' }),
  marksEntryLocked: integer('marks_entry_locked', { mode: 'boolean' }).notNull(),
  questionPaperId: text('question_paper_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_exam_schedules_exam_id_class_subject_id'.slice(0, 63)).on(t.examId, t.classSubjectId),
]);

/** Duty roster. */
export const examInvigilators = sqliteTable('exam_invigilators', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  scheduleId: text('schedule_id').notNull(),
  roomId: text('room_id').notNull(),
  staffId: text('staff_id').notNull(),
}, (t) => [
  uniqueIndex('uq_exam_invigilators_schedule_id_room_id_staff_id'.slice(0, 63)).on(t.scheduleId, t.roomId, t.staffId),
]);

/** Seat per student per exam; admit-card file; eligibility flag. */
export const examSeatPlans = sqliteTable('exam_seat_plans', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  examId: text('exam_id').notNull(),
  studentId: text('student_id').notNull(),
  roomId: text('room_id'),
  seatNo: text('seat_no').notNull(),
  admitCardFileId: text('admit_card_file_id'),
  isEligible: integer('is_eligible', { mode: 'boolean' }).notNull(),
  ineligibleReason: text('ineligible_reason'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_exam_seat_plans_exam_id_student_id'.slice(0, 63)).on(t.examId, t.studentId),
  uniqueIndex('uq_exam_seat_plans_exam_id_room_id_seat_no'.slice(0, 63)).on(t.examId, t.roomId, t.seatNo),
]);

/** Marks per student per paper with computed grade and workflow status. */
export const marks = sqliteTable('marks', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  scheduleId: text('schedule_id').notNull(),
  studentId: text('student_id').notNull(),
  theoryObtained: numeric('theory_obtained', { mode: 'number' }),
  practicalObtained: numeric('practical_obtained', { mode: 'number' }),
  caObtained: numeric('ca_obtained', { mode: 'number' }),
  totalObtained: numeric('total_obtained', { mode: 'number' }),
  isAbsent: integer('is_absent', { mode: 'boolean' }).notNull(),
  grade: text('grade'),
  gradePoint: numeric('grade_point', { mode: 'number' }),
  isPass: integer('is_pass', { mode: 'boolean' }),
  status: text('status', { enum: ['draft', 'submitted', 'verified', 'locked'] }).notNull(),
  enteredBy: text('entered_by'),
  enteredAt: text('entered_at'),
  verifiedBy: text('verified_by'),
  verifiedAt: text('verified_at'),
  remarks: text('remarks'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_marks_schedule_id_student_id'.slice(0, 63)).on(t.scheduleId, t.studentId),
]);

/** Competency-based rating per student per learning outcome per term (no marks). */
export const competencyAssessments = sqliteTable('competency_assessments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  outcomeId: text('outcome_id').notNull(),
  termId: text('term_id').notNull(),
  scaleId: text('scale_id').notNull(),
  levelCode: text('level_code').notNull(),
  evidence: text('evidence', { mode: 'json' }),
  assessedBy: text('assessed_by'),
  assessedAt: text('assessed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_competency_assessments_student_id_outcome_id_term_id'.slice(0, 63)).on(t.studentId, t.outcomeId, t.termId),
]);

/** Aggregated result per student per exam, frozen on publish. */
export const examResults = sqliteTable('exam_results', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  examId: text('exam_id').notNull(),
  studentId: text('student_id').notNull(),
  sectionId: text('section_id'),
  totalFullMarks: numeric('total_full_marks', { mode: 'number' }).notNull(),
  totalObtained: numeric('total_obtained', { mode: 'number' }).notNull(),
  percentage: numeric('percentage', { mode: 'number' }).notNull(),
  gpa: numeric('gpa', { mode: 'number' }),
  grade: text('grade'),
  failedSubjects: integer('failed_subjects').notNull(),
  isPass: integer('is_pass', { mode: 'boolean' }).notNull(),
  rankInSection: integer('rank_in_section'),
  rankInClass: integer('rank_in_class'),
  attendancePct: numeric('attendance_pct', { mode: 'number' }),
  teacherRemark: text('teacher_remark'),
  principalRemark: text('principal_remark'),
  reportCardFileId: text('report_card_file_id'),
  publishedAt: text('published_at'),
  computedAt: text('computed_at').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_exam_results_exam_id_student_id'.slice(0, 63)).on(t.examId, t.studentId),
  index('ix_exam_results_exam_id_section_id_rank_in_section'.slice(0, 63)).on(t.examId, t.sectionId, t.rankInSection),
]);

/** Weighted aggregate across the year’s exams → promotion input. */
export const annualResults = sqliteTable('annual_results', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  studentId: text('student_id').notNull(),
  weightedGpa: numeric('weighted_gpa', { mode: 'number' }),
  weightedPct: numeric('weighted_pct', { mode: 'number' }),
  rankInClass: integer('rank_in_class'),
  decision: text('decision', { enum: ['promoted', 'retained', 'graduated', 'conditional', 'pending'] }).notNull(),
  computedAt: text('computed_at').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_annual_results_academic_year_id_student_id'.slice(0, 63)).on(t.academicYearId, t.studentId),
]);

/** Min GPA, max failed subjects, min attendance; auto-apply switch. */
export const promotionRules = sqliteTable('promotion_rules', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  classId: text('class_id'),
  minGpa: numeric('min_gpa', { mode: 'number' }).notNull(),
  maxFailedSubjects: integer('max_failed_subjects').notNull(),
  minAttendancePct: numeric('min_attendance_pct', { mode: 'number' }).notNull(),
  autoApply: integer('auto_apply', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_promotion_rules_academic_year_id_class_id'.slice(0, 63)).on(t.academicYearId, t.classId),
]);

/** Decision per enrollment. */
export const promotions = sqliteTable('promotions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  fromEnrollmentId: text('from_enrollment_id').notNull(),
  toEnrollmentId: text('to_enrollment_id'),
  decision: text('decision', { enum: ['promoted', 'retained', 'graduated', 'conditional'] }).notNull(),
  annualGpa: numeric('annual_gpa', { mode: 'number' }),
  isAuto: integer('is_auto', { mode: 'boolean' }).notNull(),
  decidedBy: text('decided_by'),
  note: text('note'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Question bank with type, difficulty, outcome mapping and auto-grade answer key. */
export const questions = sqliteTable('questions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  subjectId: text('subject_id').notNull(),
  classId: text('class_id'),
  unitId: text('unit_id'),
  outcomeId: text('outcome_id'),
  qType: text('q_type', { enum: ['mcq', 'true_false', 'short', 'long', 'fill_blank', 'match', 'numeric', 'essay'] }).notNull(),
  difficulty: text('difficulty', { enum: ['easy', 'medium', 'hard'] }).notNull(),
  body: text('body').notNull(),
  bodyBn: text('body_bn'),
  options: text('options', { mode: 'json' }),
  answer: text('answer', { mode: 'json' }),
  marks: numeric('marks', { mode: 'number' }).notNull(),
  tags: text('tags', { mode: 'json' }),
  aiGenerated: integer('ai_generated', { mode: 'boolean' }).notNull(),
  createdBy: text('created_by'),
  usageCount: integer('usage_count').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_questions_school_id_subject_id_class_id'.slice(0, 63)).on(t.schoolId, t.subjectId, t.classId),
]);

/** Generated/curated question papers (blueprint: marks by unit × difficulty), PDF output. */
export const questionPapers = sqliteTable('question_papers', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  examId: text('exam_id'),
  title: text('title').notNull(),
  blueprint: text('blueprint', { mode: 'json' }),
  totalMarks: numeric('total_marks', { mode: 'number' }).notNull(),
  durationMin: integer('duration_min'),
  instructions: text('instructions'),
  setLabel: text('set_label'),
  pdfFileId: text('pdf_file_id'),
  status: text('status', { enum: ['draft', 'final'] }).notNull(),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Questions in a paper. */
export const questionPaperItems = sqliteTable('question_paper_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  paperId: text('paper_id').notNull(),
  questionId: text('question_id').notNull(),
  sequence: integer('sequence').notNull(),
  marks: numeric('marks', { mode: 'number' }).notNull(),
  section: text('section'),
}, (t) => [
  uniqueIndex('uq_question_paper_items_paper_id_question_id'.slice(0, 63)).on(t.paperId, t.questionId),
]);

/** Timed online exam; MCQ auto-graded; can sync into a formal paper. */
export const onlineExams = sqliteTable('online_exams', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  scheduleId: text('schedule_id'),
  sectionId: text('section_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  paperId: text('paper_id'),
  title: text('title').notNull(),
  instructions: text('instructions'),
  startsAt: text('starts_at').notNull(),
  endsAt: text('ends_at').notNull(),
  durationMin: integer('duration_min').notNull(),
  totalMarks: numeric('total_marks', { mode: 'number' }).notNull(),
  shuffleQuestions: integer('shuffle_questions', { mode: 'boolean' }).notNull(),
  autoGrade: integer('auto_grade', { mode: 'boolean' }).notNull(),
  autoPublish: integer('auto_publish', { mode: 'boolean' }).notNull(),
  proctoring: text('proctoring', { mode: 'json' }),
  status: text('status', { enum: ['draft', 'scheduled', 'live', 'closed', 'published'] }).notNull(),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Attempt with answers, auto/manual score, proctoring meta. */
export const onlineExamAttempts = sqliteTable('online_exam_attempts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  onlineExamId: text('online_exam_id').notNull(),
  studentId: text('student_id').notNull(),
  startedAt: text('started_at').notNull(),
  submittedAt: text('submitted_at'),
  answers: text('answers', { mode: 'json' }),
  autoScore: numeric('auto_score', { mode: 'number' }),
  manualScore: numeric('manual_score', { mode: 'number' }),
  finalScore: numeric('final_score', { mode: 'number' }),
  gradedBy: text('graded_by'),
  status: text('status', { enum: ['in_progress', 'submitted', 'auto_graded', 'graded'] }).notNull(),
  clientMeta: text('client_meta', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_online_exam_attempts_online_exam_id_student_id'.slice(0, 63)).on(t.onlineExamId, t.studentId),
]);

/** Scanned OMR answer sheets processed by the OMR engine. */
export const omrSheets = sqliteTable('omr_sheets', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  scheduleId: text('schedule_id'),
  onlineExamId: text('online_exam_id'),
  studentId: text('student_id'),
  fileId: text('file_id').notNull(),
  detectedRoll: text('detected_roll'),
  answers: text('answers', { mode: 'json' }),
  score: numeric('score', { mode: 'number' }),
  confidence: numeric('confidence', { mode: 'number' }),
  status: text('status', { enum: ['uploaded', 'processed', 'needs_review', 'applied', 'rejected'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Board exam (SSC/HSC/JSC/Dakhil) registration and form fill-up per student with fees and export. */
export const boardRegistrations = sqliteTable('board_registrations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  board: text('board').notNull(),
  examName: text('exam_name').notNull(),
  registrationNo: text('registration_no'),
  rollNo: text('roll_no'),
  centre: text('centre'),
  groupName: text('group_name'),
  subjects: text('subjects', { mode: 'json' }),
  feeInvoiceId: text('fee_invoice_id'),
  status: text('status', { enum: ['draft', 'submitted', 'confirmed', 'admitted', 'result_received'] }).notNull(),
  boardResult: text('board_result', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_board_registrations_student_id_exam_name'.slice(0, 63)).on(t.studentId, t.examName),
]);

// ============ LMS · courses, lessons, live classes ============
/** A course: tied to a class-subject or sold standalone (coaching, skills). */
export const courses = sqliteTable('courses', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  classSubjectId: text('class_subject_id'),
  title: text('title').notNull(),
  slug: text('slug').notNull(),
  description: text('description'),
  coverFileId: text('cover_file_id'),
  teacherId: text('teacher_id'),
  isPaid: integer('is_paid', { mode: 'boolean' }).notNull(),
  price: numeric('price', { mode: 'number' }).notNull(),
  feeHeadId: text('fee_head_id'),
  status: text('status', { enum: ['draft', 'published', 'archived'] }).notNull(),
  publishedAt: text('published_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
  deletedAt: text('deleted_at'),
}, (t) => [
  uniqueIndex('uq_courses_school_id_slug'.slice(0, 63)).on(t.schoolId, t.slug),
]);

/** Sections inside a course. */
export const courseModules = sqliteTable('course_modules', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  courseId: text('course_id').notNull(),
  title: text('title').notNull(),
  sequence: integer('sequence').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Lesson content: video, notes, link, SCORM/H5P, quiz. */
export const lessons = sqliteTable('lessons', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  moduleId: text('module_id').notNull(),
  title: text('title').notNull(),
  sequence: integer('sequence').notNull(),
  lessonType: text('lesson_type', { enum: ['video', 'note', 'link', 'file', 'quiz', 'assignment', 'live'] }).notNull(),
  body: text('body'),
  fileId: text('file_id'),
  videoUrl: text('video_url'),
  durationMin: integer('duration_min'),
  isFreePreview: integer('is_free_preview', { mode: 'boolean' }).notNull(),
  unitId: text('unit_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Student enrolled in a course (auto for class-subject courses). */
export const courseEnrollments = sqliteTable('course_enrollments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  courseId: text('course_id').notNull(),
  studentId: text('student_id').notNull(),
  enrolledAt: text('enrolled_at').notNull(),
  progressPct: numeric('progress_pct', { mode: 'number' }).notNull(),
  completedAt: text('completed_at'),
  certificateDocId: text('certificate_doc_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_course_enrollments_course_id_student_id'.slice(0, 63)).on(t.courseId, t.studentId),
]);

/** Per student per lesson. */
export const lessonProgress = sqliteTable('lesson_progress', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  lessonId: text('lesson_id').notNull(),
  studentId: text('student_id').notNull(),
  status: text('status', { enum: ['not_started', 'in_progress', 'completed'] }).notNull(),
  secondsWatched: integer('seconds_watched').notNull(),
  lastPosition: integer('last_position').notNull(),
  completedAt: text('completed_at'),
}, (t) => [
  uniqueIndex('uq_lesson_progress_lesson_id_student_id'.slice(0, 63)).on(t.lessonId, t.studentId),
]);

/** A go at the quiz inside a lesson: answers, score, and whether it passed. */
export const lessonQuizAttempts = sqliteTable('lesson_quiz_attempts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  lessonId: text('lesson_id').notNull(),
  studentId: text('student_id').notNull(),
  attemptNo: integer('attempt_no').notNull(),
  answers: text('answers', { mode: 'json' }),
  score: numeric('score', { mode: 'number' }).notNull(),
  maxScore: numeric('max_score', { mode: 'number' }).notNull(),
  passed: integer('passed', { mode: 'boolean' }).notNull(),
  submittedAt: text('submitted_at').notNull(),
}, (t) => [
  index('ix_lesson_quiz_attempts_lesson_id_student_id'.slice(0, 63)).on(t.lessonId, t.studentId),
]);

/** Homework/assignment with due date, penalty and submission type. */
export const assignments = sqliteTable('assignments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  sectionId: text('section_id').notNull(),
  classSubjectId: text('class_subject_id').notNull(),
  teacherId: text('teacher_id').notNull(),
  lessonId: text('lesson_id'),
  title: text('title').notNull(),
  description: text('description'),
  attachments: text('attachments', { mode: 'json' }),
  assignedAt: text('assigned_at').notNull(),
  dueAt: text('due_at').notNull(),
  maxMarks: numeric('max_marks', { mode: 'number' }),
  allowLate: integer('allow_late', { mode: 'boolean' }).notNull(),
  latePenaltyPct: numeric('late_penalty_pct', { mode: 'number' }).notNull(),
  submissionType: text('submission_type', { enum: ['file', 'text', 'both', 'offline', 'photo'] }).notNull(),
  status: text('status', { enum: ['draft', 'published', 'closed'] }).notNull(),
  reminderSentAt: text('reminder_sent_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_assignments_school_id_due_at'.slice(0, 63)).on(t.schoolId, t.dueAt),
]);

/** Submission with grading and feedback; plagiarism score optional. */
export const assignmentSubmissions = sqliteTable('assignment_submissions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  assignmentId: text('assignment_id').notNull(),
  studentId: text('student_id').notNull(),
  submittedAt: text('submitted_at').notNull(),
  isLate: integer('is_late', { mode: 'boolean' }).notNull(),
  textAnswer: text('text_answer'),
  attachments: text('attachments', { mode: 'json' }),
  marks: numeric('marks', { mode: 'number' }),
  feedback: text('feedback'),
  similarityPct: numeric('similarity_pct', { mode: 'number' }),
  gradedBy: text('graded_by'),
  gradedAt: text('graded_at'),
  status: text('status', { enum: ['submitted', 'graded', 'returned', 'resubmit'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_assignment_submissions_assignment_id_student_id'.slice(0, 63)).on(t.assignmentId, t.studentId),
]);

/** Notes, slides, videos, links by unit. */
export const studyMaterials = sqliteTable('study_materials', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  classSubjectId: text('class_subject_id'),
  sectionId: text('section_id'),
  unitId: text('unit_id'),
  title: text('title').notNull(),
  materialType: text('material_type', { enum: ['note', 'slide', 'video', 'link', 'book', 'audio'] }).notNull(),
  fileId: text('file_id'),
  externalUrl: text('external_url'),
  uploadedBy: text('uploaded_by'),
  publishedAt: text('published_at').notNull(),
  viewCount: integer('view_count').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Live class (Zoom/Meet/Jitsi) auto-created from timetable on remote days. */
export const onlineClasses = sqliteTable('online_classes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  sectionId: text('section_id').notNull(),
  classSubjectId: text('class_subject_id'),
  teacherId: text('teacher_id').notNull(),
  slotId: text('slot_id'),
  title: text('title').notNull(),
  platform: text('platform', { enum: ['zoom', 'google_meet', 'jitsi', 'bbb', 'youtube_live'] }).notNull(),
  meetingId: text('meeting_id'),
  joinUrl: text('join_url'),
  hostUrl: text('host_url'),
  startsAt: text('starts_at').notNull(),
  durationMin: integer('duration_min').notNull(),
  recordingUrl: text('recording_url'),
  status: text('status', { enum: ['scheduled', 'live', 'ended', 'cancelled'] }).notNull(),
  reminderSentAt: text('reminder_sent_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_online_classes_school_id_starts_at'.slice(0, 63)).on(t.schoolId, t.startsAt),
]);

/** From platform webhooks. */
export const onlineClassAttendance = sqliteTable('online_class_attendance', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  onlineClassId: text('online_class_id').notNull(),
  studentId: text('student_id').notNull(),
  joinedAt: text('joined_at'),
  leftAt: text('left_at'),
  minutes: integer('minutes'),
}, (t) => [
  uniqueIndex('uq_online_class_attendance_online_class_id_student_id'.slice(0, 63)).on(t.onlineClassId, t.studentId),
]);

/** Q&A threads under lessons/courses. */
export const discussions = sqliteTable('discussions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  courseId: text('course_id'),
  lessonId: text('lesson_id'),
  authorId: text('author_id'),
  parentId: text('parent_id'),
  body: text('body').notNull(),
  isAnswer: integer('is_answer', { mode: 'boolean' }).notNull(),
  upvotes: integer('upvotes').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Daily diary & early years ============
/** Per section per day: homework, notes, announcements — replaces the paper diary. */
export const diaryEntries = sqliteTable('diary_entries', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  sectionId: text('section_id').notNull(),
  onDate: text('on_date').notNull(),
  teacherId: text('teacher_id'),
  classSubjectId: text('class_subject_id'),
  entryType: text('entry_type', { enum: ['homework', 'note', 'reminder', 'announcement'] }).notNull(),
  body: text('body').notNull(),
  attachments: text('attachments', { mode: 'json' }),
  dueDate: text('due_date'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_diary_entries_section_id_on_date'.slice(0, 63)).on(t.sectionId, t.onDate),
]);

/** Guardian saw/acknowledged. */
export const diaryAcknowledgements = sqliteTable('diary_acknowledgements', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  entryId: text('entry_id').notNull(),
  studentId: text('student_id').notNull(),
  guardianId: text('guardian_id'),
  ackedAt: text('acked_at').notNull(),
}, (t) => [
  uniqueIndex('uq_diary_acknowledgements_entry_id_student_id'.slice(0, 63)).on(t.entryId, t.studentId),
]);

/** Early-years daily report per child (meals, nap, mood, activities, photos). */
export const dailyReports = sqliteTable('daily_reports', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  onDate: text('on_date').notNull(),
  meals: text('meals', { mode: 'json' }),
  napMinutes: integer('nap_minutes'),
  mood: text('mood', { enum: ['happy', 'calm', 'tired', 'upset', 'sick'] }),
  activities: text('activities', { mode: 'json' }),
  toilet: text('toilet', { mode: 'json' }),
  notes: text('notes'),
  photos: text('photos', { mode: 'json' }),
  teacherId: text('teacher_id'),
  sentAt: text('sent_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_daily_reports_student_id_on_date'.slice(0, 63)).on(t.studentId, t.onDate),
]);

/** Teacher remarks/observations on a student (positive or concern) visible to guardians. */
export const studentRemarks = sqliteTable('student_remarks', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  teacherId: text('teacher_id').notNull(),
  onDate: text('on_date').notNull(),
  remark: text('remark').notNull(),
  polarity: text('polarity', { enum: ['positive', 'neutral', 'concern'] }).notNull(),
  visibleToGuardian: integer('visible_to_guardian', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Co-curricular · clubs, sports, achievements, portfolio ============
/** Clubs/societies with advisor. */
export const clubs = sqliteTable('clubs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  category: text('category', { enum: ['academic', 'sports', 'arts', 'social', 'tech', 'religious', 'other'] }).notNull(),
  advisorId: text('advisor_id'),
  description: text('description'),
  meetingSchedule: text('meeting_schedule'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Members and roles. */
export const clubMemberships = sqliteTable('club_memberships', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  clubId: text('club_id').notNull(),
  studentId: text('student_id').notNull(),
  role: text('role', { enum: ['member', 'secretary', 'president', 'captain'] }).notNull(),
  joinedOn: text('joined_on').notNull(),
  leftOn: text('left_on'),
}, (t) => [
  uniqueIndex('uq_club_memberships_club_id_student_id'.slice(0, 63)).on(t.clubId, t.studentId),
]);

/** Internal/external competitions and events with results. */
export const competitions = sqliteTable('competitions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  kind: text('kind', { enum: ['sports', 'academic', 'cultural', 'science', 'debate', 'olympiad', 'other'] }).notNull(),
  level: text('level', { enum: ['intra', 'inter_school', 'district', 'national', 'international'] }).notNull(),
  heldOn: text('held_on'),
  venue: text('venue'),
  organiser: text('organiser'),
  eventId: text('event_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Placement per participant/team. */
export const competitionResults = sqliteTable('competition_results', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  competitionId: text('competition_id').notNull(),
  studentId: text('student_id'),
  clubId: text('club_id'),
  houseId: text('house_id'),
  position: integer('position'),
  award: text('award'),
  points: integer('points').notNull(),
  certificateDocId: text('certificate_doc_id'),
});

/** Points ledger per house (behaviour, sports, competitions). */
export const housePoints = sqliteTable('house_points', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  houseId: text('house_id').notNull(),
  studentId: text('student_id'),
  points: integer('points').notNull(),
  reason: text('reason').notNull(),
  sourceType: text('source_type'),
  sourceId: text('source_id'),
  awardedBy: text('awarded_by'),
  awardedAt: text('awarded_at').notNull(),
});

/** Student achievements and awards (portfolio). */
export const achievements = sqliteTable('achievements', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  title: text('title').notNull(),
  category: text('category'),
  achievedOn: text('achieved_on'),
  description: text('description'),
  fileId: text('file_id'),
  verifiedBy: text('verified_by'),
  isPublic: integer('is_public', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Badge catalogue (e.g. Reading star, Coder L1). */
export const skillBadges = sqliteTable('skill_badges', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  iconFileId: text('icon_file_id'),
  criteria: text('criteria'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Badges earned. */
export const studentBadges = sqliteTable('student_badges', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  badgeId: text('badge_id').notNull(),
  studentId: text('student_id').notNull(),
  awardedBy: text('awarded_by'),
  awardedAt: text('awarded_at').notNull(),
}, (t) => [
  uniqueIndex('uq_student_badges_badge_id_student_id'.slice(0, 63)).on(t.badgeId, t.studentId),
]);

// ============ Library & e-library ============
/** Category tree. */
export const libraryCategories = sqliteTable('library_categories', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  parentId: text('parent_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_library_categories_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Title-level record. */
export const libraryBooks = sqliteTable('library_books', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  isbn: text('isbn'),
  title: text('title').notNull(),
  subtitle: text('subtitle'),
  authors: text('authors', { mode: 'json' }),
  publisher: text('publisher'),
  edition: text('edition'),
  publishedYear: integer('published_year'),
  language: text('language').notNull(),
  categoryId: text('category_id'),
  subjectId: text('subject_id'),
  classId: text('class_id'),
  pages: integer('pages'),
  price: numeric('price', { mode: 'number' }),
  coverFileId: text('cover_file_id'),
  ebookFileId: text('ebook_file_id'),
  description: text('description'),
  totalCopies: integer('total_copies').notNull(),
  availableCopies: integer('available_copies').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_library_books_school_id_title'.slice(0, 63)).on(t.schoolId, t.title),
]);

/** Physical copy with accession no and status. */
export const libraryBookCopies = sqliteTable('library_book_copies', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  bookId: text('book_id').notNull(),
  accessionNo: text('accession_no').notNull(),
  barcode: text('barcode'),
  rack: text('rack'),
  shelf: text('shelf'),
  conditionNote: text('condition_note', { enum: ['new', 'good', 'fair', 'poor'] }).notNull(),
  status: text('status', { enum: ['available', 'issued', 'reserved', 'lost', 'damaged', 'withdrawn'] }).notNull(),
  acquiredOn: text('acquired_on'),
  source: text('source', { enum: ['purchase', 'donation'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_library_book_copies_school_id_accession_no'.slice(0, 63)).on(t.schoolId, t.accessionNo),
]);

/** Student or staff member with limits. */
export const libraryMembers = sqliteTable('library_members', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  memberType: text('member_type', { enum: ['student', 'staff', 'guardian'] }).notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  cardNo: text('card_no').notNull(),
  maxBooks: integer('max_books').notNull(),
  loanDays: integer('loan_days').notNull(),
  finePerDay: numeric('fine_per_day', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['active', 'blocked', 'inactive'] }).notNull(),
  blockedReason: text('blocked_reason'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_library_members_school_id_card_no'.slice(0, 63)).on(t.schoolId, t.cardNo),
]);

/** Issue/return with fine accrual and reminder stage. */
export const libraryIssues = sqliteTable('library_issues', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  copyId: text('copy_id').notNull(),
  memberId: text('member_id').notNull(),
  issuedAt: text('issued_at').notNull(),
  dueAt: text('due_at').notNull(),
  returnedAt: text('returned_at'),
  renewCount: integer('renew_count').notNull(),
  issuedBy: text('issued_by'),
  returnedTo: text('returned_to'),
  fineAmount: numeric('fine_amount', { mode: 'number' }).notNull(),
  fineInvoiceItemId: text('fine_invoice_item_id'),
  fineWaivedBy: text('fine_waived_by'),
  reminderStage: text('reminder_stage'),
  status: text('status', { enum: ['issued', 'returned', 'lost', 'overdue'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_library_issues_school_id_due_at'.slice(0, 63)).on(t.schoolId, t.dueAt),
]);

/** Hold queue. */
export const libraryReservations = sqliteTable('library_reservations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  bookId: text('book_id').notNull(),
  memberId: text('member_id').notNull(),
  reservedAt: text('reserved_at').notNull(),
  notifiedAt: text('notified_at'),
  expiresAt: text('expires_at'),
  status: text('status', { enum: ['waiting', 'ready', 'fulfilled', 'expired', 'cancelled'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Reading programme: pages read, reviews (reading badges). */
export const readingLogs = sqliteTable('reading_logs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  bookId: text('book_id').notNull(),
  pagesRead: integer('pages_read').notNull(),
  finished: integer('finished', { mode: 'boolean' }).notNull(),
  review: text('review'),
  rating: integer('rating'),
  loggedAt: text('logged_at').notNull(),
});

// ============ Fees & billing ============
/** Tuition, admission, exam, transport, hostel, fine, canteen… */
export const feeHeads = sqliteTable('fee_heads', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  code: text('code').notNull(),
  headKind: text('head_kind', { enum: ['academic', 'transport', 'hostel', 'fine', 'misc', 'course', 'shop', 'canteen'] }).notNull(),
  glAccountId: text('gl_account_id'),
  isRefundable: integer('is_refundable', { mode: 'boolean' }).notNull(),
  taxPct: numeric('tax_pct', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_fee_heads_school_id_code'.slice(0, 63)).on(t.schoolId, t.code),
]);

/** Grace days, flat/percent/per-day, cap. */
export const lateFineRules = sqliteTable('late_fine_rules', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  graceDays: integer('grace_days').notNull(),
  fineType: text('fine_type', { enum: ['flat', 'percent', 'per_day', 'per_week'] }).notNull(),
  value: numeric('value', { mode: 'number' }).notNull(),
  maxAmount: numeric('max_amount', { mode: 'number' }),
  fineHeadId: text('fine_head_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_late_fine_rules_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Fee plan for a class (optionally campus/shift/programme) per year. */
export const feeStructures = sqliteTable('fee_structures', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  classId: text('class_id').notNull(),
  campusId: text('campus_id'),
  shiftId: text('shift_id'),
  programId: text('program_id'),
  name: text('name').notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_fee_structures_academic_year_id_class_id_campus_id_shift_id_program_id'.slice(0, 63)).on(t.academicYearId, t.classId, t.campusId, t.shiftId, t.programId),
]);

/** Head × amount × frequency × due day × months. */
export const feeStructureItems = sqliteTable('fee_structure_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  feeStructureId: text('fee_structure_id').notNull(),
  feeHeadId: text('fee_head_id').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  frequency: text('frequency', { enum: ['one_time', 'monthly', 'quarterly', 'half_yearly', 'yearly', 'per_term'] }).notNull(),
  dueDay: integer('due_day').notNull(),
  applicableMonths: text('applicable_months', { mode: 'json' }),
  lateFineRuleId: text('late_fine_rule_id'),
}, (t) => [
  uniqueIndex('uq_fee_structure_items_fee_structure_id_fee_head_id'.slice(0, 63)).on(t.feeStructureId, t.feeHeadId),
]);

/** Per-student amount or waiver for a head. */
export const studentFeeOverrides = sqliteTable('student_fee_overrides', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  feeHeadId: text('fee_head_id').notNull(),
  amount: numeric('amount', { mode: 'number' }),
  reason: text('reason'),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_student_fee_overrides_student_id_academic_year_id_fee_head_id'.slice(0, 63)).on(t.studentId, t.academicYearId, t.feeHeadId),
]);

/** Sibling / merit / staff child / need-based / early payment with auto rule. */
export const discountSchemes = sqliteTable('discount_schemes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  discountKind: text('discount_kind', { enum: ['sibling', 'merit', 'staff_child', 'need_based', 'early_payment', 'scholarship', 'custom'] }).notNull(),
  valueType: text('value_type', { enum: ['percent', 'flat'] }).notNull(),
  value: numeric('value', { mode: 'number' }).notNull(),
  appliesToHeads: text('applies_to_heads', { mode: 'json' }),
  autoRule: text('auto_rule', { mode: 'json' }),
  requiresApproval: integer('requires_approval', { mode: 'boolean' }).notNull(),
  budgetCap: numeric('budget_cap', { mode: 'number' }),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_discount_schemes_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Discount granted (or auto-proposed) to a student for a year. */
export const studentDiscounts = sqliteTable('student_discounts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  discountSchemeId: text('discount_scheme_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  valueOverride: numeric('value_override', { mode: 'number' }),
  validFrom: text('valid_from'),
  validTo: text('valid_to'),
  isAuto: integer('is_auto', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'expired'] }).notNull(),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_student_discounts_student_id_discount_scheme_id_academic_year_id'.slice(0, 63)).on(t.studentId, t.discountSchemeId, t.academicYearId),
]);

/** Split a large fee (admission, yearly) into instalments per student. */
export const instalmentPlans = sqliteTable('instalment_plans', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  feeHeadId: text('fee_head_id').notNull(),
  totalAmount: numeric('total_amount', { mode: 'number' }).notNull(),
  instalments: text('instalments', { mode: 'json' }),
  status: text('status', { enum: ['active', 'completed', 'cancelled'] }).notNull(),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** One generation run (e.g. October tuition for all classes). */
export const invoiceBatches = sqliteTable('invoice_batches', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  billingPeriod: text('billing_period').notNull(),
  scope: text('scope', { mode: 'json' }),
  generatedBy: text('generated_by'),
  invoiceCount: integer('invoice_count').notNull(),
  totalAmount: numeric('total_amount', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['pending', 'running', 'success', 'failed'] }).notNull(),
  startedAt: text('started_at'),
  finishedAt: text('finished_at'),
  error: text('error'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Student (or applicant) invoice with totals, status and reminder stamps. */
export const invoices = sqliteTable('invoices', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  invoiceNo: text('invoice_no').notNull(),
  studentId: text('student_id'),
  applicationId: text('application_id'),
  academicYearId: text('academic_year_id'),
  batchId: text('batch_id'),
  billingPeriod: text('billing_period'),
  issueDate: text('issue_date').notNull(),
  dueDate: text('due_date').notNull(),
  subtotal: numeric('subtotal', { mode: 'number' }).notNull(),
  discountTotal: numeric('discount_total', { mode: 'number' }).notNull(),
  fineTotal: numeric('fine_total', { mode: 'number' }).notNull(),
  taxTotal: numeric('tax_total', { mode: 'number' }).notNull(),
  total: numeric('total', { mode: 'number' }).notNull(),
  paidTotal: numeric('paid_total', { mode: 'number' }).notNull(),
  balance: numeric('balance', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['draft', 'issued', 'partially_paid', 'paid', 'overdue', 'cancelled', 'written_off'] }).notNull(),
  isAuto: integer('is_auto', { mode: 'boolean' }).notNull(),
  notes: text('notes'),
  pdfFileId: text('pdf_file_id'),
  lastReminderStage: text('last_reminder_stage'),
  lastReminderAt: text('last_reminder_at'),
  fineAppliedAt: text('fine_applied_at'),
  cancelledAt: text('cancelled_at'),
  cancelReason: text('cancel_reason'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_invoices_school_id_invoice_no'.slice(0, 63)).on(t.schoolId, t.invoiceNo),
  index('ix_invoices_student_id_status'.slice(0, 63)).on(t.studentId, t.status),
  index('ix_invoices_school_id_due_date_status'.slice(0, 63)).on(t.schoolId, t.dueDate, t.status),
]);

/** Lines: fee, fine, adjustment, previous due; source pointer (library issue, transport…). */
export const invoiceItems = sqliteTable('invoice_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  invoiceId: text('invoice_id').notNull(),
  feeHeadId: text('fee_head_id'),
  description: text('description').notNull(),
  quantity: numeric('quantity', { mode: 'number' }).notNull(),
  unitAmount: numeric('unit_amount', { mode: 'number' }).notNull(),
  discountAmount: numeric('discount_amount', { mode: 'number' }).notNull(),
  discountId: text('discount_id'),
  taxAmount: numeric('tax_amount', { mode: 'number' }).notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  itemKind: text('item_kind', { enum: ['fee', 'fine', 'adjustment', 'previous_due', 'course', 'shop'] }).notNull(),
  sourceType: text('source_type'),
  sourceId: text('source_id'),
});

/** Configured gateways with encrypted credentials and settlement account. */
export const paymentGateways = sqliteTable('payment_gateways', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  provider: text('provider', { enum: ['sslcommerz', 'bkash', 'nagad', 'rocket', 'upay', 'aamarpay', 'shurjopay', 'stripe', 'paypal', 'razorpay'] }).notNull(),
  displayName: text('display_name').notNull(),
  credentials: text('credentials', { mode: 'json' }),
  isSandbox: integer('is_sandbox', { mode: 'boolean' }).notNull(),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  settleToBankAccountId: text('settle_to_bank_account_id'),
  feePct: numeric('fee_pct', { mode: 'number' }).notNull(),
  feeFixed: numeric('fee_fixed', { mode: 'number' }).notNull(),
  sortOrder: integer('sort_order').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_payment_gateways_school_id_provider'.slice(0, 63)).on(t.schoolId, t.provider),
]);

/** Money received; allocated to invoices; journal auto-posted. */
export const payments = sqliteTable('payments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  paymentNo: text('payment_no').notNull(),
  studentId: text('student_id'),
  applicationId: text('application_id'),
  payerUserId: text('payer_user_id'),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  method: text('method', { enum: ['cash', 'bank_transfer', 'cheque', 'card', 'bkash', 'nagad', 'rocket', 'upay', 'sslcommerz', 'stripe', 'wallet', 'adjustment', 'other'] }).notNull(),
  gatewayId: text('gateway_id'),
  gatewayTxnId: text('gateway_txn_id'),
  gatewayPayload: text('gateway_payload', { mode: 'json' }),
  bankAccountId: text('bank_account_id'),
  reference: text('reference'),
  paidAt: text('paid_at').notNull(),
  receivedBy: text('received_by'),
  status: text('status', { enum: ['pending', 'success', 'failed', 'refunded', 'reversed'] }).notNull(),
  receiptFileId: text('receipt_file_id'),
  journalEntryId: text('journal_entry_id'),
  cashSessionId: text('cash_session_id'),
  notes: text('notes'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_payments_school_id_payment_no'.slice(0, 63)).on(t.schoolId, t.paymentNo),
  uniqueIndex('uq_payments_gateway_id_gateway_txn_id'.slice(0, 63)).on(t.gatewayId, t.gatewayTxnId),
  index('ix_payments_student_id_paid_at'.slice(0, 63)).on(t.studentId, t.paidAt),
]);

/** Payment → invoice amounts; unallocated remainder = advance credit. */
export const paymentAllocations = sqliteTable('payment_allocations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  paymentId: text('payment_id').notNull(),
  invoiceId: text('invoice_id').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
}, (t) => [
  uniqueIndex('uq_payment_allocations_payment_id_invoice_id'.slice(0, 63)).on(t.paymentId, t.invoiceId),
]);

/** Refund with approval, gateway refund id and reversal journal. */
export const refunds = sqliteTable('refunds', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  paymentId: text('payment_id').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  reason: text('reason').notNull(),
  requestedBy: text('requested_by'),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'refunded'] }).notNull(),
  approvedBy: text('approved_by'),
  refundedAt: text('refunded_at'),
  gatewayRefundId: text('gateway_refund_id'),
  journalEntryId: text('journal_entry_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Append-only running ledger per student. */
export const studentLedgerEntries = sqliteTable('student_ledger_entries', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  entryType: text('entry_type', { enum: ['invoice', 'payment', 'refund', 'adjustment', 'fine', 'write_off', 'advance'] }).notNull(),
  refType: text('ref_type').notNull(),
  refId: text('ref_id').notNull(),
  debit: numeric('debit', { mode: 'number' }).notNull(),
  credit: numeric('credit', { mode: 'number' }).notNull(),
  balanceAfter: numeric('balance_after', { mode: 'number' }).notNull(),
  description: text('description'),
  createdAt: text('created_at').notNull(),
}, (t) => [
  index('ix_student_ledger_entries_student_id_created_at'.slice(0, 63)).on(t.studentId, t.createdAt),
]);

/** Reminder sent per invoice per stage per channel. */
export const feeReminders = sqliteTable('fee_reminders', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  invoiceId: text('invoice_id').notNull(),
  stage: text('stage').notNull(),
  channel: text('channel', { enum: ['sms', 'push', 'email', 'whatsapp', 'call_task'] }).notNull(),
  notificationId: text('notification_id'),
  sentAt: text('sent_at').notNull(),
}, (t) => [
  uniqueIndex('uq_fee_reminders_invoice_id_stage_channel'.slice(0, 63)).on(t.invoiceId, t.stage, t.channel),
]);

/** Counter session with opening/closing count and variance. */
export const cashSessions = sqliteTable('cash_sessions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  cashierId: text('cashier_id').notNull(),
  openedAt: text('opened_at').notNull(),
  closedAt: text('closed_at'),
  openingCash: numeric('opening_cash', { mode: 'number' }).notNull(),
  expectedCash: numeric('expected_cash', { mode: 'number' }),
  countedCash: numeric('counted_cash', { mode: 'number' }),
  variance: numeric('variance', { mode: 'number' }),
  depositedTo: text('deposited_to'),
  note: text('note'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Rollup per day per method for dashboards. */
export const feeCollectionDaily = sqliteTable('fee_collection_daily', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  onDate: text('on_date').notNull(),
  method: text('method').notNull(),
  count: integer('count').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
}, (t) => [
  uniqueIndex('uq_fee_collection_daily_school_id_on_date_method'.slice(0, 63)).on(t.schoolId, t.onDate, t.method),
]);

// ============ Accounting · GL, AP/AR, assets, statements ============
/** Financial year (BD: July–June). */
export const fiscalYears = sqliteTable('fiscal_years', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  startDate: text('start_date').notNull(),
  endDate: text('end_date').notNull(),
  isClosed: integer('is_closed', { mode: 'boolean' }).notNull(),
  closedAt: text('closed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_fiscal_years_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Chart of accounts tree. */
export const glAccounts = sqliteTable('gl_accounts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  code: text('code').notNull(),
  name: text('name').notNull(),
  accountType: text('account_type', { enum: ['asset', 'liability', 'equity', 'income', 'expense'] }).notNull(),
  parentId: text('parent_id'),
  isGroup: integer('is_group', { mode: 'boolean' }).notNull(),
  isSystem: integer('is_system', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_gl_accounts_school_id_code'.slice(0, 63)).on(t.schoolId, t.code),
]);

/** Campus / department / project for reporting. */
export const costCenters = sqliteTable('cost_centers', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  campusId: text('campus_id'),
  departmentId: text('department_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_cost_centers_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Bank, MFS merchant and cash boxes. */
export const bankAccounts = sqliteTable('bank_accounts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  glAccountId: text('gl_account_id').notNull(),
  bankName: text('bank_name').notNull(),
  branch: text('branch'),
  accountName: text('account_name').notNull(),
  accountNo: text('account_no').notNull(),
  routingNo: text('routing_no'),
  accountKind: text('account_kind', { enum: ['bank', 'mfs', 'cash_box', 'card'] }).notNull(),
  isDefaultCollection: integer('is_default_collection', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Balanced entries; app refuses unbalanced. */
export const journalEntries = sqliteTable('journal_entries', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  fiscalYearId: text('fiscal_year_id'),
  entryNo: text('entry_no').notNull(),
  entryDate: text('entry_date').notNull(),
  memo: text('memo'),
  sourceType: text('source_type'),
  sourceId: text('source_id'),
  status: text('status', { enum: ['draft', 'posted', 'reversed'] }).notNull(),
  reversalOfId: text('reversal_of_id'),
  postedBy: text('posted_by'),
  isAuto: integer('is_auto', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_journal_entries_school_id_entry_no'.slice(0, 63)).on(t.schoolId, t.entryNo),
  index('ix_journal_entries_source_type_source_id'.slice(0, 63)).on(t.sourceType, t.sourceId),
  index('ix_journal_entries_school_id_entry_date'.slice(0, 63)).on(t.schoolId, t.entryDate),
]);

/** Debit/credit lines. */
export const journalLines = sqliteTable('journal_lines', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  entryId: text('entry_id').notNull(),
  accountId: text('account_id').notNull(),
  costCenterId: text('cost_center_id'),
  debit: numeric('debit', { mode: 'number' }).notNull(),
  credit: numeric('credit', { mode: 'number' }).notNull(),
  description: text('description'),
});

/** Expense categories with GL account and approval threshold. */
export const expenseCategories = sqliteTable('expense_categories', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  glAccountId: text('gl_account_id'),
  requiresApprovalAbove: numeric('requires_approval_above', { mode: 'number' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_expense_categories_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Suppliers/contractors. */
export const vendors = sqliteTable('vendors', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  phone: text('phone'),
  email: text('email'),
  address: text('address', { mode: 'json' }),
  taxId: text('tax_id'),
  bankDetails: text('bank_details', { mode: 'json' }),
  payableGlAccountId: text('payable_gl_account_id'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Expense claim/voucher with approval and payment. */
export const expenses = sqliteTable('expenses', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  expenseNo: text('expense_no').notNull(),
  categoryId: text('category_id').notNull(),
  vendorId: text('vendor_id'),
  costCenterId: text('cost_center_id'),
  expenseDate: text('expense_date').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  taxAmount: numeric('tax_amount', { mode: 'number' }).notNull(),
  paidFromId: text('paid_from_id'),
  paymentMethod: text('payment_method'),
  reference: text('reference'),
  description: text('description'),
  billFileId: text('bill_file_id'),
  requestedBy: text('requested_by'),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'paid'] }).notNull(),
  approvalRequestId: text('approval_request_id'),
  approvedBy: text('approved_by'),
  paidAt: text('paid_at'),
  journalEntryId: text('journal_entry_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_expenses_school_id_expense_no'.slice(0, 63)).on(t.schoolId, t.expenseNo),
]);

/** Accounts payable: bills from vendors (from POs) with due dates and payments. */
export const vendorBills = sqliteTable('vendor_bills', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  vendorId: text('vendor_id').notNull(),
  billNo: text('bill_no').notNull(),
  billDate: text('bill_date').notNull(),
  dueDate: text('due_date'),
  subtotal: numeric('subtotal', { mode: 'number' }).notNull(),
  tax: numeric('tax', { mode: 'number' }).notNull(),
  total: numeric('total', { mode: 'number' }).notNull(),
  paidTotal: numeric('paid_total', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['open', 'partially_paid', 'paid', 'void'] }).notNull(),
  poId: text('po_id'),
  fileId: text('file_id'),
  journalEntryId: text('journal_entry_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Payments to vendors. */
export const vendorPayments = sqliteTable('vendor_payments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  vendorId: text('vendor_id').notNull(),
  billId: text('bill_id'),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  paidFromId: text('paid_from_id'),
  method: text('method'),
  reference: text('reference'),
  paidAt: text('paid_at').notNull(),
  journalEntryId: text('journal_entry_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Non-fee income (rent, donations receipts, interest). */
export const otherIncomes = sqliteTable('other_incomes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  glAccountId: text('gl_account_id').notNull(),
  receivedInId: text('received_in_id'),
  incomeDate: text('income_date').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  payer: text('payer'),
  description: text('description'),
  journalEntryId: text('journal_entry_id'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Budget per account/cost centre with alert threshold. */
export const budgets = sqliteTable('budgets', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  fiscalYearId: text('fiscal_year_id').notNull(),
  glAccountId: text('gl_account_id').notNull(),
  costCenterId: text('cost_center_id'),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  alertAtPct: numeric('alert_at_pct', { mode: 'number' }).notNull(),
  alertedAt: text('alerted_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_budgets_fiscal_year_id_gl_account_id_cost_center_id'.slice(0, 63)).on(t.fiscalYearId, t.glAccountId, t.costCenterId),
]);

/** Imported statement lines with auto-match. */
export const bankStatementLines = sqliteTable('bank_statement_lines', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  bankAccountId: text('bank_account_id').notNull(),
  txnDate: text('txn_date').notNull(),
  description: text('description'),
  reference: text('reference'),
  debit: numeric('debit', { mode: 'number' }).notNull(),
  credit: numeric('credit', { mode: 'number' }).notNull(),
  balance: numeric('balance', { mode: 'number' }),
  matchedType: text('matched_type'),
  matchedId: text('matched_id'),
  matchedAt: text('matched_at'),
  importBatch: text('import_batch'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Depreciation schedule per asset (straight-line / reducing). */
export const fixedAssetsLedger = sqliteTable('fixed_assets_ledger', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  assetId: text('asset_id').notNull(),
  fiscalYearId: text('fiscal_year_id').notNull(),
  method: text('method', { enum: ['straight_line', 'reducing'] }).notNull(),
  ratePct: numeric('rate_pct', { mode: 'number' }).notNull(),
  openingValue: numeric('opening_value', { mode: 'number' }).notNull(),
  depreciation: numeric('depreciation', { mode: 'number' }).notNull(),
  closingValue: numeric('closing_value', { mode: 'number' }).notNull(),
  journalEntryId: text('journal_entry_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_fixed_assets_ledger_asset_id_fiscal_year_id'.slice(0, 63)).on(t.assetId, t.fiscalYearId),
]);

/** VAT/AIT rates. */
export const taxRates = sqliteTable('tax_rates', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  ratePct: numeric('rate_pct', { mode: 'number' }).notNull(),
  kind: text('kind', { enum: ['vat', 'ait', 'other'] }).notNull(),
  glAccountId: text('gl_account_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Generated statements (income statement, balance sheet, cash flow) per period. */
export const financialStatements = sqliteTable('financial_statements', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  fiscalYearId: text('fiscal_year_id').notNull(),
  kind: text('kind', { enum: ['income_statement', 'balance_sheet', 'cash_flow', 'trial_balance', 'receivables_ageing'] }).notNull(),
  periodStart: text('period_start').notNull(),
  periodEnd: text('period_end').notNull(),
  data: text('data', { mode: 'json' }),
  fileId: text('file_id'),
  generatedAt: text('generated_at').notNull(),
});

// ============ Student wallet & POS · canteen, shop ============
/** One wallet per student (or staff). */
export const wallets = sqliteTable('wallets', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  balance: numeric('balance', { mode: 'number' }).notNull(),
  dailyLimit: numeric('daily_limit', { mode: 'number' }),
  status: text('status', { enum: ['active', 'frozen', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Top-ups, spends, refunds; append-only. */
export const walletTransactions = sqliteTable('wallet_transactions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  walletId: text('wallet_id').notNull(),
  kind: text('kind', { enum: ['topup', 'spend', 'refund', 'adjustment', 'transfer'] }).notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  balanceAfter: numeric('balance_after', { mode: 'number' }).notNull(),
  sourceType: text('source_type'),
  sourceId: text('source_id'),
  paymentId: text('payment_id'),
  description: text('description'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
}, (t) => [
  index('ix_wallet_transactions_wallet_id_created_at'.slice(0, 63)).on(t.walletId, t.createdAt),
]);

/** Canteen, bookshop, uniform shop. */
export const posOutlets = sqliteTable('pos_outlets', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  kind: text('kind', { enum: ['canteen', 'bookshop', 'uniform', 'stationery', 'other'] }).notNull(),
  campusId: text('campus_id'),
  incomeGlAccountId: text('income_gl_account_id'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Sellable products with price and stock link. */
export const posProducts = sqliteTable('pos_products', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  outletId: text('outlet_id').notNull(),
  name: text('name').notNull(),
  sku: text('sku'),
  price: numeric('price', { mode: 'number' }).notNull(),
  taxRateId: text('tax_rate_id'),
  inventoryItemId: text('inventory_item_id'),
  imageFileId: text('image_file_id'),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  category: text('category'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** A sale (wallet, cash or gateway). */
export const posSales = sqliteTable('pos_sales', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  outletId: text('outlet_id').notNull(),
  saleNo: text('sale_no').notNull(),
  studentId: text('student_id'),
  walletId: text('wallet_id'),
  cashierId: text('cashier_id'),
  subtotal: numeric('subtotal', { mode: 'number' }).notNull(),
  tax: numeric('tax', { mode: 'number' }).notNull(),
  total: numeric('total', { mode: 'number' }).notNull(),
  paidBy: text('paid_by', { enum: ['wallet', 'cash', 'bkash', 'card', 'invoice'] }).notNull(),
  paymentId: text('payment_id'),
  status: text('status', { enum: ['completed', 'refunded', 'void'] }).notNull(),
  journalEntryId: text('journal_entry_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_pos_sales_school_id_sale_no'.slice(0, 63)).on(t.schoolId, t.saleNo),
]);

/** Lines. */
export const posSaleItems = sqliteTable('pos_sale_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  saleId: text('sale_id').notNull(),
  productId: text('product_id').notNull(),
  quantity: numeric('quantity', { mode: 'number' }).notNull(),
  unitPrice: numeric('unit_price', { mode: 'number' }).notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
});

/** Guardian orders (uniform, books) from the app with delivery/pickup. */
export const shopOrders = sqliteTable('shop_orders', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  orderNo: text('order_no').notNull(),
  studentId: text('student_id').notNull(),
  outletId: text('outlet_id').notNull(),
  items: text('items', { mode: 'json' }),
  total: numeric('total', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['placed', 'paid', 'ready', 'delivered', 'cancelled'] }).notNull(),
  invoiceId: text('invoice_id'),
  notes: text('notes'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_shop_orders_school_id_order_no'.slice(0, 63)).on(t.schoolId, t.orderNo),
]);

// ============ HR & payroll ============
/** Vacancies published on the website. */
export const jobPostings = sqliteTable('job_postings', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  departmentId: text('department_id'),
  designationId: text('designation_id'),
  description: text('description'),
  vacancies: integer('vacancies').notNull(),
  salaryRange: text('salary_range'),
  closesAt: text('closes_at'),
  status: text('status', { enum: ['draft', 'open', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Applicants with CV and interview pipeline. */
export const jobApplicants = sqliteTable('job_applicants', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  postingId: text('posting_id').notNull(),
  fullName: text('full_name').notNull(),
  phone: text('phone').notNull(),
  email: text('email'),
  cvFileId: text('cv_file_id'),
  score: numeric('score', { mode: 'number' }),
  stage: text('stage', { enum: ['applied', 'shortlisted', 'interview', 'offered', 'hired', 'rejected'] }).notNull(),
  interviewAt: text('interview_at'),
  notes: text('notes'),
  staffId: text('staff_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Per new staff: tasks like ID card, biometric enrol, bank details. */
export const onboardingChecklists = sqliteTable('onboarding_checklists', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  items: text('items', { mode: 'json' }),
  completedAt: text('completed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Contract periods with expiry alerts. */
export const staffContracts = sqliteTable('staff_contracts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  contractType: text('contract_type', { enum: ['permanent', 'contract', 'part_time', 'intern', 'volunteer', 'mpo'] }).notNull(),
  startDate: text('start_date').notNull(),
  endDate: text('end_date'),
  fileId: text('file_id'),
  notes: text('notes'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Staff work shifts / rosters. */
export const workShifts = sqliteTable('work_shifts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  startTime: text('start_time').notNull(),
  endTime: text('end_time').notNull(),
  days: text('days', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Who is on which shift when. */
export const staffShiftAssignments = sqliteTable('staff_shift_assignments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  shiftId: text('shift_id').notNull(),
  fromDate: text('from_date').notNull(),
  toDate: text('to_date'),
});

/** Earning/deduction/employer components with calculation type and formula. */
export const salaryComponents = sqliteTable('salary_components', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  code: text('code').notNull(),
  componentType: text('component_type', { enum: ['earning', 'deduction', 'employer_contribution'] }).notNull(),
  calcType: text('calc_type', { enum: ['fixed', 'percent_of_basic', 'percent_of_gross', 'formula', 'attendance_based', 'slab'] }).notNull(),
  defaultValue: numeric('default_value', { mode: 'number' }),
  formula: text('formula'),
  isTaxable: integer('is_taxable', { mode: 'boolean' }).notNull(),
  isStatutory: integer('is_statutory', { mode: 'boolean' }).notNull(),
  glAccountId: text('gl_account_id'),
  sortOrder: integer('sort_order').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_salary_components_school_id_code'.slice(0, 63)).on(t.schoolId, t.code),
]);

/** Per staff with effective range (no overlap, app-enforced). */
export const salaryStructures = sqliteTable('salary_structures', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  effectiveFrom: text('effective_from').notNull(),
  effectiveTo: text('effective_to'),
  basic: numeric('basic', { mode: 'number' }).notNull(),
  payFrequency: text('pay_frequency', { enum: ['monthly', 'weekly'] }).notNull(),
  mpoPortion: numeric('mpo_portion', { mode: 'number' }).notNull(),
  bankAccount: text('bank_account', { mode: 'json' }),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_salary_structures_staff_id_effective_from'.slice(0, 63)).on(t.staffId, t.effectiveFrom),
]);

/** Component values. */
export const salaryStructureItems = sqliteTable('salary_structure_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  structureId: text('structure_id').notNull(),
  componentId: text('component_id').notNull(),
  value: numeric('value', { mode: 'number' }).notNull(),
}, (t) => [
  uniqueIndex('uq_salary_structure_items_structure_id_component_id'.slice(0, 63)).on(t.structureId, t.componentId),
]);

/** Income tax slabs (NBR) per fiscal year and taxpayer category. */
export const taxSlabs = sqliteTable('tax_slabs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  fiscalYearId: text('fiscal_year_id').notNull(),
  category: text('category', { enum: ['general', 'female_senior', 'disabled'] }).notNull(),
  slabs: text('slabs', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Loans/advances with monthly deduction. */
export const staffLoans = sqliteTable('staff_loans', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  loanType: text('loan_type', { enum: ['advance', 'loan', 'pf_loan'] }).notNull(),
  principal: numeric('principal', { mode: 'number' }).notNull(),
  monthlyDeduction: numeric('monthly_deduction', { mode: 'number' }).notNull(),
  balance: numeric('balance', { mode: 'number' }).notNull(),
  startsFrom: text('starts_from').notNull(),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'active', 'closed'] }).notNull(),
  approvedBy: text('approved_by'),
  journalEntryId: text('journal_entry_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** PF balance per staff (employee + employer + interest). */
export const providentFundAccounts = sqliteTable('provident_fund_accounts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  employeeTotal: numeric('employee_total', { mode: 'number' }).notNull(),
  employerTotal: numeric('employer_total', { mode: 'number' }).notNull(),
  interestTotal: numeric('interest_total', { mode: 'number' }).notNull(),
  withdrawnTotal: numeric('withdrawn_total', { mode: 'number' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Monthly run with totals and approval. */
export const payrollRuns = sqliteTable('payroll_runs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  periodMonth: text('period_month').notNull(),
  campusId: text('campus_id'),
  status: text('status', { enum: ['draft', 'calculated', 'approved', 'paid', 'locked'] }).notNull(),
  staffCount: integer('staff_count').notNull(),
  totalGross: numeric('total_gross', { mode: 'number' }).notNull(),
  totalDeductions: numeric('total_deductions', { mode: 'number' }).notNull(),
  totalNet: numeric('total_net', { mode: 'number' }).notNull(),
  totalMpo: numeric('total_mpo', { mode: 'number' }).notNull(),
  calculatedAt: text('calculated_at'),
  approvedBy: text('approved_by'),
  approvedAt: text('approved_at'),
  paidFromId: text('paid_from_id'),
  paidAt: text('paid_at'),
  bankFileId: text('bank_file_id'),
  journalEntryId: text('journal_entry_id'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_payroll_runs_school_id_period_month_campus_id'.slice(0, 63)).on(t.schoolId, t.periodMonth, t.campusId),
]);

/** Frozen payslip per staff per run. */
export const payslips = sqliteTable('payslips', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  payrollRunId: text('payroll_run_id').notNull(),
  staffId: text('staff_id').notNull(),
  structureId: text('structure_id'),
  workingDays: numeric('working_days', { mode: 'number' }).notNull(),
  presentDays: numeric('present_days', { mode: 'number' }).notNull(),
  paidLeaveDays: numeric('paid_leave_days', { mode: 'number' }).notNull(),
  lopDays: numeric('lop_days', { mode: 'number' }).notNull(),
  lateCount: integer('late_count').notNull(),
  overtimeHours: numeric('overtime_hours', { mode: 'number' }).notNull(),
  gross: numeric('gross', { mode: 'number' }).notNull(),
  totalDeductions: numeric('total_deductions', { mode: 'number' }).notNull(),
  tax: numeric('tax', { mode: 'number' }).notNull(),
  netPay: numeric('net_pay', { mode: 'number' }).notNull(),
  breakdown: text('breakdown', { mode: 'json' }),
  payslipFileId: text('payslip_file_id'),
  paidAt: text('paid_at'),
  paymentRef: text('payment_ref'),
  status: text('status', { enum: ['draft', 'approved', 'paid', 'held'] }).notNull(),
  holdReason: text('hold_reason'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_payslips_payroll_run_id_staff_id'.slice(0, 63)).on(t.payrollRunId, t.staffId),
]);

/** Cycle with weighted criteria. */
export const appraisalCycles = sqliteTable('appraisal_cycles', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  name: text('name').notNull(),
  criteria: text('criteria', { mode: 'json' }),
  opensAt: text('opens_at').notNull(),
  closesAt: text('closes_at').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Self + reviewer scores, auto metrics. */
export const staffAppraisals = sqliteTable('staff_appraisals', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  cycleId: text('cycle_id').notNull(),
  staffId: text('staff_id').notNull(),
  reviewerId: text('reviewer_id').notNull(),
  selfScores: text('self_scores', { mode: 'json' }),
  reviewerScores: text('reviewer_scores', { mode: 'json' }),
  autoMetrics: text('auto_metrics', { mode: 'json' }),
  overallScore: numeric('overall_score', { mode: 'number' }),
  comments: text('comments'),
  status: text('status', { enum: ['pending', 'self_done', 'reviewed', 'finalised'] }).notNull(),
  finalisedAt: text('finalised_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_staff_appraisals_cycle_id_staff_id'.slice(0, 63)).on(t.cycleId, t.staffId),
]);

/** Training records and certificates. */
export const staffTrainings = sqliteTable('staff_trainings', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  title: text('title').notNull(),
  provider: text('provider'),
  startDate: text('start_date'),
  endDate: text('end_date'),
  hours: numeric('hours', { mode: 'number' }),
  certificateFileId: text('certificate_file_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Resignation/termination with clearance and final settlement. */
export const staffExits = sqliteTable('staff_exits', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  staffId: text('staff_id').notNull(),
  exitType: text('exit_type', { enum: ['resignation', 'termination', 'retirement', 'end_of_contract', 'death'] }).notNull(),
  noticeDate: text('notice_date'),
  lastWorkingDay: text('last_working_day').notNull(),
  clearance: text('clearance', { mode: 'json' }),
  settlement: text('settlement', { mode: 'json' }),
  settlementJournalId: text('settlement_journal_id'),
  status: text('status', { enum: ['initiated', 'cleared', 'settled'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Scholarships, donations & fundraising ============
/** A fund with balance and rules. */
export const scholarshipFunds = sqliteTable('scholarship_funds', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  kind: text('kind', { enum: ['internal', 'government_stipend', 'donor', 'zakat', 'alumni'] }).notNull(),
  balance: numeric('balance', { mode: 'number' }).notNull(),
  glAccountId: text('gl_account_id'),
  rules: text('rules', { mode: 'json' }),
  status: text('status', { enum: ['active', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Award to a student for a period; links to a discount. */
export const scholarshipAwards = sqliteTable('scholarship_awards', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  fundId: text('fund_id').notNull(),
  studentId: text('student_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  frequency: text('frequency', { enum: ['monthly', 'yearly', 'one_time'] }).notNull(),
  discountId: text('discount_id'),
  sponsorDonorId: text('sponsor_donor_id'),
  status: text('status', { enum: ['proposed', 'approved', 'active', 'ended'] }).notNull(),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Individuals/organisations who give. */
export const donors = sqliteTable('donors', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  kind: text('kind', { enum: ['individual', 'organisation', 'alumni'] }).notNull(),
  phone: text('phone'),
  email: text('email'),
  address: text('address', { mode: 'json' }),
  alumniId: text('alumni_id'),
  totalDonated: numeric('total_donated', { mode: 'number' }).notNull(),
  isAnonymous: integer('is_anonymous', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Campaign with goal and public page. */
export const fundraisingCampaigns = sqliteTable('fundraising_campaigns', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  slug: text('slug').notNull(),
  goalAmount: numeric('goal_amount', { mode: 'number' }).notNull(),
  raisedAmount: numeric('raised_amount', { mode: 'number' }).notNull(),
  startsAt: text('starts_at'),
  endsAt: text('ends_at'),
  description: text('description'),
  coverFileId: text('cover_file_id'),
  status: text('status', { enum: ['draft', 'live', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_fundraising_campaigns_school_id_slug'.slice(0, 63)).on(t.schoolId, t.slug),
]);

/** Donation/pledge with receipt and journal. */
export const donations = sqliteTable('donations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  donorId: text('donor_id').notNull(),
  campaignId: text('campaign_id'),
  fundId: text('fund_id'),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  kind: text('kind', { enum: ['pledge', 'received'] }).notNull(),
  method: text('method'),
  reference: text('reference'),
  receivedAt: text('received_at'),
  receiptDocId: text('receipt_doc_id'),
  journalEntryId: text('journal_entry_id'),
  message: text('message'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Transport & GPS ============
/** Fleet with compliance dates. */
export const vehicles = sqliteTable('vehicles', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campusId: text('campus_id'),
  registrationNo: text('registration_no').notNull(),
  vehicleType: text('vehicle_type', { enum: ['bus', 'microbus', 'van', 'car'] }).notNull(),
  makeModel: text('make_model'),
  capacity: integer('capacity').notNull(),
  driverId: text('driver_id'),
  helperId: text('helper_id'),
  gpsDeviceId: text('gps_device_id'),
  insuranceExpiry: text('insurance_expiry'),
  fitnessExpiry: text('fitness_expiry'),
  taxTokenExpiry: text('tax_token_expiry'),
  routePermitExpiry: text('route_permit_expiry'),
  odometerKm: integer('odometer_km'),
  status: text('status', { enum: ['active', 'maintenance', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_vehicles_school_id_registration_no'.slice(0, 63)).on(t.schoolId, t.registrationNo),
]);

/** Route with default fee. */
export const transportRoutes = sqliteTable('transport_routes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  vehicleId: text('vehicle_id'),
  startPoint: text('start_point'),
  endPoint: text('end_point'),
  distanceKm: numeric('distance_km', { mode: 'number' }),
  monthlyFee: numeric('monthly_fee', { mode: 'number' }).notNull(),
  feeHeadId: text('fee_head_id'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_transport_routes_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Ordered stops with geofence and times. */
export const routeStops = sqliteTable('route_stops', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  routeId: text('route_id').notNull(),
  name: text('name').notNull(),
  sequence: integer('sequence').notNull(),
  latitude: numeric('latitude', { mode: 'number' }),
  longitude: numeric('longitude', { mode: 'number' }),
  geofenceM: integer('geofence_m').notNull(),
  pickupTime: text('pickup_time'),
  dropTime: text('drop_time'),
  feeOverride: numeric('fee_override', { mode: 'number' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_route_stops_route_id_sequence'.slice(0, 63)).on(t.routeId, t.sequence),
]);

/** Student assignment to route/stop with fee snapshot. */
export const studentTransport = sqliteTable('student_transport', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  routeId: text('route_id').notNull(),
  stopId: text('stop_id').notNull(),
  tripType: text('trip_type', { enum: ['pickup', 'drop', 'both'] }).notNull(),
  startDate: text('start_date').notNull(),
  endDate: text('end_date'),
  monthlyFee: numeric('monthly_fee', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_student_transport_student_id_academic_year_id'.slice(0, 63)).on(t.studentId, t.academicYearId),
]);

/** Daily trip instance with status and delay stamp. */
export const vehicleTrips = sqliteTable('vehicle_trips', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  vehicleId: text('vehicle_id').notNull(),
  routeId: text('route_id').notNull(),
  tripDate: text('trip_date').notNull(),
  tripType: text('trip_type', { enum: ['pickup', 'drop'] }).notNull(),
  driverId: text('driver_id'),
  helperId: text('helper_id'),
  scheduledStart: text('scheduled_start'),
  startedAt: text('started_at'),
  endedAt: text('ended_at'),
  status: text('status', { enum: ['scheduled', 'running', 'completed', 'cancelled', 'delayed'] }).notNull(),
  delayAlertSentAt: text('delay_alert_sent_at'),
  checklist: text('checklist', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_vehicle_trips_vehicle_id_trip_date_trip_type'.slice(0, 63)).on(t.vehicleId, t.tripDate, t.tripType),
]);

/** Telemetry (prune/rotate monthly). */
export const vehicleGpsLogs = sqliteTable('vehicle_gps_logs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  vehicleId: text('vehicle_id').notNull(),
  tripId: text('trip_id'),
  latitude: numeric('latitude', { mode: 'number' }).notNull(),
  longitude: numeric('longitude', { mode: 'number' }).notNull(),
  speedKmh: numeric('speed_kmh', { mode: 'number' }),
  heading: integer('heading'),
  recordedAt: text('recorded_at').notNull(),
}, (t) => [
  index('ix_vehicle_gps_logs_vehicle_id_recorded_at'.slice(0, 63)).on(t.vehicleId, t.recordedAt),
]);

/** Boarded/alighted per student per trip. */
export const transportBoardings = sqliteTable('transport_boardings', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  tripId: text('trip_id').notNull(),
  studentId: text('student_id').notNull(),
  stopId: text('stop_id'),
  boardedAt: text('boarded_at'),
  alightedAt: text('alighted_at'),
  source: text('source', { enum: ['rfid', 'helper_app', 'manual'] }).notNull(),
  guardianNotifiedAt: text('guardian_notified_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_transport_boardings_trip_id_student_id'.slice(0, 63)).on(t.tripId, t.studentId),
]);

/** Approaching/arrived/departed alerts sent per trip-stop. */
export const stopAlerts = sqliteTable('stop_alerts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  tripId: text('trip_id').notNull(),
  stopId: text('stop_id').notNull(),
  alertType: text('alert_type', { enum: ['approaching', 'arrived', 'departed'] }).notNull(),
  sentAt: text('sent_at').notNull(),
}, (t) => [
  uniqueIndex('uq_stop_alerts_trip_id_stop_id_alert_type'.slice(0, 63)).on(t.tripId, t.stopId, t.alertType),
]);

/** Service history with next due. */
export const vehicleMaintenance = sqliteTable('vehicle_maintenance', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  vehicleId: text('vehicle_id').notNull(),
  serviceType: text('service_type').notNull(),
  serviceDate: text('service_date').notNull(),
  odometerKm: integer('odometer_km'),
  cost: numeric('cost', { mode: 'number' }),
  vendorId: text('vendor_id'),
  expenseId: text('expense_id'),
  nextDueDate: text('next_due_date'),
  nextDueKm: integer('next_due_km'),
  notes: text('notes'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Fuel fills. */
export const fuelLogs = sqliteTable('fuel_logs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  vehicleId: text('vehicle_id').notNull(),
  filledAt: text('filled_at').notNull(),
  litres: numeric('litres', { mode: 'number' }).notNull(),
  cost: numeric('cost', { mode: 'number' }).notNull(),
  odometerKm: integer('odometer_km'),
  expenseId: text('expense_id'),
});

/** Accidents, over-speed, complaints per driver. */
export const driverIncidents = sqliteTable('driver_incidents', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  vehicleId: text('vehicle_id').notNull(),
  driverId: text('driver_id'),
  kind: text('kind', { enum: ['overspeed', 'accident', 'complaint', 'breakdown', 'other'] }).notNull(),
  occurredAt: text('occurred_at').notNull(),
  details: text('details'),
  severity: text('severity', { enum: ['low', 'medium', 'high'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Hostel ============
/** Building with warden, curfew and fee head. */
export const hostels = sqliteTable('hostels', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campusId: text('campus_id'),
  name: text('name').notNull(),
  hostelType: text('hostel_type', { enum: ['boys', 'girls', 'staff'] }).notNull(),
  wardenId: text('warden_id'),
  address: text('address', { mode: 'json' }),
  curfewTime: text('curfew_time'),
  feeHeadId: text('fee_head_id'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_hostels_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Rooms with capacity and fee. */
export const hostelRooms = sqliteTable('hostel_rooms', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  hostelId: text('hostel_id').notNull(),
  roomNo: text('room_no').notNull(),
  floor: text('floor'),
  roomType: text('room_type', { enum: ['single', 'double', 'shared', 'dorm'] }).notNull(),
  capacity: integer('capacity').notNull(),
  monthlyFee: numeric('monthly_fee', { mode: 'number' }).notNull(),
  amenities: text('amenities', { mode: 'json' }),
  status: text('status', { enum: ['active', 'maintenance', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_hostel_rooms_hostel_id_room_no'.slice(0, 63)).on(t.hostelId, t.roomNo),
]);

/** Individual beds. */
export const hostelBeds = sqliteTable('hostel_beds', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  roomId: text('room_id').notNull(),
  bedNo: text('bed_no').notNull(),
  status: text('status', { enum: ['vacant', 'occupied', 'maintenance'] }).notNull(),
}, (t) => [
  uniqueIndex('uq_hostel_beds_room_id_bed_no'.slice(0, 63)).on(t.roomId, t.bedNo),
]);

/** Student on a bed for a period (no overlap, app-enforced). */
export const hostelAllocations = sqliteTable('hostel_allocations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  bedId: text('bed_id').notNull(),
  academicYearId: text('academic_year_id').notNull(),
  fromDate: text('from_date').notNull(),
  toDate: text('to_date'),
  monthlyFee: numeric('monthly_fee', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['active', 'ended'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_hostel_allocations_bed_id_from_date'.slice(0, 63)).on(t.bedId, t.fromDate),
]);

/** Leave from hostel with guardian consent, warden approval, QR, late-return alert. */
export const hostelOutpasses = sqliteTable('hostel_outpasses', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  hostelId: text('hostel_id').notNull(),
  leaveFrom: text('leave_from').notNull(),
  expectedReturn: text('expected_return').notNull(),
  actualOutAt: text('actual_out_at'),
  actualReturnAt: text('actual_return_at'),
  reason: text('reason').notNull(),
  destination: text('destination'),
  guardianConsentAt: text('guardian_consent_at'),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'out', 'returned', 'late'] }).notNull(),
  approvedBy: text('approved_by'),
  qrCode: text('qr_code'),
  lateAlertSentAt: text('late_alert_sent_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Visitor log per resident. */
export const hostelVisitors = sqliteTable('hostel_visitors', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  hostelId: text('hostel_id').notNull(),
  studentId: text('student_id').notNull(),
  visitorName: text('visitor_name').notNull(),
  relation: text('relation'),
  phone: text('phone'),
  idProof: text('id_proof'),
  inAt: text('in_at').notNull(),
  outAt: text('out_at'),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Morning/night roll call. */
export const hostelAttendance = sqliteTable('hostel_attendance', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  hostelId: text('hostel_id').notNull(),
  studentId: text('student_id').notNull(),
  onDate: text('on_date').notNull(),
  rollCall: text('roll_call', { enum: ['morning', 'night'] }).notNull(),
  status: text('status', { enum: ['present', 'absent', 'on_outpass', 'sick'] }).notNull(),
  markedBy: text('marked_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_hostel_attendance_student_id_on_date_roll_call'.slice(0, 63)).on(t.studentId, t.onDate, t.rollCall),
]);

/** Weekly menu. */
export const messMenus = sqliteTable('mess_menus', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  hostelId: text('hostel_id').notNull(),
  dayOfWeek: integer('day_of_week').notNull(),
  meal: text('meal', { enum: ['breakfast', 'lunch', 'snack', 'dinner'] }).notNull(),
  items: text('items').notNull(),
}, (t) => [
  uniqueIndex('uq_mess_menus_hostel_id_day_of_week_meal'.slice(0, 63)).on(t.hostelId, t.dayOfWeek, t.meal),
]);

/** Per-meal attendance/billing (optional per-meal mess billing). */
export const mealRecords = sqliteTable('meal_records', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  hostelId: text('hostel_id').notNull(),
  studentId: text('student_id').notNull(),
  onDate: text('on_date').notNull(),
  meal: text('meal', { enum: ['breakfast', 'lunch', 'snack', 'dinner'] }).notNull(),
  taken: integer('taken', { mode: 'boolean' }).notNull(),
  cost: numeric('cost', { mode: 'number' }),
}, (t) => [
  uniqueIndex('uq_meal_records_student_id_on_date_meal'.slice(0, 63)).on(t.studentId, t.onDate, t.meal),
]);

/** Laundry drop/return tracking. */
export const laundryRecords = sqliteTable('laundry_records', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  hostelId: text('hostel_id').notNull(),
  studentId: text('student_id').notNull(),
  droppedAt: text('dropped_at').notNull(),
  items: integer('items').notNull(),
  returnedAt: text('returned_at'),
  charge: numeric('charge', { mode: 'number' }),
});

/** Resident complaints. */
export const hostelComplaints = sqliteTable('hostel_complaints', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  hostelId: text('hostel_id').notNull(),
  studentId: text('student_id'),
  category: text('category', { enum: ['maintenance', 'food', 'safety', 'cleanliness', 'other'] }).notNull(),
  description: text('description').notNull(),
  status: text('status', { enum: ['open', 'in_progress', 'resolved'] }).notNull(),
  resolvedAt: text('resolved_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Procurement, inventory & assets ============
/** Categories; asset categories create fixed assets on receipt. */
export const inventoryCategories = sqliteTable('inventory_categories', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  isAsset: integer('is_asset', { mode: 'boolean' }).notNull(),
  glAccountId: text('gl_account_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_inventory_categories_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Stores/warehouses with keeper. */
export const stores = sqliteTable('stores', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  campusId: text('campus_id'),
  keeperId: text('keeper_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_stores_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** Item master with reorder rule. */
export const inventoryItems = sqliteTable('inventory_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  categoryId: text('category_id').notNull(),
  sku: text('sku').notNull(),
  name: text('name').notNull(),
  unit: text('unit').notNull(),
  reorderLevel: numeric('reorder_level', { mode: 'number' }).notNull(),
  reorderQty: numeric('reorder_qty', { mode: 'number' }),
  preferredVendorId: text('preferred_vendor_id'),
  lastCost: numeric('last_cost', { mode: 'number' }),
  barcode: text('barcode'),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_inventory_items_school_id_sku'.slice(0, 63)).on(t.schoolId, t.sku),
]);

/** Current quantity per item per store (maintained by app on each movement). */
export const stockLevels = sqliteTable('stock_levels', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  itemId: text('item_id').notNull(),
  storeId: text('store_id').notNull(),
  quantity: numeric('quantity', { mode: 'number' }).notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_stock_levels_item_id_store_id'.slice(0, 63)).on(t.itemId, t.storeId),
]);

/** Purchase requests from departments. */
export const requisitions = sqliteTable('requisitions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  requestedBy: text('requested_by').notNull(),
  departmentId: text('department_id'),
  items: text('items', { mode: 'json' }),
  justification: text('justification'),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'ordered'] }).notNull(),
  approvalRequestId: text('approval_request_id'),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Vendor quotations / tender bids for a requisition. */
export const quotations = sqliteTable('quotations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  requisitionId: text('requisition_id'),
  vendorId: text('vendor_id').notNull(),
  quotedAt: text('quoted_at').notNull(),
  validUntil: text('valid_until'),
  items: text('items', { mode: 'json' }),
  total: numeric('total', { mode: 'number' }).notNull(),
  fileId: text('file_id'),
  isSelected: integer('is_selected', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** PO with approval, receipt and bill link. */
export const purchaseOrders = sqliteTable('purchase_orders', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  poNo: text('po_no').notNull(),
  vendorId: text('vendor_id').notNull(),
  storeId: text('store_id').notNull(),
  requisitionId: text('requisition_id'),
  orderDate: text('order_date').notNull(),
  expectedDate: text('expected_date'),
  subtotal: numeric('subtotal', { mode: 'number' }).notNull(),
  taxTotal: numeric('tax_total', { mode: 'number' }).notNull(),
  total: numeric('total', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['draft', 'pending_approval', 'approved', 'ordered', 'partially_received', 'received', 'cancelled'] }).notNull(),
  isAuto: integer('is_auto', { mode: 'boolean' }).notNull(),
  approvalRequestId: text('approval_request_id'),
  approvedBy: text('approved_by'),
  billId: text('bill_id'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_purchase_orders_school_id_po_no'.slice(0, 63)).on(t.schoolId, t.poNo),
]);

/** PO lines with received qty. */
export const purchaseOrderItems = sqliteTable('purchase_order_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  poId: text('po_id').notNull(),
  itemId: text('item_id').notNull(),
  quantity: numeric('quantity', { mode: 'number' }).notNull(),
  receivedQty: numeric('received_qty', { mode: 'number' }).notNull(),
  unitCost: numeric('unit_cost', { mode: 'number' }).notNull(),
}, (t) => [
  uniqueIndex('uq_purchase_order_items_po_id_item_id'.slice(0, 63)).on(t.poId, t.itemId),
]);

/** GRN against a PO. */
export const goodsReceipts = sqliteTable('goods_receipts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  poId: text('po_id').notNull(),
  receivedAt: text('received_at').notNull(),
  receivedBy: text('received_by'),
  items: text('items', { mode: 'json' }),
  notes: text('notes'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Append-only ledger: in/out/adjust/transfer/return. */
export const stockMovements = sqliteTable('stock_movements', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  itemId: text('item_id').notNull(),
  storeId: text('store_id').notNull(),
  moveType: text('move_type', { enum: ['in', 'out', 'adjust', 'transfer', 'return', 'consume'] }).notNull(),
  quantity: numeric('quantity', { mode: 'number' }).notNull(),
  unitCost: numeric('unit_cost', { mode: 'number' }),
  refType: text('ref_type'),
  refId: text('ref_id'),
  issuedToStaffId: text('issued_to_staff_id'),
  issuedToRoomId: text('issued_to_room_id'),
  note: text('note'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
}, (t) => [
  index('ix_stock_movements_item_id_created_at'.slice(0, 63)).on(t.itemId, t.createdAt),
]);

/** Consumable requests → stock-out on approval. */
export const issueRequests = sqliteTable('issue_requests', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  requestedBy: text('requested_by').notNull(),
  storeId: text('store_id').notNull(),
  items: text('items', { mode: 'json' }),
  purpose: text('purpose'),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'issued'] }).notNull(),
  approvedBy: text('approved_by'),
  issuedAt: text('issued_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Fixed asset with tag, custodian, warranty and value. */
export const assets = sqliteTable('assets', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  itemId: text('item_id'),
  assetTag: text('asset_tag').notNull(),
  name: text('name').notNull(),
  serialNo: text('serial_no'),
  purchaseDate: text('purchase_date'),
  purchaseCost: numeric('purchase_cost', { mode: 'number' }),
  vendorId: text('vendor_id'),
  poId: text('po_id'),
  warrantyUntil: text('warranty_until'),
  depreciationPct: numeric('depreciation_pct', { mode: 'number' }),
  currentValue: numeric('current_value', { mode: 'number' }),
  locationRoomId: text('location_room_id'),
  custodianStaffId: text('custodian_staff_id'),
  conditionNote: text('condition_note', { enum: ['new', 'good', 'fair', 'repair', 'damaged'] }).notNull(),
  status: text('status', { enum: ['in_use', 'in_store', 'repair', 'disposed', 'lost'] }).notNull(),
  qrFileId: text('qr_file_id'),
  disposedAt: text('disposed_at'),
  disposalNote: text('disposal_note'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_assets_school_id_asset_tag'.slice(0, 63)).on(t.schoolId, t.assetTag),
]);

/** Service history with next due. */
export const assetMaintenance = sqliteTable('asset_maintenance', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  assetId: text('asset_id').notNull(),
  serviceDate: text('service_date').notNull(),
  serviceType: text('service_type', { enum: ['preventive', 'repair', 'calibration', 'inspection'] }).notNull(),
  cost: numeric('cost', { mode: 'number' }),
  vendorId: text('vendor_id'),
  expenseId: text('expense_id'),
  nextDueDate: text('next_due_date'),
  notes: text('notes'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Periodic physical verification runs. */
export const assetAudits = sqliteTable('asset_audits', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  startedAt: text('started_at').notNull(),
  finishedAt: text('finished_at'),
  results: text('results', { mode: 'json' }),
  status: text('status', { enum: ['running', 'completed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Facilities & maintenance ============
/** Book a room/hall/ground for an event or class (no overlap, app-enforced). */
export const roomBookings = sqliteTable('room_bookings', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  roomId: text('room_id').notNull(),
  bookedBy: text('booked_by').notNull(),
  purpose: text('purpose').notNull(),
  startsAt: text('starts_at').notNull(),
  endsAt: text('ends_at').notNull(),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'cancelled'] }).notNull(),
  approvedBy: text('approved_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_room_bookings_room_id_starts_at'.slice(0, 63)).on(t.roomId, t.startsAt),
]);

/** Repair/maintenance requests with SLA. */
export const workOrders = sqliteTable('work_orders', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  description: text('description'),
  locationRoomId: text('location_room_id'),
  assetId: text('asset_id'),
  category: text('category', { enum: ['electrical', 'plumbing', 'civil', 'it', 'furniture', 'cleaning', 'other'] }).notNull(),
  priority: text('priority', { enum: ['low', 'normal', 'high', 'urgent'] }).notNull(),
  reportedBy: text('reported_by'),
  assignedTo: text('assigned_to'),
  vendorId: text('vendor_id'),
  status: text('status', { enum: ['open', 'assigned', 'in_progress', 'done', 'cancelled'] }).notNull(),
  dueAt: text('due_at'),
  cost: numeric('cost', { mode: 'number' }),
  expenseId: text('expense_id'),
  completedAt: text('completed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Recurring cleaning tasks with checklist. */
export const cleaningSchedules = sqliteTable('cleaning_schedules', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  area: text('area').notNull(),
  frequency: text('frequency', { enum: ['daily', 'weekly', 'monthly'] }).notNull(),
  assignedTo: text('assigned_to'),
  checklist: text('checklist', { mode: 'json' }),
  lastDoneAt: text('last_done_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Electricity/water/gas meter readings and bills. */
export const utilityReadings = sqliteTable('utility_readings', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  utility: text('utility', { enum: ['electricity', 'water', 'gas', 'internet', 'generator_fuel'] }).notNull(),
  campusId: text('campus_id'),
  readAt: text('read_at').notNull(),
  reading: numeric('reading', { mode: 'number' }).notNull(),
  cost: numeric('cost', { mode: 'number' }),
  expenseId: text('expense_id'),
});

/** Fire/earthquake drills and inspections. */
export const safetyDrills = sqliteTable('safety_drills', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  kind: text('kind', { enum: ['fire', 'earthquake', 'evacuation', 'first_aid', 'inspection'] }).notNull(),
  heldOn: text('held_on').notNull(),
  participants: integer('participants'),
  findings: text('findings'),
  fileId: text('file_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Front office, helpdesk & gate ============
/** Check-in/out with purpose, host and badge. */
export const visitorLogs = sqliteTable('visitor_logs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  campusId: text('campus_id'),
  visitorName: text('visitor_name').notNull(),
  phone: text('phone'),
  idProof: text('id_proof'),
  purpose: text('purpose', { enum: ['admission', 'meeting', 'delivery', 'pickup', 'interview', 'vendor', 'other'] }).notNull(),
  toMeetStaffId: text('to_meet_staff_id'),
  studentId: text('student_id'),
  badgeNo: text('badge_no'),
  photoFileId: text('photo_file_id'),
  inAt: text('in_at').notNull(),
  outAt: text('out_at'),
  hostNotifiedAt: text('host_notified_at'),
  loggedBy: text('logged_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Student early leave / staff out pass with QR. */
export const gatePasses = sqliteTable('gate_passes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  personType: text('person_type', { enum: ['student', 'staff'] }).notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  reason: text('reason').notNull(),
  outAt: text('out_at').notNull(),
  expectedIn: text('expected_in'),
  actualIn: text('actual_in'),
  pickedBy: text('picked_by'),
  authorisationId: text('authorisation_id'),
  approvedBy: text('approved_by'),
  qrCode: text('qr_code'),
  status: text('status', { enum: ['pending', 'approved', 'out', 'returned', 'rejected'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Inbound/outbound calls with follow-up. */
export const callLogs = sqliteTable('call_logs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  direction: text('direction', { enum: ['inbound', 'outbound'] }).notNull(),
  callerName: text('caller_name'),
  phone: text('phone').notNull(),
  purpose: text('purpose'),
  notes: text('notes'),
  relatedType: text('related_type'),
  relatedId: text('related_id'),
  followUpAt: text('follow_up_at'),
  loggedBy: text('logged_by'),
  calledAt: text('called_at').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Dispatch/receive register. */
export const postalRecords = sqliteTable('postal_records', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  direction: text('direction', { enum: ['dispatch', 'receive'] }).notNull(),
  referenceNo: text('reference_no'),
  fromParty: text('from_party'),
  toParty: text('to_party'),
  subject: text('subject'),
  recordDate: text('record_date').notNull(),
  fileId: text('file_id'),
  loggedBy: text('logged_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Helpdesk tickets with category, priority, SLA, escalation and satisfaction. */
export const complaints = sqliteTable('complaints', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  ticketNo: text('ticket_no').notNull(),
  complainantUserId: text('complainant_user_id'),
  complainantName: text('complainant_name'),
  complainantPhone: text('complainant_phone'),
  studentId: text('student_id'),
  category: text('category', { enum: ['academic', 'fees', 'transport', 'hostel', 'staff_behaviour', 'facility', 'safety', 'it', 'other'] }).notNull(),
  subject: text('subject').notNull(),
  description: text('description').notNull(),
  priority: text('priority', { enum: ['low', 'normal', 'high', 'urgent'] }).notNull(),
  assignedTo: text('assigned_to'),
  slaDueAt: text('sla_due_at'),
  escalatedAt: text('escalated_at'),
  status: text('status', { enum: ['open', 'in_progress', 'resolved', 'closed', 'reopened'] }).notNull(),
  resolution: text('resolution'),
  resolvedAt: text('resolved_at'),
  satisfaction: integer('satisfaction'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_complaints_school_id_ticket_no'.slice(0, 63)).on(t.schoolId, t.ticketNo),
]);

/** Thread of updates. */
export const complaintUpdates = sqliteTable('complaint_updates', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  complaintId: text('complaint_id').notNull(),
  byUserId: text('by_user_id'),
  note: text('note').notNull(),
  isInternal: integer('is_internal', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
});

/** Lost & found register. */
export const lostFoundItems = sqliteTable('lost_found_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  description: text('description').notNull(),
  foundAt: text('found_at'),
  location: text('location'),
  photoFileId: text('photo_file_id'),
  claimedBy: text('claimed_by'),
  claimedAt: text('claimed_at'),
  status: text('status', { enum: ['found', 'claimed', 'disposed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Communication · SMS, push, email, WhatsApp, chat ============
/** SMS/email/push/WhatsApp/voice providers. */
export const messagingProviders = sqliteTable('messaging_providers', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  channel: text('channel', { enum: ['sms', 'email', 'push', 'whatsapp', 'voice'] }).notNull(),
  provider: text('provider').notNull(),
  credentials: text('credentials', { mode: 'json' }),
  senderId: text('sender_id'),
  isDefault: integer('is_default', { mode: 'boolean' }).notNull(),
  balance: numeric('balance', { mode: 'number' }),
  lowBalanceThreshold: numeric('low_balance_threshold', { mode: 'number' }),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  costPerUnit: numeric('cost_per_unit', { mode: 'number' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Per event × channel × locale with placeholders. */
export const notificationTemplates = sqliteTable('notification_templates', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  eventKey: text('event_key').notNull(),
  channel: text('channel', { enum: ['sms', 'email', 'push', 'whatsapp', 'in_app', 'voice'] }).notNull(),
  locale: text('locale').notNull(),
  subject: text('subject'),
  body: text('body').notNull(),
  variables: text('variables', { mode: 'json' }),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_notification_templates_school_id_event_key_channel_locale'.slice(0, 63)).on(t.schoolId, t.eventKey, t.channel, t.locale),
]);

/** Every message sent with status, provider id, cost, read time. */
export const notifications = sqliteTable('notifications', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  recipientUserId: text('recipient_user_id'),
  recipientAddress: text('recipient_address'),
  channel: text('channel', { enum: ['sms', 'email', 'push', 'whatsapp', 'in_app', 'voice'] }).notNull(),
  eventKey: text('event_key'),
  templateId: text('template_id'),
  title: text('title'),
  body: text('body').notNull(),
  data: text('data', { mode: 'json' }),
  entityType: text('entity_type'),
  entityId: text('entity_id'),
  status: text('status', { enum: ['queued', 'sent', 'delivered', 'failed', 'read'] }).notNull(),
  providerId: text('provider_id'),
  providerMsgId: text('provider_msg_id'),
  cost: numeric('cost', { mode: 'number' }),
  attempts: integer('attempts').notNull(),
  error: text('error'),
  scheduledFor: text('scheduled_for').notNull(),
  sentAt: text('sent_at'),
  deliveredAt: text('delivered_at'),
  readAt: text('read_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_notifications_status_scheduled_for'.slice(0, 63)).on(t.status, t.scheduledFor),
  index('ix_notifications_recipient_user_id_created_at'.slice(0, 63)).on(t.recipientUserId, t.createdAt),
  index('ix_notifications_entity_type_entity_id'.slice(0, 63)).on(t.entityType, t.entityId),
]);

/** Per user per event per channel. */
export const notificationPreferences = sqliteTable('notification_preferences', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userId: text('user_id').notNull(),
  eventKey: text('event_key').notNull(),
  channel: text('channel', { enum: ['sms', 'email', 'push', 'whatsapp', 'in_app', 'voice'] }).notNull(),
  enabled: integer('enabled', { mode: 'boolean' }).notNull(),
}, (t) => [
  uniqueIndex('uq_notification_preferences_user_id_event_key_channel'.slice(0, 63)).on(t.userId, t.eventKey, t.channel),
]);

/** Notices with audience, schedule, pin, attachments. */
export const notices = sqliteTable('notices', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  body: text('body').notNull(),
  noticeType: text('notice_type', { enum: ['general', 'academic', 'exam', 'fee', 'holiday', 'urgent', 'event'] }).notNull(),
  audience: text('audience', { mode: 'json' }),
  attachments: text('attachments', { mode: 'json' }),
  publishAt: text('publish_at').notNull(),
  expiresAt: text('expires_at'),
  isPinned: integer('is_pinned', { mode: 'boolean' }).notNull(),
  sendPush: integer('send_push', { mode: 'boolean' }).notNull(),
  sendSms: integer('send_sms', { mode: 'boolean' }).notNull(),
  sendEmail: integer('send_email', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['draft', 'scheduled', 'published', 'archived'] }).notNull(),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
  deletedAt: text('deleted_at'),
}, (t) => [
  index('ix_notices_school_id_publish_at'.slice(0, 63)).on(t.schoolId, t.publishAt),
]);

/** Read receipts. */
export const noticeReads = sqliteTable('notice_reads', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  noticeId: text('notice_id').notNull(),
  userId: text('user_id').notNull(),
  readAt: text('read_at').notNull(),
}, (t) => [
  uniqueIndex('uq_notice_reads_notice_id_user_id'.slice(0, 63)).on(t.noticeId, t.userId),
]);

/** Direct chats, groups, section channels, broadcast lists. */
export const conversations = sqliteTable('conversations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  kind: text('kind', { enum: ['direct', 'group', 'section_channel', 'broadcast', 'support'] }).notNull(),
  subject: text('subject'),
  sectionId: text('section_id'),
  createdBy: text('created_by'),
  lastMessageAt: text('last_message_at'),
  isLocked: integer('is_locked', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Members with role, mute and read pointer. */
export const conversationParticipants = sqliteTable('conversation_participants', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  conversationId: text('conversation_id').notNull(),
  userId: text('user_id').notNull(),
  role: text('role', { enum: ['member', 'admin'] }).notNull(),
  muted: integer('muted', { mode: 'boolean' }).notNull(),
  lastReadAt: text('last_read_at'),
}, (t) => [
  uniqueIndex('uq_conversation_participants_conversation_id_user_id'.slice(0, 63)).on(t.conversationId, t.userId),
]);

/** Chat messages with attachments; moderated. */
export const messages = sqliteTable('messages', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  conversationId: text('conversation_id').notNull(),
  senderId: text('sender_id'),
  body: text('body'),
  attachments: text('attachments', { mode: 'json' }),
  replyToId: text('reply_to_id'),
  sentAt: text('sent_at').notNull(),
  editedAt: text('edited_at'),
  deletedAt: text('deleted_at'),
  flagged: integer('flagged', { mode: 'boolean' }).notNull(),
}, (t) => [
  index('ix_messages_conversation_id_sent_at'.slice(0, 63)).on(t.conversationId, t.sentAt),
]);

/** Parent–teacher meeting slots. */
export const ptmSlots = sqliteTable('ptm_slots', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  eventId: text('event_id'),
  teacherId: text('teacher_id').notNull(),
  startsAt: text('starts_at').notNull(),
  endsAt: text('ends_at').notNull(),
  capacity: integer('capacity').notNull(),
  roomId: text('room_id'),
  mode: text('mode', { enum: ['in_person', 'online'] }).notNull(),
  joinUrl: text('join_url'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_ptm_slots_teacher_id_starts_at'.slice(0, 63)).on(t.teacherId, t.startsAt),
]);

/** Bookings with reminders and outcome notes. */
export const ptmBookings = sqliteTable('ptm_bookings', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  slotId: text('slot_id').notNull(),
  studentId: text('student_id').notNull(),
  guardianId: text('guardian_id').notNull(),
  status: text('status', { enum: ['booked', 'attended', 'no_show', 'cancelled'] }).notNull(),
  notes: text('notes'),
  reminderSentAt: text('reminder_sent_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_ptm_bookings_slot_id_student_id'.slice(0, 63)).on(t.slotId, t.studentId),
]);

/** Surveys/polls to guardians, students, staff (uses form builder). */
export const surveys = sqliteTable('surveys', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  formId: text('form_id').notNull(),
  title: text('title').notNull(),
  audience: text('audience', { mode: 'json' }),
  isAnonymous: integer('is_anonymous', { mode: 'boolean' }).notNull(),
  opensAt: text('opens_at'),
  closesAt: text('closes_at'),
  status: text('status', { enum: ['draft', 'open', 'closed'] }).notNull(),
  results: text('results', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Email/WhatsApp newsletters and campaigns. */
export const newsletters = sqliteTable('newsletters', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  body: text('body'),
  audience: text('audience', { mode: 'json' }),
  channel: text('channel', { enum: ['email', 'whatsapp', 'sms'] }).notNull(),
  scheduledFor: text('scheduled_for'),
  sentAt: text('sent_at'),
  stats: text('stats', { mode: 'json' }),
  status: text('status', { enum: ['draft', 'scheduled', 'sent'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** IVR / recorded voice calls (emergency, fee reminder). */
export const voiceBroadcasts = sqliteTable('voice_broadcasts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  audioFileId: text('audio_file_id'),
  ttsText: text('tts_text'),
  audience: text('audience', { mode: 'json' }),
  scheduledFor: text('scheduled_for'),
  stats: text('stats', { mode: 'json' }),
  status: text('status', { enum: ['draft', 'scheduled', 'running', 'done'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Welfare · behaviour, health, counselling, safeguarding ============
/** Positive/negative categories with default points and severity. */
export const behaviourCategories = sqliteTable('behaviour_categories', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  polarity: text('polarity', { enum: ['positive', 'negative'] }).notNull(),
  defaultPoints: integer('default_points').notNull(),
  severity: text('severity', { enum: ['low', 'medium', 'high', 'critical'] }).notNull(),
  notifyGuardian: integer('notify_guardian', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_behaviour_categories_school_id_name'.slice(0, 63)).on(t.schoolId, t.name),
]);

/** An incident with points and reporter. */
export const behaviourIncidents = sqliteTable('behaviour_incidents', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  categoryId: text('category_id').notNull(),
  incidentDate: text('incident_date').notNull(),
  points: integer('points').notNull(),
  description: text('description').notNull(),
  reportedBy: text('reported_by').notNull(),
  witnesses: text('witnesses'),
  attachments: text('attachments', { mode: 'json' }),
  guardianNotifiedAt: text('guardian_notified_at'),
  status: text('status', { enum: ['open', 'under_review', 'actioned', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_behaviour_incidents_student_id_incident_date'.slice(0, 63)).on(t.studentId, t.incidentDate),
]);

/** Warning, detention, suspension… with approval and guardian acknowledgement. */
export const disciplinaryActions = sqliteTable('disciplinary_actions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  incidentId: text('incident_id'),
  studentId: text('student_id').notNull(),
  actionType: text('action_type', { enum: ['verbal_warning', 'written_warning', 'detention', 'suspension', 'expulsion', 'counselling', 'community_service'] }).notNull(),
  fromDate: text('from_date'),
  toDate: text('to_date'),
  description: text('description'),
  isAutoProposed: integer('is_auto_proposed', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['pending', 'approved', 'rejected', 'completed'] }).notNull(),
  approvedBy: text('approved_by'),
  guardianAcknowledgedAt: text('guardian_acknowledged_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Threshold rules → proposed action. */
export const behaviourRules = sqliteTable('behaviour_rules', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  windowDays: integer('window_days').notNull(),
  thresholdPoints: integer('threshold_points').notNull(),
  actionType: text('action_type').notNull(),
  notifyRoles: text('notify_roles', { mode: 'json' }),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Growth and screening records. */
export const healthRecords = sqliteTable('health_records', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  recordedOn: text('recorded_on').notNull(),
  heightCm: numeric('height_cm', { mode: 'number' }),
  weightKg: numeric('weight_kg', { mode: 'number' }),
  bmi: numeric('bmi', { mode: 'number' }),
  visionLeft: text('vision_left'),
  visionRight: text('vision_right'),
  hearing: text('hearing'),
  dental: text('dental'),
  bloodPressure: text('blood_pressure'),
  allergies: text('allergies', { mode: 'json' }),
  chronicConditions: text('chronic_conditions', { mode: 'json' }),
  medications: text('medications'),
  doctorNotes: text('doctor_notes'),
  recordedBy: text('recorded_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Doses and next due. */
export const vaccinations = sqliteTable('vaccinations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  vaccine: text('vaccine').notNull(),
  doseNo: integer('dose_no').notNull(),
  givenOn: text('given_on'),
  nextDueOn: text('next_due_on'),
  certificateFileId: text('certificate_file_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_vaccinations_student_id_vaccine_dose_no'.slice(0, 63)).on(t.studentId, t.vaccine, t.doseNo),
]);

/** School clinic visit with treatment, medicines (stock-out) and send-home flag. */
export const clinicVisits = sqliteTable('clinic_visits', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  personType: text('person_type', { enum: ['student', 'staff'] }).notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  visitedAt: text('visited_at').notNull(),
  complaint: text('complaint').notNull(),
  treatment: text('treatment'),
  medicinesGiven: text('medicines_given', { mode: 'json' }),
  referredTo: text('referred_to'),
  sentHome: integer('sent_home', { mode: 'boolean' }).notNull(),
  guardianNotifiedAt: text('guardian_notified_at'),
  attendedBy: text('attended_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Sessions with encrypted notes (counsellor-only). */
export const counsellingSessions = sqliteTable('counselling_sessions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  counsellorId: text('counsellor_id').notNull(),
  sessionAt: text('session_at').notNull(),
  referralSource: text('referral_source', { enum: ['self', 'teacher', 'behaviour_rule', 'result_drop', 'guardian', 'clinic'] }).notNull(),
  notesEncrypted: text('notes_encrypted'),
  followUpAt: text('follow_up_at'),
  status: text('status', { enum: ['scheduled', 'done', 'no_show', 'cancelled'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Student/staff insurance and claims. */
export const insurancePolicies = sqliteTable('insurance_policies', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  personType: text('person_type', { enum: ['student', 'staff'] }).notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  provider: text('provider').notNull(),
  policyNo: text('policy_no').notNull(),
  coverage: numeric('coverage', { mode: 'number' }),
  validFrom: text('valid_from'),
  validTo: text('valid_to'),
  claims: text('claims', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Confidential child-protection cases with restricted access. */
export const safeguardingCases = sqliteTable('safeguarding_cases', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  reportedBy: text('reported_by'),
  category: text('category', { enum: ['abuse', 'neglect', 'bullying', 'online_safety', 'self_harm', 'other'] }).notNull(),
  detailsEncrypted: text('details_encrypted'),
  riskLevel: text('risk_level', { enum: ['low', 'medium', 'high'] }).notNull(),
  status: text('status', { enum: ['open', 'monitoring', 'closed'] }).notNull(),
  caseOwnerId: text('case_owner_id'),
  closedAt: text('closed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** IEP / accommodation plans. */
export const specialNeedsPlans = sqliteTable('special_needs_plans', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  diagnosis: text('diagnosis'),
  accommodations: text('accommodations', { mode: 'json' }),
  goals: text('goals', { mode: 'json' }),
  reviewDate: text('review_date'),
  coordinatorId: text('coordinator_id'),
  fileId: text('file_id'),
  status: text('status', { enum: ['active', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Documents, certificates & ID cards ============
/** Versioned templates per document type. */
export const documentTemplates = sqliteTable('document_templates', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  docType: text('doc_type', { enum: ['tc', 'testimonial', 'character', 'bonafide', 'id_card', 'admit_card', 'report_card', 'payslip', 'receipt', 'donation_receipt', 'invoice', 'offer_letter', 'appointment_letter', 'experience_letter', 'certificate', 'marksheet', 'custom'] }).notNull(),
  name: text('name').notNull(),
  htmlTemplate: text('html_template').notNull(),
  css: text('css'),
  pageSize: text('page_size').notNull(),
  orientation: text('orientation', { enum: ['portrait', 'landscape'] }).notNull(),
  variables: text('variables', { mode: 'json' }),
  isDefault: integer('is_default', { mode: 'boolean' }).notNull(),
  version: integer('version').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_document_templates_school_id_doc_type_name'.slice(0, 63)).on(t.schoolId, t.docType, t.name),
]);

/** Request with eligibility snapshot and fee. */
export const documentRequests = sqliteTable('document_requests', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  docType: text('doc_type').notNull(),
  personType: text('person_type', { enum: ['student', 'staff', 'alumni'] }).notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  requestedBy: text('requested_by'),
  reason: text('reason'),
  feeInvoiceId: text('fee_invoice_id'),
  eligibility: text('eligibility', { mode: 'json' }),
  status: text('status', { enum: ['requested', 'blocked', 'approved', 'issued', 'rejected'] }).notNull(),
  approvalRequestId: text('approval_request_id'),
  decidedBy: text('decided_by'),
  decidedAt: text('decided_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Issued document with frozen data, file and verification code. */
export const issuedDocuments = sqliteTable('issued_documents', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  templateId: text('template_id'),
  requestId: text('request_id'),
  docType: text('doc_type').notNull(),
  documentNo: text('document_no').notNull(),
  personType: text('person_type', { enum: ['student', 'staff', 'alumni', 'other'] }).notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  dataSnapshot: text('data_snapshot', { mode: 'json' }),
  fileId: text('file_id'),
  verificationCode: text('verification_code').notNull(),
  signedBy: text('signed_by'),
  signatureHash: text('signature_hash'),
  issuedAt: text('issued_at').notNull(),
  validUntil: text('valid_until'),
  revokedAt: text('revoked_at'),
  revokeReason: text('revoke_reason'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_issued_documents_school_id_document_no'.slice(0, 63)).on(t.schoolId, t.documentNo),
  index('ix_issued_documents_student_id_doc_type'.slice(0, 63)).on(t.studentId, t.docType),
]);

/** Cards with validity, RFID and print status. */
export const idCards = sqliteTable('id_cards', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  personType: text('person_type', { enum: ['student', 'staff', 'guardian', 'visitor'] }).notNull(),
  studentId: text('student_id'),
  staffId: text('staff_id'),
  guardianId: text('guardian_id'),
  cardNo: text('card_no').notNull(),
  templateId: text('template_id'),
  validFrom: text('valid_from').notNull(),
  validTo: text('valid_to').notNull(),
  rfidTag: text('rfid_tag'),
  fileId: text('file_id'),
  status: text('status', { enum: ['pending_print', 'active', 'lost', 'expired', 'cancelled'] }).notNull(),
  printedAt: text('printed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_id_cards_school_id_card_no'.slice(0, 63)).on(t.schoolId, t.cardNo),
]);

/** Bulk print batches (ID cards, admit cards, report cards). */
export const printJobs = sqliteTable('print_jobs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  kind: text('kind').notNull(),
  items: text('items', { mode: 'json' }),
  fileId: text('file_id'),
  status: text('status', { enum: ['queued', 'rendering', 'ready', 'printed'] }).notNull(),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Public verification hits (who checked what). */
export const documentVerifications = sqliteTable('document_verifications', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  documentId: text('document_id').notNull(),
  verifiedAt: text('verified_at').notNull(),
  ip: text('ip'),
  userAgent: text('user_agent'),
});

// ============ Alumni & career ============
/** Alumni profile. */
export const alumni = sqliteTable('alumni', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id'),
  userId: text('user_id'),
  fullName: text('full_name').notNull(),
  graduationYear: integer('graduation_year').notNull(),
  lastClassId: text('last_class_id'),
  phone: text('phone'),
  email: text('email'),
  currentOrganisation: text('current_organisation'),
  currentPosition: text('current_position'),
  city: text('city'),
  country: text('country'),
  linkedinUrl: text('linkedin_url'),
  bio: text('bio'),
  photoFileId: text('photo_file_id'),
  isPublic: integer('is_public', { mode: 'boolean' }).notNull(),
  isMentor: integer('is_mentor', { mode: 'boolean' }).notNull(),
  status: text('status', { enum: ['active', 'inactive'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  index('ix_alumni_school_id_graduation_year'.slice(0, 63)).on(t.schoolId, t.graduationYear),
]);

/** Batch groups with reps. */
export const alumniBatches = sqliteTable('alumni_batches', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  graduationYear: integer('graduation_year').notNull(),
  name: text('name'),
  repAlumniId: text('rep_alumni_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_alumni_batches_school_id_graduation_year'.slice(0, 63)).on(t.schoolId, t.graduationYear),
]);

/** Alumni mentor ↔ student. */
export const mentorshipPairs = sqliteTable('mentorship_pairs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  mentorId: text('mentor_id').notNull(),
  studentId: text('student_id').notNull(),
  topic: text('topic'),
  startedOn: text('started_on').notNull(),
  endedOn: text('ended_on'),
  status: text('status', { enum: ['active', 'ended'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Jobs/internships shared by alumni or partners. */
export const jobBoardPosts = sqliteTable('job_board_posts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  postedByAlumniId: text('posted_by_alumni_id'),
  title: text('title').notNull(),
  company: text('company'),
  location: text('location'),
  description: text('description'),
  applyUrl: text('apply_url'),
  expiresAt: text('expires_at'),
  status: text('status', { enum: ['open', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

// ============ Events & ticketing ============
/** Event with venue, audience, RSVP and ticketing. */
export const events = sqliteTable('events', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  calendarEventId: text('calendar_event_id'),
  title: text('title').notNull(),
  description: text('description'),
  eventType: text('event_type', { enum: ['sports', 'cultural', 'ptm', 'seminar', 'trip', 'ceremony', 'competition', 'workshop', 'other'] }).notNull(),
  startsAt: text('starts_at').notNull(),
  endsAt: text('ends_at'),
  venue: text('venue'),
  roomId: text('room_id'),
  audience: text('audience', { mode: 'json' }),
  rsvpRequired: integer('rsvp_required', { mode: 'boolean' }).notNull(),
  ticketPrice: numeric('ticket_price', { mode: 'number' }),
  ticketLimit: integer('ticket_limit'),
  feeHeadId: text('fee_head_id'),
  bannerFileId: text('banner_file_id'),
  organiserId: text('organiser_id'),
  status: text('status', { enum: ['draft', 'scheduled', 'live', 'completed', 'cancelled'] }).notNull(),
  feedbackFormId: text('feedback_form_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Responses. */
export const eventRsvps = sqliteTable('event_rsvps', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  eventId: text('event_id').notNull(),
  userId: text('user_id').notNull(),
  studentId: text('student_id'),
  response: text('response', { enum: ['yes', 'no', 'maybe'] }).notNull(),
  guests: integer('guests').notNull(),
  respondedAt: text('responded_at').notNull(),
}, (t) => [
  uniqueIndex('uq_event_rsvps_event_id_user_id'.slice(0, 63)).on(t.eventId, t.userId),
]);

/** Paid/free tickets with QR and check-in. */
export const eventTickets = sqliteTable('event_tickets', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  eventId: text('event_id').notNull(),
  holderUserId: text('holder_user_id'),
  holderName: text('holder_name'),
  quantity: integer('quantity').notNull(),
  amount: numeric('amount', { mode: 'number' }).notNull(),
  paymentId: text('payment_id'),
  qrCode: text('qr_code').notNull(),
  checkedInAt: text('checked_in_at'),
  status: text('status', { enum: ['reserved', 'paid', 'cancelled', 'used'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Programme items inside an event. */
export const eventScheduleItems = sqliteTable('event_schedule_items', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  eventId: text('event_id').notNull(),
  startsAt: text('starts_at').notNull(),
  title: text('title').notNull(),
  presenter: text('presenter'),
  sequence: integer('sequence').notNull(),
});

/** Volunteer sign-ups. */
export const eventVolunteers = sqliteTable('event_volunteers', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  eventId: text('event_id').notNull(),
  userId: text('user_id').notNull(),
  role: text('role'),
  status: text('status', { enum: ['applied', 'confirmed', 'declined'] }).notNull(),
}, (t) => [
  uniqueIndex('uq_event_volunteers_event_id_user_id'.slice(0, 63)).on(t.eventId, t.userId),
]);

// ============ Governance · committee, meetings, policies ============
/** Managing committee, academic council, PTA, student council. */
export const committees = sqliteTable('committees', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  kind: text('kind', { enum: ['managing', 'academic', 'pta', 'student_council', 'disciplinary', 'other'] }).notNull(),
  status: text('status', { enum: ['active', 'dissolved'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Members with role and term. */
export const committeeMembers = sqliteTable('committee_members', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  committeeId: text('committee_id').notNull(),
  personName: text('person_name').notNull(),
  userId: text('user_id'),
  role: text('role').notNull(),
  termStart: text('term_start'),
  termEnd: text('term_end'),
  status: text('status', { enum: ['active', 'ended'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Meeting with agenda, minutes and attendance. */
export const meetings = sqliteTable('meetings', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  committeeId: text('committee_id'),
  title: text('title').notNull(),
  heldAt: text('held_at').notNull(),
  venue: text('venue'),
  agenda: text('agenda', { mode: 'json' }),
  minutes: text('minutes'),
  attendees: text('attendees', { mode: 'json' }),
  minutesFileId: text('minutes_file_id'),
  status: text('status', { enum: ['scheduled', 'held', 'cancelled'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Decisions with follow-up tasks. */
export const resolutions = sqliteTable('resolutions', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  meetingId: text('meeting_id').notNull(),
  number: text('number'),
  text: text('text').notNull(),
  ownerId: text('owner_id'),
  dueDate: text('due_date'),
  status: text('status', { enum: ['open', 'done', 'dropped'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** School policies with versions and acknowledgement tracking. */
export const policyDocuments = sqliteTable('policy_documents', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  category: text('category'),
  version: integer('version').notNull(),
  fileId: text('file_id'),
  body: text('body'),
  appliesTo: text('applies_to', { mode: 'json' }),
  effectiveFrom: text('effective_from'),
  status: text('status', { enum: ['draft', 'active', 'retired'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Who acknowledged which policy. */
export const policyAcknowledgements = sqliteTable('policy_acknowledgements', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  policyId: text('policy_id').notNull(),
  userId: text('user_id').notNull(),
  ackedAt: text('acked_at').notNull(),
}, (t) => [
  uniqueIndex('uq_policy_acknowledgements_policy_id_user_id'.slice(0, 63)).on(t.policyId, t.userId),
]);

/** Student council / class captain elections with online voting. */
export const elections = sqliteTable('elections', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  title: text('title').notNull(),
  scope: text('scope', { mode: 'json' }),
  opensAt: text('opens_at').notNull(),
  closesAt: text('closes_at').notNull(),
  candidates: text('candidates', { mode: 'json' }),
  results: text('results', { mode: 'json' }),
  status: text('status', { enum: ['draft', 'open', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Anonymous votes (voter hashed). */
export const electionVotes = sqliteTable('election_votes', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  electionId: text('election_id').notNull(),
  voterHash: text('voter_hash').notNull(),
  candidateId: text('candidate_id').notNull(),
  castAt: text('cast_at').notNull(),
}, (t) => [
  uniqueIndex('uq_election_votes_election_id_voter_hash'.slice(0, 63)).on(t.electionId, t.voterHash),
]);

// ============ Compliance & government reporting ============
/** Generated regulatory reports with period and file. */
export const govtReports = sqliteTable('govt_reports', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  reportType: text('report_type', { enum: ['banbeis_census', 'board_registration', 'mpo_salary_sheet', 'stipend_list', 'annual_return', 'custom'] }).notNull(),
  period: text('period').notNull(),
  data: text('data', { mode: 'json' }),
  fileId: text('file_id'),
  submittedAt: text('submitted_at'),
  status: text('status', { enum: ['draft', 'generated', 'submitted', 'accepted'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Govt stipend schemes (PESP, secondary stipend) and enrolled students. */
export const stipendPrograms = sqliteTable('stipend_programs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  authority: text('authority'),
  criteria: text('criteria', { mode: 'json' }),
  amount: numeric('amount', { mode: 'number' }),
  frequency: text('frequency', { enum: ['monthly', 'quarterly', 'half_yearly', 'yearly'] }),
  status: text('status', { enum: ['active', 'closed'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Students in a stipend programme with disbursement history. */
export const stipendEnrollments = sqliteTable('stipend_enrollments', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  programId: text('program_id').notNull(),
  studentId: text('student_id').notNull(),
  enrolledOn: text('enrolled_on').notNull(),
  bankOrMfs: text('bank_or_mfs', { mode: 'json' }),
  disbursements: text('disbursements', { mode: 'json' }),
  status: text('status', { enum: ['active', 'suspended', 'ended'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_stipend_enrollments_program_id_student_id'.slice(0, 63)).on(t.programId, t.studentId),
]);

/** Guardian/staff consents (photo use, data processing, trips). */
export const consentRecords = sqliteTable('consent_records', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userId: text('user_id').notNull(),
  studentId: text('student_id'),
  consentType: text('consent_type').notNull(),
  granted: integer('granted', { mode: 'boolean' }).notNull(),
  grantedAt: text('granted_at').notNull(),
  expiresAt: text('expires_at'),
  evidence: text('evidence', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Data export / deletion requests (privacy). */
export const dataRequests = sqliteTable('data_requests', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userId: text('user_id').notNull(),
  kind: text('kind', { enum: ['export', 'delete', 'correct'] }).notNull(),
  status: text('status', { enum: ['requested', 'processing', 'done', 'rejected'] }).notNull(),
  fileId: text('file_id'),
  processedBy: text('processed_by'),
  processedAt: text('processed_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** How long to keep each data class. */
export const retentionPolicies = sqliteTable('retention_policies', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  entityType: text('entity_type').notNull(),
  keepYears: integer('keep_years').notNull(),
  action: text('action', { enum: ['archive', 'anonymise', 'delete'] }).notNull(),
  isActive: integer('is_active', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_retention_policies_school_id_entity_type'.slice(0, 63)).on(t.schoolId, t.entityType),
]);

// ============ Analytics, BI & predictions ============
/** Configurable dashboards per role/user. */
export const dashboards = sqliteTable('dashboards', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  roleId: text('role_id'),
  userId: text('user_id'),
  layout: text('layout', { mode: 'json' }),
  isDefault: integer('is_default', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Metric catalogue with query and thresholds. */
export const metrics = sqliteTable('metrics', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  keyName: text('key_name').notNull(),
  name: text('name').notNull(),
  definition: text('definition', { mode: 'json' }),
  unit: text('unit'),
  warnBelow: numeric('warn_below', { mode: 'number' }),
  warnAbove: numeric('warn_above', { mode: 'number' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_metrics_school_id_key_name'.slice(0, 63)).on(t.schoolId, t.keyName),
]);

/** Time series per metric per dimension. */
export const metricValues = sqliteTable('metric_values', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  metricId: text('metric_id').notNull(),
  period: text('period').notNull(),
  dimension: text('dimension'),
  value: numeric('value', { mode: 'number' }).notNull(),
}, (t) => [
  uniqueIndex('uq_metric_values_metric_id_period_dimension'.slice(0, 63)).on(t.metricId, t.period, t.dimension),
]);

/** Predicted risks per student with explanation, refreshed weekly. */
export const riskScores = sqliteTable('risk_scores', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  studentId: text('student_id').notNull(),
  riskType: text('risk_type', { enum: ['dropout', 'fee_default', 'result_decline', 'attendance'] }).notNull(),
  score: numeric('score', { mode: 'number' }).notNull(),
  factors: text('factors', { mode: 'json' }),
  computedAt: text('computed_at').notNull(),
  acknowledgedBy: text('acknowledged_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_risk_scores_student_id_risk_type'.slice(0, 63)).on(t.studentId, t.riskType),
]);

/** Detected anomalies (collection drop, attendance dip, SMS spike). */
export const anomalyAlerts = sqliteTable('anomaly_alerts', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  metricKey: text('metric_key').notNull(),
  detectedAt: text('detected_at').notNull(),
  expected: numeric('expected', { mode: 'number' }),
  actual: numeric('actual', { mode: 'number' }),
  severity: text('severity', { enum: ['info', 'warn', 'critical'] }).notNull(),
  status: text('status', { enum: ['open', 'acknowledged', 'resolved'] }).notNull(),
  details: text('details', { mode: 'json' }),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Anonymised cross-school benchmarks (platform level). */
export const benchmarkSnapshots = sqliteTable('benchmark_snapshots', {
  id: text('id').primaryKey(),
  period: text('period').notNull(),
  cohort: text('cohort').notNull(),
  metricKey: text('metric_key').notNull(),
  p25: numeric('p25', { mode: 'number' }),
  p50: numeric('p50', { mode: 'number' }),
  p75: numeric('p75', { mode: 'number' }),
}, (t) => [
  uniqueIndex('uq_benchmark_snapshots_period_cohort_metric_key'.slice(0, 63)).on(t.period, t.cohort, t.metricKey),
]);

// ============ AI assistant & automation copilots ============
/** Assistant chat sessions per user with channel. */
export const aiConversations = sqliteTable('ai_conversations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  userId: text('user_id').notNull(),
  channel: text('channel', { enum: ['web', 'app', 'whatsapp', 'sms'] }).notNull(),
  title: text('title'),
  context: text('context', { mode: 'json' }),
  lastMessageAt: text('last_message_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Messages with tool calls and token usage. */
export const aiMessages = sqliteTable('ai_messages', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  conversationId: text('conversation_id').notNull(),
  role: text('role', { enum: ['user', 'assistant', 'tool', 'system'] }).notNull(),
  content: text('content'),
  toolCalls: text('tool_calls', { mode: 'json' }),
  tokensIn: integer('tokens_in').notNull(),
  tokensOut: integer('tokens_out').notNull(),
  cost: numeric('cost', { mode: 'number' }).notNull(),
  createdAt: text('created_at').notNull(),
});

/** Generated artefacts (questions, remarks, lesson plans, notices, summaries) with review status. */
export const aiGenerations = sqliteTable('ai_generations', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  kind: text('kind', { enum: ['questions', 'remarks', 'lesson_plan', 'notice', 'summary', 'translation', 'report_narrative', 'other'] }).notNull(),
  requestedBy: text('requested_by'),
  input: text('input', { mode: 'json' }),
  output: text('output', { mode: 'json' }),
  model: text('model'),
  status: text('status', { enum: ['generated', 'reviewed', 'applied', 'discarded'] }).notNull(),
  appliedToType: text('applied_to_type'),
  appliedToId: text('applied_to_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** OCR/OMR of uploaded images (marks sheets, documents) into structured data. */
export const ocrJobs = sqliteTable('ocr_jobs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  fileId: text('file_id').notNull(),
  kind: text('kind', { enum: ['marks_sheet', 'omr', 'document', 'id_card', 'receipt'] }).notNull(),
  result: text('result', { mode: 'json' }),
  confidence: numeric('confidence', { mode: 'number' }),
  status: text('status', { enum: ['queued', 'done', 'needs_review', 'applied', 'failed'] }).notNull(),
  appliedToType: text('applied_to_type'),
  appliedToId: text('applied_to_id'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Per-school AI settings: enabled features, data scope, monthly budget, model. */
export const aiPolicies = sqliteTable('ai_policies', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  features: text('features', { mode: 'json' }),
  dataScope: text('data_scope', { mode: 'json' }),
  monthlyBudget: numeric('monthly_budget', { mode: 'number' }),
  model: text('model'),
  isEnabled: integer('is_enabled', { mode: 'boolean' }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Usage rollup for billing. */
export const aiUsageMonthly = sqliteTable('ai_usage_monthly', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  month: text('month').notNull(),
  tokensIn: integer('tokens_in').notNull(),
  tokensOut: integer('tokens_out').notNull(),
  cost: numeric('cost', { mode: 'number' }).notNull(),
}, (t) => [
  uniqueIndex('uq_ai_usage_monthly_school_id_month'.slice(0, 63)).on(t.schoolId, t.month),
]);

// ============ Marketplace, plugins & developer platform ============
/** Registered plugins/apps. */
export const plugins = sqliteTable('plugins', {
  id: text('id').primaryKey(),
  slug: text('slug').notNull(),
  name: text('name').notNull(),
  vendor: text('vendor'),
  description: text('description'),
  version: text('version').notNull(),
  hooks: text('hooks', { mode: 'json' }),
  settingsSchema: text('settings_schema', { mode: 'json' }),
  priceMonthly: numeric('price_monthly', { mode: 'number' }).notNull(),
  status: text('status', { enum: ['draft', 'published', 'suspended'] }).notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Plugin enabled for a school with settings. */
export const pluginInstalls = sqliteTable('plugin_installs', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  pluginId: text('plugin_id').notNull(),
  settings: text('settings', { mode: 'json' }),
  isEnabled: integer('is_enabled', { mode: 'boolean' }).notNull(),
  installedBy: text('installed_by'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
}, (t) => [
  uniqueIndex('uq_plugin_installs_school_id_plugin_id'.slice(0, 63)).on(t.schoolId, t.pluginId),
]);

/** OAuth2 clients for third-party apps and SSO. */
export const oauthClients = sqliteTable('oauth_clients', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  name: text('name').notNull(),
  clientId: text('client_id').notNull(),
  clientSecretHash: text('client_secret_hash').notNull(),
  redirectUris: text('redirect_uris', { mode: 'json' }),
  scopes: text('scopes', { mode: 'json' }),
  isConfidential: integer('is_confidential', { mode: 'boolean' }).notNull(),
  revokedAt: text('revoked_at'),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

/** Issued access/refresh tokens. */
export const oauthTokens = sqliteTable('oauth_tokens', {
  id: text('id').primaryKey(),
  schoolId: text('school_id').notNull(),
  clientId: text('client_id').notNull(),
  userId: text('user_id'),
  tokenHash: text('token_hash').notNull(),
  kind: text('kind', { enum: ['access', 'refresh', 'authorization_code'] }).notNull(),
  scopes: text('scopes', { mode: 'json' }),
  expiresAt: text('expires_at').notNull(),
  revokedAt: text('revoked_at'),
  createdAt: text('created_at').notNull(),
});

/** Installable packs: document templates, notification templates, chart of accounts, grading scales per board/country. */
export const templatePacks = sqliteTable('template_packs', {
  id: text('id').primaryKey(),
  slug: text('slug').notNull(),
  name: text('name').notNull(),
  kind: text('kind', { enum: ['documents', 'notifications', 'accounting', 'grading', 'curriculum', 'forms'] }).notNull(),
  locale: text('locale'),
  content: text('content', { mode: 'json' }),
  version: text('version').notNull(),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

export const tableNames = ['schools', 'campuses', 'users', 'roles', 'permissions', 'rolePermissions', 'userRoles', 'authSessions', 'pushSubscriptions', 'otpCodes', 'files', 'settings', 'customFields', 'customFieldValues', 'auditLogs', 'numberSequences', 'translations', 'featureFlags', 'installerState', 'systemHealth', 'outboxEvents', 'eventConsumptions', 'automationRules', 'automationRuns', 'scheduledJobs', 'backgroundJobs', 'approvalWorkflows', 'approvalRequests', 'approvalActions', 'tasks', 'formDefinitions', 'formSubmissions', 'workflowDefinitions', 'webhooks', 'webhookDeliveries', 'integrations', 'apiKeys', 'reportDefinitions', 'reportSnapshots', 'kpiDaily', 'importJobs', 'backups', 'notificationsQueueStats', 'saasPlans', 'saasSubscriptions', 'saasInvoices', 'saasUsage', 'saasPartners', 'saasPartnerPayouts', 'saasSupportTickets', 'cmsPages', 'cmsPosts', 'cmsMenus', 'cmsGalleries', 'cmsGalleryItems', 'cmsThemes', 'cmsDomains', 'cmsContactMessages', 'academicYears', 'terms', 'shifts', 'programs', 'classes', 'rooms', 'sections', 'subjects', 'classSubjects', 'studentSubjectChoices', 'courseRegistrations', 'periods', 'houses', 'calendarEvents', 'weeklyOffs', 'hifzProgress', 'departments', 'designations', 'staff', 'staffQualifications', 'staffSubjects', 'staffDocuments', 'students', 'studentEnrollments', 'guardians', 'studentGuardians', 'pickupAuthorisations', 'studentDocuments', 'studentStatusHistory', 'sectionSubjectTeachers', 'timetableVersions', 'timetableSlots', 'timetableSubstitutions', 'syllabi', 'syllabusUnits', 'learningOutcomes', 'lessonPlans', 'syllabusProgress', 'admissionCampaigns', 'admissionCampaignClasses', 'admissionEnquiries', 'enquiryFollowups', 'admissionApplications', 'applicationDocuments', 'admissionTests', 'admissionTestResults', 'admissionInterviews', 'admissionOffers', 'attendanceDevices', 'devicePunchLogs', 'studentAttendance', 'studentPeriodAttendance', 'staffAttendance', 'attendancePolicies', 'leaveTypes', 'leaveBalances', 'leaveApplications', 'attendanceMonthlySummary', 'gradingScales', 'gradingBands', 'competencyScales', 'examTypes', 'exams', 'examSchedules', 'examInvigilators', 'examSeatPlans', 'marks', 'competencyAssessments', 'examResults', 'annualResults', 'promotionRules', 'promotions', 'questions', 'questionPapers', 'questionPaperItems', 'onlineExams', 'onlineExamAttempts', 'omrSheets', 'boardRegistrations', 'courses', 'courseModules', 'lessons', 'courseEnrollments', 'lessonProgress', 'lessonQuizAttempts', 'assignments', 'assignmentSubmissions', 'studyMaterials', 'onlineClasses', 'onlineClassAttendance', 'discussions', 'diaryEntries', 'diaryAcknowledgements', 'dailyReports', 'studentRemarks', 'clubs', 'clubMemberships', 'competitions', 'competitionResults', 'housePoints', 'achievements', 'skillBadges', 'studentBadges', 'libraryCategories', 'libraryBooks', 'libraryBookCopies', 'libraryMembers', 'libraryIssues', 'libraryReservations', 'readingLogs', 'feeHeads', 'lateFineRules', 'feeStructures', 'feeStructureItems', 'studentFeeOverrides', 'discountSchemes', 'studentDiscounts', 'instalmentPlans', 'invoiceBatches', 'invoices', 'invoiceItems', 'paymentGateways', 'payments', 'paymentAllocations', 'refunds', 'studentLedgerEntries', 'feeReminders', 'cashSessions', 'feeCollectionDaily', 'fiscalYears', 'glAccounts', 'costCenters', 'bankAccounts', 'journalEntries', 'journalLines', 'expenseCategories', 'vendors', 'expenses', 'vendorBills', 'vendorPayments', 'otherIncomes', 'budgets', 'bankStatementLines', 'fixedAssetsLedger', 'taxRates', 'financialStatements', 'wallets', 'walletTransactions', 'posOutlets', 'posProducts', 'posSales', 'posSaleItems', 'shopOrders', 'jobPostings', 'jobApplicants', 'onboardingChecklists', 'staffContracts', 'workShifts', 'staffShiftAssignments', 'salaryComponents', 'salaryStructures', 'salaryStructureItems', 'taxSlabs', 'staffLoans', 'providentFundAccounts', 'payrollRuns', 'payslips', 'appraisalCycles', 'staffAppraisals', 'staffTrainings', 'staffExits', 'scholarshipFunds', 'scholarshipAwards', 'donors', 'fundraisingCampaigns', 'donations', 'vehicles', 'transportRoutes', 'routeStops', 'studentTransport', 'vehicleTrips', 'vehicleGpsLogs', 'transportBoardings', 'stopAlerts', 'vehicleMaintenance', 'fuelLogs', 'driverIncidents', 'hostels', 'hostelRooms', 'hostelBeds', 'hostelAllocations', 'hostelOutpasses', 'hostelVisitors', 'hostelAttendance', 'messMenus', 'mealRecords', 'laundryRecords', 'hostelComplaints', 'inventoryCategories', 'stores', 'inventoryItems', 'stockLevels', 'requisitions', 'quotations', 'purchaseOrders', 'purchaseOrderItems', 'goodsReceipts', 'stockMovements', 'issueRequests', 'assets', 'assetMaintenance', 'assetAudits', 'roomBookings', 'workOrders', 'cleaningSchedules', 'utilityReadings', 'safetyDrills', 'visitorLogs', 'gatePasses', 'callLogs', 'postalRecords', 'complaints', 'complaintUpdates', 'lostFoundItems', 'messagingProviders', 'notificationTemplates', 'notifications', 'notificationPreferences', 'notices', 'noticeReads', 'conversations', 'conversationParticipants', 'messages', 'ptmSlots', 'ptmBookings', 'surveys', 'newsletters', 'voiceBroadcasts', 'behaviourCategories', 'behaviourIncidents', 'disciplinaryActions', 'behaviourRules', 'healthRecords', 'vaccinations', 'clinicVisits', 'counsellingSessions', 'insurancePolicies', 'safeguardingCases', 'specialNeedsPlans', 'documentTemplates', 'documentRequests', 'issuedDocuments', 'idCards', 'printJobs', 'documentVerifications', 'alumni', 'alumniBatches', 'mentorshipPairs', 'jobBoardPosts', 'events', 'eventRsvps', 'eventTickets', 'eventScheduleItems', 'eventVolunteers', 'committees', 'committeeMembers', 'meetings', 'resolutions', 'policyDocuments', 'policyAcknowledgements', 'elections', 'electionVotes', 'govtReports', 'stipendPrograms', 'stipendEnrollments', 'consentRecords', 'dataRequests', 'retentionPolicies', 'dashboards', 'metrics', 'metricValues', 'riskScores', 'anomalyAlerts', 'benchmarkSnapshots', 'aiConversations', 'aiMessages', 'aiGenerations', 'ocrJobs', 'aiPolicies', 'aiUsageMonthly', 'plugins', 'pluginInstalls', 'oauthClients', 'oauthTokens', 'templatePacks'] as const;
